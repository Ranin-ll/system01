# 超管端能力扩展方案

> 需求来源：2026-09-17 用户口述 ——
> ①「超级管理员不仅可以看整体多维可视化分析，也可以下钻明细，按部门筛选或搜索，下钻一直到具体的个人或题目；
> 　而且可以管理所有部门管理员督促业务处理推进；也可以给系统所有人员发通知，或者给指定人员发。」
> ②「还有各部门每次考核的所有记录，可以一直下钻，横向要有可视化对比，其实就是**所有的业务数据超管都可以看到**。」
> ③「有些可以各部门横向比对，部门内部统计数据可以按部门分类筛选查看。」
>
> 方案文本（本文）+ 可视化稿：**`超管端整体设计稿-v3.html`**（14 节）。V2 原稿保持不变，供逐节对照。

---

## 〇、决策已定（2026-09-17 用户确认）

| # | 决策点 | **用户的选择** | 与我建议的差异 | 落地位置 |
|---|---|---|---|---|
| 1 | 目录形态 | 维持扁平**五目录**，不新增一级；新项挂「组织与人员」「培养运营」 | 与建议一致 | 设计稿 §3 |
| 2 | 通知用哪张表 | 新「通知中心」统一读写业务表 `notice`；`sys_notice` 保留原样做系统级公告 | 与建议一致 | 设计稿 §12；需补 `scope_type='USER'` + `notice_target` |
| 3 | 超管写权限 | `isGlobalReadOnly()` **收窄到业务实例**；账号 / 通知 / 督办放开写 | 与建议一致 | 设计稿 §1 |
| 4 | 补读缺口的方式 | 新增「超管分析」接口统一取数，**不改** `learning` / `practice` 既有的类级 `@PreAuthorize` | 与建议一致 | 设计稿 §2 / §8 |
| 5 | 实施顺序 | **P0 → P4** 逐段推进 | 与建议一致 | 设计稿 §13 |

> 5 条全部按建议执行，无否决项。原先的 §七「待确认」已作废，本文与设计稿均已按上表落地。

---

## 一、权限边界

### 1.1 写：只读于「业务实例」，账号 / 通知 / 督办放开

原实现把超管的写操作**整体**封死（`isGlobalReadOnly()` 命中即抛异常，覆盖 6 个业务 Service）。
本次需求明确：超管在**账号 / 通知 / 督办**三域必须有写权限。

| 能力域 | 超级管理员 | 部门管理员 | 落地方式 |
|---|---|---|---|
| 账号与组织 | ✅ 全量可写（建/停用部门管理员、改派导师、重置密码） | 仅本部门实习生 | 复用 `/system/user`（超管已授权） |
| 通知 | ✅ 全员 / 部门 / 岗位 / 指定人员 | 仅本部门 | 业务 `notice` 表（需补 `USER` 档） |
| 督办催办 | ✅ 任意部门管理员 / 实习生 | 本部门 | 新增 |
| 业务实例（课程/题库/考试/实操题） | ❌ **仍只读** | ✅ 本部门 | 保持 `isGlobalReadOnly()` |
| 报名审核 | ✅ 可写（现 `business:register:audit` 已给） | 本部门 | 现状保留 |
| 系统配置 / 菜单 / 字典 | ✅ | ❌ | 原生 |

→ `isGlobalReadOnly()` 的**适用范围收窄到「业务实例」**（Course / CourseContent / Exam / Question / QuestionBank / PracticeSubject 六个 Service 继续只读）。

### 1.2 读：**全量、无死角** —— 但现状有 3 个真缺口

> 需求 ② 的实质是：超管对**所有业务数据**拥有全局读权限，且要能一路下钻。

审计了 12 个业务 Controller 的注解（2026-09-17），分三类：

| # | 接口 | 注解 | 超管 | 处置 |
|---|---|---|---|---|
| **A. 已通（零改动）** | | | | |
| 1 | `/business/register/list`、`/summary` | `hasPermi('business:register:list')` | ✅ 有 | 超管命中 `isGlobalReadOnly()` → `currentScopeDeptId()` 返回 `null` → SQL 不带部门过滤，**天然全量** |
| 2 | `/business/course/list` | `business:course:list` | ✅ 有 | 同上 |
| 3 | `/business/exam/list`、`/bank/{id}/knowledge-points`、`/config/{id}` | `business:bank:list/query` | ✅ 有 | 同上 |
| 4 | `/business/question-bank/list`、`/business/question/list` | `business:bank:list` / `question:list` | ✅ 有 | 同上 |
| 5 | `/business/answer-sheet/grading/{examId}`、`/detail/{sheetId}` | `business:bank:list` | ✅ 有 | **正式考核逐场逐题记录已可读** |
| 6 | `/business/course/{id}/study-records` | `business:course:query` | ✅ 有 | 课程级学习记录已可读 |
| 7 | `/system/dept`、`/system/user`、`/monitor/operlog` | 原生 | ✅ 有 | — |
| **B. 缺授权（1 条 SQL 即可，零代码）** | | | | |
| 8 | `/business/position/{id}` | `business:position:query` | ❌ 缺 | 补授权（`list` 已有，仅详情缺） |
| **C. 缺接口（注解把超管挡在门外，改注解属禁区 → 必须新增接口）** | | | | |
| 9 | `/business/learning/**` | **类级** `hasAnyRoles('PRE_TRAINEE,FORMAL_TRAINEE')` | ❌ **完全读不到** | 课程列表 / 课程详情 / 进度 —— 下钻到「个人学习进度」会**断在这里** |
| 10 | `/business/practice/my`、`/record/{recordId}` | `hasAnyRoles('PRE_TRAINEE,FORMAL_TRAINEE')` | ❌ 读不到 | 模拟考核记录（含 80 条逐题明细）读不到 |
| 11 | `/business/answer-sheet/my` | `hasAnyRoles('PRE_TRAINEE,FORMAL_TRAINEE')` | ❌ 读不到 | 影响不大（正式记录走 `grading` 已可读） |

**结论**：#9、#10 是必须补的（否则「下钻到个人」的两条支线断裂）。
**不改既有注解**，改为在新增的「超管分析」接口里统一取数。

---

## 二、下钻：维度与链路

### 2.1 下钻轴（多轴交叉，层级不固定）

| 轴 | 粒度序列 |
|---|---|
| **组织** | 全局 → 部门 → 岗位 → 个人 |
| **考核** | 考核名称（批次）→ 场次（每次考核）→ 环节（理论 / 实操）→ 答卷 → 逐题 → 选项分布 |
| **学习** | 课程 → 章节 → 单项 → 个人进度 |
| **题库** | 题库 → 知识点 → 题目 → 作答记录 |

需求 ② 特别点到「**各部门每次考核的所有记录**」，即「组织轴 × 考核轴」是主路径：

```
L1 全局         KPI + 部门横向对比 + 场次横向对比
   ▼  点某个部门
L2 部门         该部门考核总览：批次 → 场次 → 环节，逐环节完成率/通过率
   ▼  点某一场次
L3 场次         该场次全量名单：应考/实考/缺考/通过，逐人分数
   ▼  点某个人
L4 个人 360     学习进度 · 模拟逐场逐题 · 正式逐场逐题 · 阶段评价 · 协议证书
   ▼  点某道错题
L5 题目         正确率 · 选项分布 · 所属知识点 · 出现过的场次与考生
```

- 要求**可以一直下钻**：实现为「下钻栈 + 面包屑」，任意一层可点回（不做固定 5 级硬编码）。
- 每层都带：**部门筛选**（可多选）+ **关键字搜索**（姓名 / 工号 / 手机号 / 考核名 / 题干 / 知识点）+ **时间窗**。

### 2.2 横向可视化对比（需求 ② 的「横向要有对比」）

| 对比对象 | 图形 | 适用作用域 | 说明 |
|---|---|---|---|
| 部门 × 部门 | 分组柱状 / 条形 | **需 ≥2 部门** | 在培人数、完成率、模拟均分、正式通过率，带排名 |
| 场次 × 场次 | 折线 + 柱状 | 全局 / 单部门 | 同一考核名称下多场次的分数走势与通过率 |
| 环节 × 环节 | 双柱 | 全局 / 单部门 | 理论 vs 实操，及格率差 |
| 部门 × 知识点 | **热力矩阵** | **需 ≥2 部门** | 行=部门、列=知识点、值=得分率 → 一眼看出哪个部门哪块弱 |
| 岗位 × 指标 | 分组柱 | 全局 / 单部门 | 5 个岗位的通过率与均分 |
| 个人 × 个人 | 雷达 / 箱线 | **需 ≥2 人**（同部门更佳） | 四维能力对比；同岗位内的分布与离群 |
| 分数段分布 | 直方图 | 全局 / 单部门 | 分数段分布，叠及格线参考线 |

### 2.3 作用域与筛选：三档视角（全局共用一个选择器）

需求 ② 其实区分了**两种视图语义** —— 「各部门横向比对」是**跨部门**的，
「部门内部统计」是**部门内**的，两者口径不同，不能混。**

| 档位 | 选择结果 | 视图语义 | 典型图表 | 自动降级规则 |
|---|---|---|---|---|
| **① 全局** | 不选部门（默认） | 各部门并列 | 部门横向比对、部门×知识点热力矩阵、场次趋势 | — |
| **② 多部门横向** | 选中 **≥2** 个部门 | 只在选中部门之间比对 | 分组柱、热力矩阵、部门排名 | 只剩 1 个 → 自动切「单部门」档 |
| **③ 单部门** | 恰好 **1** 个部门 | 该部门**内部**统计 | 本部门岗位分布、分数段直方图、个人明细与排名、本部门薄弱知识点 | 清空 → 回「全局」档 |

**统一规则**（避免同一页出现互相打架的口径）：

1. **一个页面只用一个作用域选择器**（页头工具栏），切换后**所有卡片同步刷新**；
   不做「每张卡片各自筛选」。
2. 部门筛选支持**多选**，并与 **岗位 / 考核场次 / 时间窗 / 关键字** 四个筛选器**叠加**。
3. **横向比对类卡片**（表中标「需 ≥2 部门」的）在「单部门」档下**自动降级**，不显示空表：
   - 部门×部门 分组柱 → 退化为「本部门 vs 全局均值」双柱
   - 部门×知识点 热力矩阵 → 退化为本部门「知识点得分率」条形图
4. **部门内统计类卡片**（岗位分布、个人排名、分数段）在「全局」档下改为**按部门分组**展示，
   或在单部门档下才高亮 —— 不让它出现「跨部门混算成一个数」的错误口径。
5. 全局档的部门对比必须带**排名**与**部门均值参考线**，方便一眼看出谁高谁低。

---

## 三、目录与页面（维持扁平五目录，不新增一级）

```
全局工作台                                    （不变）

组织与人员  ›  组织岗位
            ·  角色权限
            ·  账号管理                      （复用 /system/user，超管可写）
            ·  🆕 部门管理员                 清单 + 待办量 + 一键催办
            ·  🆕 人员下钻                   部门/岗位/状态/关键字 → 个人 360

培养运营    ›  课程与题库总览
            ·  考核运营总览
            ·  成绩与统计分析                🆕 补真数据（现为骨架）
            ·  🆕 考核记录下钻               部门 × 场次 × 环节 × 个人 全量记录
            ·  🆕 题目与知识点分析            题目级正确率 / 知识点得分率 / 薄弱 Top / 热力矩阵

规则与配置                                    （不变）
审计与合规                                    （不变）
─────────────────────────────
系统管理 › 通知公告                           原生，超管已授权 5 条菜单，**现在就能用**
```

> **待确认**：也可新开一级「通知与督办」，把「部门管理员」也挪进去。我倾向**不新增一级**。

---

## 四、数据来源与新增接口清单

### 4.1 可直接复用（零新增）

| 用途 | 接口 |
|---|---|
| 花名册 / 报名状态（全量） | `GET /business/register/list`、`/summary` |
| 课程（全量） | `GET /business/course/list` |
| 考核场次（全量） | `GET /business/exam/list` |
| 题库 / 题目（全量） | `GET /business/question-bank/list`、`/business/question/list` |
| **正式考核逐场逐题** | `GET /business/answer-sheet/grading/{examId}`、`/detail/{sheetId}` |
| 课程级学习记录 | `GET /business/course/{courseId}/study-records` |
| 部门树 / 账号 / 操作日志 | `/system/dept/list`、`/system/user/list`、`/monitor/operlog/list` |
| 全员通知 | `/system/notice`（超管已授权增删改查） |

### 4.2 需新增（均为**新增**接口，不改既有签名与注解）

| 新接口 | 用途 | 依赖表 | 覆盖缺口 |
|---|---|---|---|
| `GET /business/super/analysis/overview` | L1 全局 KPI + 部门/场次横向对比 | register / study_record / practice_record / answer_sheet | — |
| `GET /business/super/analysis/dept-stats` | L2 部门考核总览（批次→场次→环节） | exam / answer_sheet / register | — |
| `GET /business/super/analysis/exam-sessions` | L3 场次全量名单（应考/实考/缺考/分数） | exam / exam_participant / answer_sheet / sys_user | — |
| `GET /business/super/analysis/interns` | 人员下钻列表（`deptId`/`keyword`/`status`） | register + sys_user | — |
| `GET /business/super/analysis/intern/{userId}` | **L4 个人 360**（四段一次返回） | study_record / practice_record(_item) / answer_sheet(_item) / stage_evaluation | **补 #9 #10 读不到的支线** |
| `GET /business/super/analysis/questions` | L5 题目级正确率 + 选项分布 | answer_sheet_item + practice_record_item × question | — |
| `GET /business/super/analysis/knowledge-points` | 知识点得分率 + 部门×知识点热力矩阵 | 同上（按 `question.knowledge_point` 聚合） | 补 `ops/scores` 的「待聚合接口」 |
| `GET /business/super/dept-admins` | 部门管理员清单 + 各自待办量 | sys_user(is_dept_admin=1) + 待办源 | — |
| `GET /business/super/todos` | 督办看板四源聚合 | register_application / task_submission / task_assignment / study_record | — |
| `POST /business/super/urge` | 一键催办（可多选） | 写 `notice`（定向） | — |
| `GET/POST/PUT/DELETE /business/super/notice` | 定向通知 CRUD | `notice`（⚠️ 当前零 Java 层） | — |

### 4.3 通知：统一到业务 `notice` 表

| 表 | 能力 | 现状 |
|---|---|---|
| `sys_notice` | 全员公告（无范围概念） | ✅ RuoYi 原生，超管已授权，**可用** |
| `notice` | **已内建范围**：`scope_type = ALL / DEPT / POSITION` + `scope_id` + `is_top` + `effective_to` | ❌ 零 Java 层 |

方案：新「通知中心」统一读写 `notice`，并补一档 `scope_type = USER`（指定人员），
多选人员落一张 `notice_target(notice_id, user_id)`。`sys_notice` 保持原样（系统级公告），避免两个入口打架。

---

## 五、督办看板的四个待办源（均已存在，可直接算）

| 待办 | 判定 | 责任角色 |
|---|---|---|
| 待审报名 | `register_application.status = 'WAIT_AUDIT'` | 本部门 DEPT_ADMIN |
| 待批作业 | `task_submission.status = 'SUBMITTED'` 且未完成批阅 | `learning_task.reviewer_id` |
| 逾期任务 | `task_assignment.status = 'OVERDUE'` | 实习生 + 其部门管理员 |
| 学习停滞 | `study_record` 超 N 天无 `last_study_time` 更新且课程未完成 | 实习生 + 其部门管理员 |

> ⚠️ `learning_task` / `task_assignment` / `task_submission` 三张表**行数均为 0**，
> 演示阶段「待批作业 / 逾期任务」会是**真实空态** —— 不填假数字。

---

## 六、分阶段实施（建议）

| 阶段 | 内容 | 后端 | 前端 |
|---|---|---|---|
| **P0** | 通知入口归位（侧栏可达 + 全员通知可用） | 无 | 侧栏加一项 |
| **P1** | 部门管理员清单 + 督办看板 + 一键催办 | 3 个新接口 + `notice` 定向能力 | 2 个新页 |
| **P2** | 下钻 L1→L4（部门 / 场次 / 个人 360） | 5 个新接口 | 2 页 + 下钻栈 |
| **P3** | L5 题目级 + 知识点热力矩阵（补 `ops/scores` 真数据） | 2 个新接口 | 2 页 |
| **P4** | 横向对比可视化组件（雷达 / 热力 / 箱线） | 无（前端算） | 共享图表组件 |

---

## 七、决策（已定，见 §〇）

原「待确认 5 点」已于 2026-09-17 全部确认，**5 条均与建议一致，无否决项**。汇总如下（详见 §〇 表）：

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 目录形态 | 维持五目录，不新增一级 |
| 2 | 通知用表 | 统一到业务 `notice`；`sys_notice` 保留原样 |
| 3 | `isGlobalReadOnly()` | 收窄到业务实例；账号 / 通知 / 督办放开写 |
| 4 | 补读缺口方式 | 新增「超管分析」接口，不改既有注解 |
| 5 | 实施顺序 | P0 → P4 逐段推进 |

### 7.1 交叉影响（决策 3 连带）

- **后端**：`isGlobalReadOnly()` 的**调用点**从「6 个业务 Service 一刀切」改为「只在业务实例 Service 内生效」；新增的账号 / 通知 / 督办三块**不调用**它。既有方法签名、返回结构、权限注解**均不变**。
- **超管权限矩阵**：`business:psubject:add/edit/remove` 三个写权限超管本来就有，但被 `isGlobalReadOnly()` 拦住 —— 本次不改动这层重复保护，仅在设计稿 §1 标注说明。
- **表结构**：唯一改动是 `notice.scope_type` 补 `USER` 档 + 新增 `notice_target(notice_id, user_id)`（承接受众多人）。建议走幂等迁移脚本。
- **另一角色端（不在本稿范围，但必须点名）**：超管发出的 `notice` 通知要能在**实习生端 / 部门管理员端的顶栏铃铛**里真正看到 —— 这两处的消息中心目前是**演示态**，若不同步，会出现「超管发了、没人收到」。已写入设计稿 §13 的反向清单。
