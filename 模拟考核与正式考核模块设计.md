# 模拟考核与正式考核模块设计

## 1. 初版范围

本期目标是跑通“建题库 -> 发布考试 -> 实习生作答/提交成果 -> 自动或人工评分 -> 发布成绩”的核心闭环。

本期实现：

- 理论题库：单选、多选、判断，支持岗位、知识点、难度、分值、启停。
- 模拟考核：按规则随机抽题，不限次数，交卷后立即自动评分，可查看答案解析。
- 正式理论考试：按规则随机抽题，服务端计时，自动保存，到时自动交卷，客观题自动评分。
- 正式实操考试：管理员上传题目说明、视频或文档，实习生上传成果文件，管理员下载后人工评分。
- 成绩发布：理论和实操都完成后计算综合分，由部门管理员确认并发布。
- 权限与数据范围：部门管理员只能管理本部门岗位、考试、实习生与成绩；实习生只能访问本人数据。

本期暂不实现：

- 主观题及 AI 评分。
- 复杂防作弊、摄像头监考、切屏检测。
- PPT/Office 服务端转换；题目附件沿用现有文件浏览和下载能力。
- Excel 批量导题、错题本、能力画像算法。保留页面入口并提示“功能开发中”。

## 2. 角色与菜单

### 2.1 部门管理员

建议在“部门运营”下增加：

1. 题库管理
   - 理论题库
   - 实操题库
2. 考试管理
   - 模拟考核配置
   - 正式考核管理
3. 考核批阅
   - 待批阅实操
   - 待发布成绩
   - 已发布成绩

部门管理员的数据查询必须由后端注入当前 `dept_id`，不得仅依赖前端筛选。

### 2.2 实习生

侧栏仍只保留“工作台”和“消息中心”。工作台提供以下二级入口：

- 模拟考核
- 正式考核
- 历史考试记录

转为正式实习生后隐藏新的模拟、正式考核入口，但保留历史记录和已发布成绩。

### 2.3 超级管理员

- 维护题型、默认题量、默认通过线、文件白名单等全局参数。
- 查看全局统计和审计记录。
- 不直接修改部门题库、考试、评分等业务实例。

## 3. 页面设计

### 3.1 理论题库管理

列表字段：题目编号、题型、题干摘要、岗位、知识点、难度、默认分值、状态、版本、更新时间。

筛选项：关键词、岗位、题型、知识点、难度、状态。

新增/编辑抽屉：

- 基础信息：岗位、题型、知识点、难度、分值。
- 题干：多行文本。
- 选项：单选和多选至少 2 项，可增删、排序。
- 正确答案：单选选 1 项，多选选多项，判断选择“正确/错误”。
- 答案解析：模拟考核交卷后展示，正式考试仅成绩发布后按配置展示。
- 状态：草稿、启用、停用。

删除规则：未被考试引用的草稿可删除；已被引用的题目只能停用。考试答卷使用题目快照，不受题库后续修改影响。

### 3.2 实操题库管理

列表字段：题目编号、题目名称、岗位、难度、交付要求、附件数量、评分项数量、状态、更新时间。

编辑页采用三个页签：

1. 题目说明：名称、简介、详细要求、预计时长、成果格式、文件大小上限。
2. 题目资料：上传视频、PDF、PPT、Word、ZIP 等附件，可排序和删除。
3. 评分标准：评分项名称、分值、评分说明；各项分值之和必须等于实操满分。

题目附件与实习生提交成果必须分开存储和鉴权。

### 3.3 模拟考核配置

管理员配置：名称、适用岗位、题库范围、题量、题型配比、难度配比、知识点范围、总分、通过线、是否展示解析、是否允许重复参加。

发布前校验：

- 启用题目数量满足各项抽题配比。
- 各题型分值合计等于总分。
- 题量、通过线合法。

实习生端：

- 列表展示可参加的模拟考核、题量、时长、最近成绩、历史次数。
- 开始时创建新答卷并随机抽题，每次答卷独立保存。
- 答题页包含剩余时间、题号导航、已答/未答/标记状态、自动保存状态和交卷按钮。
- 交卷后立即自动评分，展示得分、正确率、各题答案和解析。
- 模拟成绩不进入正式考核结果，不影响转正门槛。

### 3.4 正式考核管理

一个正式考核是一个整体计划，包含“理论考试”和“实操考试”两个环节，不能作为互不关联的两场考试管理。

创建向导：

1. 基本信息：名称、部门、岗位、开始时间、截止时间。
2. 理论规则：题量、题型/难度/知识点配比、分值、时长、理论通过线。
3. 实操规则：选择实操题、实操通过线、提交截止时间、允许格式、大小上限。
4. 综合规则：理论权重、实操权重、综合通过线、学习完成率门槛、协议门槛、补考次数。
5. 发布确认：题库可用量、评分人、影响人数、规则摘要。

发布后冻结规则与题目版本。需要修改时应先停用，生成新版本后重新发布；已产生的答卷和提交记录继续引用原版本。

状态：`DRAFT` 草稿、`PUBLISHED` 已发布、`IN_PROGRESS` 进行中、`DISABLED` 已停用、`ENDED` 已结束。

### 3.5 正式理论考试

考试须知页显示：考试时间、时长、题量、总分、通过线、自动交卷和断网恢复说明。

开始考试时由服务端完成：

1. 校验协议、学习进度、考试时间、岗位、部门和剩余考试次数。
2. 创建答卷。
3. 根据规则随机抽题并写入本次答卷题目快照。
4. 返回服务端开始时间和截止时间。

答题过程中切题即时保存，并每 30 秒兜底保存。计时以服务端时间为准；刷新或短暂断网后恢复同一答卷。截止后由后端定时任务自动交卷。

客观题评分规则：

- 单选、判断：答案完全一致得分，否则 0 分。
- 多选：本期采用“完全匹配才得分”，不做部分得分和倒扣分。
- 未作答为 0 分。

交卷后只显示“已交卷，评分处理中”。正式答案、解析和最终分在成绩发布前不可返回给前端。

### 3.6 正式实操考试

实习生页面使用“左侧任务资料 + 右侧提交面板”布局：

- 任务资料：题目说明、视频/文档附件、交付要求、评分项、截止时间。
- 提交面板：成果名称、说明、Demo 链接、文件列表、上传进度、提交按钮。

允许在截止前保存草稿和替换文件；点击正式提交后进入 `SUBMITTED`。是否允许撤回由考试规则控制，初版默认不允许撤回，只允许管理员退回补交。

管理员批阅页：

- 查看考生、岗位、提交时间和是否逾期。
- 下载成果文件，查看 Demo 链接和提交说明。
- 按评分项逐项填写分数，系统自动汇总。
- 填写总体评语和改进建议。
- 保存草稿或确认评分。

### 3.7 成绩发布与记录

只有理论已自动评分、实操已人工评分后，结果才进入 `PENDING_PUBLISH`。

综合分计算：

`综合分 = 理论分 * 理论权重 + 实操分 * 实操权重`

通过条件默认同时满足：

- 综合分达到综合通过线。
- 理论分达到理论单项通过线。
- 实操分达到实操单项通过线。

部门管理员确认后发布成绩。实习生可查看理论分、实操分、综合分、是否通过、评语、发布时间和补考状态。已发布成绩不得直接覆盖，后续更正必须填写原因并生成新版本。

## 4. 关键状态机

### 4.1 答卷

`NOT_STARTED -> IN_PROGRESS -> SUBMITTED -> SCORED -> PUBLISHED`

异常分支：

- 截止时未开始：`ABSENT`
- 截止时进行中：自动交卷为 `SUBMITTED`
- 未通过且仍有次数：发布后附加 `RETAKE_AVAILABLE`

### 4.2 实操提交

`DRAFT -> SUBMITTED -> SCORING -> SCORED -> PUBLISHED`

异常分支：

- 截止未提交：`ABSENT`
- 管理员退回：`RETURNED`，允许在新截止时间前补交。

### 4.3 综合结果

`IN_PROGRESS -> WAIT_PRACTICE_SCORE -> PENDING_PUBLISH -> PUBLISHED`

实习生端把内部的评分等待状态统一显示为“评分处理中”或“待审核”，不暴露内部异常信息。

## 5. 数据模型调整

现有 `question`、`exam`、`exam_rule_snapshot`、`answer_sheet`、`answer_sheet_item`、`practical_submission`、`assessment_result` 可继续使用，但建议调整如下。

### 5.1 枚举修正

- `exam.exam_type` 使用 `MOCK`、`FORMAL`，不再用 `PRACTICE` 表示模拟，避免与实操环节混淆。
- `question.qtype` 初版仅启用 `SINGLE`、`MULTI`、`JUDGE`。

### 5.2 每次答卷题目快照

现有 `exam_paper_question` 只关联 `exam_id`，无法实现每次参加随机抽题。应增加 `answer_sheet_id`，或新建 `answer_sheet_question`：

- `answer_sheet_id`
- `source_question_id`
- `question_version`
- `seq`
- `qtype`
- `stem_snapshot`
- `options_snapshot_json`
- `answer_snapshot`（禁止在考生答题接口返回）
- `analysis_snapshot`
- `score`

随机抽题、题目顺序和选项顺序在答卷创建时确定并冻结。

### 5.3 实操题及附件

新增：

- `practical_task`：实操题主表。
- `practical_task_asset`：管理员题目视频/文档附件。
- `practical_score_item`：评分项和分值。
- `practical_submission_asset`：实习生提交的多个成果文件。

数据库只保存文件元数据和相对存储键，不保存二进制大文件。

### 5.4 发布版本

建议给 `exam` 增加 `version` 和 `current_rule_id`，并在发布时冻结：

- 考试规则。
- 题库筛选条件与题目版本。
- 实操题、附件与评分项版本。
- 适用部门、岗位和人员范围。

## 6. 后端接口规划

### 6.1 理论题库

- `GET /business/questions`
- `GET /business/questions/{id}`
- `POST /business/questions`
- `PUT /business/questions/{id}`
- `PUT /business/questions/{id}/status`
- `DELETE /business/questions/{id}`

### 6.2 实操题库

- `GET /business/practical-tasks`
- `POST /business/practical-tasks`
- `PUT /business/practical-tasks/{id}`
- `POST /business/practical-tasks/{id}/assets`
- `DELETE /business/practical-tasks/{id}/assets/{assetId}`
- `PUT /business/practical-tasks/{id}/score-items`

### 6.3 考试管理

- `GET /business/exams`
- `POST /business/exams`
- `PUT /business/exams/{id}`
- `GET /business/exams/{id}/publish-check`
- `POST /business/exams/{id}/publish`
- `POST /business/exams/{id}/disable`
- `POST /business/exams/{id}/new-version`

### 6.4 实习生理论考试

- `GET /business/my/exams`
- `GET /business/my/exams/{id}`
- `POST /business/my/exams/{id}/start`
- `GET /business/my/answer-sheets/{sheetId}`
- `PUT /business/my/answer-sheets/{sheetId}/answers/{itemId}`
- `POST /business/my/answer-sheets/{sheetId}/submit`
- `GET /business/my/answer-sheets/{sheetId}/result`

保存答案和交卷接口必须带 `version` 或幂等键，防止重复提交和并发覆盖。

### 6.5 实操提交与批阅

- `GET /business/my/exams/{id}/practical-task`
- `POST /business/my/practical-submissions`
- `POST /business/my/practical-submissions/{id}/assets`
- `PUT /business/my/practical-submissions/{id}`
- `POST /business/my/practical-submissions/{id}/submit`
- `GET /business/grading/practical-submissions`
- `GET /business/grading/practical-submissions/{id}`
- `GET /business/grading/practical-submissions/{id}/assets/{assetId}/download`
- `PUT /business/grading/practical-submissions/{id}/score-draft`
- `POST /business/grading/practical-submissions/{id}/confirm-score`

### 6.6 成绩

- `GET /business/grading/results`
- `GET /business/grading/results/{id}`
- `POST /business/grading/results/{id}/publish`
- `POST /business/grading/results/publish-batch`
- `POST /business/grading/results/{id}/corrections`
- `GET /business/my/results`
- `GET /business/my/results/{id}`

## 7. 文件存储与安全

- 延续在线学习模块的“数据库元数据 + 文件系统实际文件”模式。
- 建议目录：`upload/assessment/{examId}/tasks/...` 与 `upload/assessment/{examId}/submissions/{userId}/...`。
- 数据库保存相对存储键、原文件名、扩展名、MIME、大小、SHA-256、上传人和上传时间。
- 题目文件与成果文件统一通过鉴权下载接口访问，不直接暴露服务器绝对路径。
- 初版建议单文件上限 1 GB，成果可上传 ZIP；扩展名与 MIME 双重校验。
- 文件名使用 UUID，原文件名只作展示，防止路径穿越和重名覆盖。
- 下载、删除、提交、评分和发布均记录操作日志。

## 8. 开发顺序

1. 理论题库管理前端与后端 CRUD。
2. 模拟考核配置、随机组卷、答题、自动评分和历史记录。
3. 实操题库、题目附件和评分项。
4. 正式考核发布、理论答题、实操成果上传。
5. 管理员实操批阅、综合成绩计算与发布。
6. 工作台接入真实状态、消息提醒和历史记录。

第一阶段完成 1、2 后即可独立验收模拟考核；第二阶段完成 3、4、5 后形成正式考核完整闭环。

