# 页面改版说明 · 题库管理 / 模拟备考管理 / 正式考核管理

> 分支：`feature/exam-pages-redesign`（基于 `0ff82c3a`）　时间：2026-09-21
> 目标：**布局更清晰、操作更友好**。原则：已接后端的页面**只动 template + style**，script 仅**纯追加**，不改既有接口调用与业务逻辑。

---

## 一、统一版式：列表页「三段式」

```
① 总览 KPI（可点即筛选）  →  ② 筛选条（含"显示 N / M" + 重置）  →  ③ 列表 + 空态
```

三条硬规矩：

1. **KPI 一律用真实数据算**，绝不为好看编数（取不到就不显示该卡）。
2. **空态必须区分两种情况**——「没有数据」给"去新建"引导，「筛没了」给"清空筛选"按钮。
   两者文案相同的话，用户会以为数据丢了，进而去查库（本项目踩过这个坑）。
3. **"先配置、后运行"的页面必须留一个"能不能跑通"的校验位**——失败要在配置屏就被看见，而不是等终端用户点击时才暴露。

---

## 二、逐页改动

### 1) 题库管理 `views/business/questionBank/index.vue`

**改前**：页头 + 范围条 + 一张表，没有总览、没有筛选；题目数只有个数字，看不出"有没有题"。

**改后**
| 位置 | 内容 |
|---|---|
| 总览 4 卡 | 题库总数 / 题目总数 / 启用中 / 模拟题库　★ **点卡 = 一键套用该口径**（启用中→只看启用；模拟题库→只看模拟；题目总数→按题量排序；再点一次取消） |
| 筛选条 | 关键字（题库名 + 说明）、所属部门（仅超管）、类型（理论/模拟）、状态；右侧 `显示 N / M 个题库` + 排序切换 + 重置 |
| 表格 | **「类型」独立成列**（原先只藏在名称里的小标签）；题目数改为 `N` + 「题」/「未导入」副文案 |
| 空态 | 「暂无题库，点右上角新建」 / 「当前筛选条件下没有匹配的题库 + 清空筛选」 |

数据来源：全部来自既有的 `listBank({})` 返回（`questionCount` / `status` / `bankType` / `deptName`），**未新增任何接口**。

### 2) 正式考核管理 `views/business/exam/index.vue`

**改前**：inline 筛选表单较松散；表格 **9 列**在宽屏下拥挤；"点行选中 → 下方出配置卡"没有提示；**看不出某场考核能不能发布**。

**改后**
| 位置 | 内容 |
|---|---|
| 总览 4 卡 | 全部正式考核 / 待发布 / 已发布 / 待批改　★ 点卡 = 按状态筛选（真实计数：`examMode=FORMAL` 下按 status 各取一次 `total`，`pageSize=1`） |
| 筛选条 | 部门（超管）/ 名称 / 状态 + 查询 / 重置 + `当前列表 N 条` |
| 表格 | **9 列 → 8 列**：「卷面」与「通过线」合并为**一列并给出告警**；「时长」与「时间窗」合并为「考试安排」；列宽按 skill §4.7 规则用 `min-width`，避免宽屏被一列独吞 |
| ★ 校验位 | 「卷面 / 通过线」列下挂 `scoreCheck`：**满分 0 → 还没组卷/加题，无法发布**；**通过线 > 满分 → 无法发布**；**通过线 = 满分 → 必须全对**；正常 → 「余量 N 分」 |
| 选中联动 | 列表头显示「已选中：xxx」；配置区加独立标题条（含"改完点保存"提示）与「收起」；未选中时显示引导空态而非空白 |

> 为什么加这个校验位：后端**发布时**才拦"通过线不超卷面满分"，而运营在**列表里**看不见隐患。实测过真实案例——某场卷面满分 26 分、通过线却是 60 分。现在这种配置会在列表里直接红字提示。

### 3) 模拟备考管理 `views/department/study/prep/index.vue`

这页底子较好（dept 设计体系 + 组卷校验 callout + 「试抽一套」+ 内嵌实操题模块），因此**只做增量**：

| 页签 | 新增 |
|---|---|
| 备考资料 | 总览 4 卡：资料总数 / 已发布 / 待发布·已停用 / **覆盖岗位数**；筛选条：名称 + 类型（文档/视频/模拟题入口）+ 状态（已发布/草稿/已停用）+ `显示 N / M 份` |
| 模拟模块 | 总览 4 卡：模块总数 / 启用模块 / **理论模拟考核数** / **实操模拟题数**（后两项由各模块 `examCount`/`subjectCount` 求和）；筛选条：名称 + 状态 |
| 空态 | 两者都改为「没有数据」/「筛没了」两种文案 |

数据来源：`created → reloadAll()` 已同时加载 `materials` 与 `modules`，KPI 直接算已加载列表，**未新增接口**、未动 `switchTab`/组卷/试抽卷等既有逻辑。

---

## 三、验证（三条硬证据）

| 项 | 结果 |
|---|---|
| 静态三段校验（`check_sfc.js`） | 三个改版页 **+ 6 个既有页全部通过**；并反向验证**三类错误都能抓到**（模板标识符 / JS 语法 / SCSS 未定义变量） |
| 真实编译 | dev-server `Compiled successfully`（12:21 / 12:22 / 12:23）；`app.js` 里可 grep 到 `kpi-card`、`filteredBanks`、`applyKpi`、`scoreCheck`、`examFullScore`、`config-empty` 等新标识 |
| 真浏览器（CDP 探针 + 真实登录 `test_dev_admin`） | 三页均 **4 个总览卡渲染、无整页空白**：题库页实测 `2 个题库 / 48 题 / 2 启用 / 1 模拟题库`、`显示 2 / 2`；考核页开发部门 **0 场正式考核**（该部门只有模拟考核，故走新空态）；备考页 `0 份资料` |

> ⚠️ 生产构建 `vue-cli-service build` 尚未跑（dev 编译 + 真机渲染已通过）。如需最终确认可补跑一次。

---

## 四、顺带修好的工具问题（`check_sfc.js`）

静态校验脚本原先有三处漏检，**本项目每个表格页都会误报**，已修：

1. **`slot-scope="scope"` 未被当作局部变量** → 所有 `el-table` 页都报 `scope` 未定义（等于工具不可用）
2. **`$event` 不在全局白名单** → `department/messages/tasks.vue` 误报
3. **内联箭头函数参数**（`filter(x => …)`）→ `business/exam/grading.vue` 误报 `x`

修完后 **9 个页面零误报**，且反向验证三类错误仍能命中（不会因放宽而失效）。

---

## 五、正式考核：列表与配置页分离（12:31 追加）

**改动**：一级页只留列表 → **点行/点「配置」进入独立配置页**（`views/business/exam/config.vue`）。

| 项 | 内容 |
|---|---|
| 列表页 | 移除内嵌配置卡与引导空态；**整行可点**（cursor:pointer）进入配置页；表头提示「点任意一行进入该场考核的配置页」 |
| 配置页 | 顶部「返回列表 + 名称/标签 + 4 KPI（卷面满分/通过线/题目量/时长）」；★「能否发布」结论用 `el-alert` 放在**最上面**；★ **「基本参数」卡（考核名称 / 考试时长 / 通过线）就地可改**，保存走 `updateExam`（与「编辑考核」弹窗同一接口），带实时提示「余量 N 分 / 高于满分无法发布」与「只读」态；主体复用既有 `ExamConfigCard`（组卷/试抽/发布设置，逻辑零改动） |
| 路由 | **单条顶层路由** `/exam-config/:examId`（照抄批改页做法）+ 跳转带 `?from=<列表路径>`，返回用 `query.from` |
| 配置卡 | `.dm-grid` 由 4fr/8fr 调成 **3fr/9fr**（组卷区更宽），≤1280px 自动堆叠 |
| 编辑回跳 | 配置页点「编辑」→ 带 `?edit=<id>` 回列表 → 自动打开编辑弹窗（表单只在列表页维护一份） |

★ **为什么不按路径前缀猜配置页地址**：同一份列表页在不同角色下 URL 不同 —— 部门管理员是 `/department/study/exam`，**超管是 DB 菜单树生成的 `/assessment/department/exam`**（实测超管访问 `/business/exam`、`/department/study/exam` 都 404）。所以统一用顶层路由 + `?from=`。

**真机验证**：部门端列表（1 行）点行 → `/exam-config/2?from=…`，告警「当前配置无法发布」（卷面 26 < 通过线 60）、KPI 正确，点「返回列表」回到原列表 ✓；超管端 `/assessment/department/exam`（2 行）点行 → `/exam-config/28?from=…`，标题 `001 理论考核 待发布 交付部门`、无告警、卷面 100 ✓；配置页点「编辑」→ 回列表并自动弹出编辑框（名称已回填）✓

## 六、状态与下一步

- 三个 `.vue` + 新增的 `config.vue`、`router/index.js` 改动**全部在工作区未提交**（分支 `feature/exam-pages-redesign`）
- 建议顺序：① 先在浏览器里过一遍这三页 → ② 满意后提交并推送该分支 → ③ 顺带推 `feature/exam-merge-cleanup`（含 `0ff82c3a` 的 `filePreview.js` 修复，**GitHub 上的 main 目前仍是坏的**）
- 下一轮可继续：题库详情页（`questionBank/detail.vue`）、成绩管理（`department/study/scores`）、以及「考核与成绩」L0/L1/L2 三板（设计修订清单已备）
