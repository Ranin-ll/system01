# 在线学习板块开发规划

> 规划版本：V1.1  
> 编制日期：2026-09-11  
> 适用阶段：本地开发初版，优先实现核心业务闭环，保留服务器迁移能力

## 1. 模块目标

在线学习板块承接注册审核和保密协议之后的第一段核心业务：

`部门管理员创建岗位课程 -> 编排章节与学习单项 -> 发布课程 -> 对应岗位实习生学习 -> 自动记录进度 -> 汇总课程完成率 -> 回写工作台`

初版完成后，系统中的课程、学习进度、完成时间和工作台统计必须来自数据库，不再使用前端静态数据。

### 1.1 腾讯云参考页面取舍

参考页面：[腾讯云培训认证详情](https://cloud.tencent.com/edu/training/cert/detail?type=CCB)

该页面的主要信息顺序是“认证简介 -> 在线学习 -> 章节目录 -> 备考资料 -> 考试 -> 成绩 -> 常见问题”。本项目借鉴其清晰的内容层级和章节目录，不复制认证购买等外部业务。

| 参考页面能力 | 本项目处理方式 |
|---|---|
| 顶部认证简介、更新时间、考试类型 | 改为岗位培养概览、课程完成率、最近学习时间 |
| 在线课程 / 视频讲解切换 | 简化为“全部 / 文档 / 视频”分段筛选 |
| 按章节展示大量学习单项 | 保留章节折叠列表和“展开全部” |
| 每个单项提供“去学习” | 根据状态显示“开始学习 / 继续学习 / 回看” |
| 备考资料、考试、成绩、常见问题 | 仍由工作台各自下钻，不重复堆进学习详情页 |
| 购买认证、考试预约、考位查询 | 不属于内部实习系统，不实现 |
| 企业培训、商品价格、营销区块 | 不实现 |

视觉上沿用本项目现有平台后台框架，仅吸收参考页的内容分区、章节编号和直接操作入口，不复制腾讯云品牌、导航或营销页面样式。

## 2. 当前实现盘点

### 2.1 已具备

- 数据库已有 `course`、`course_chapter`、`study_item`、`study_record` 四层基础结构。
- `study_record` 已具备用户、课程、章节、学习单项、学习状态、学习时长、播放进度和完成时间等基础字段。
- 后端已有课程基础 CRUD 和发布接口。
- 前端已有课程管理基础列表、编辑弹窗和实习生工作台学习入口。
- 五个部门、五类岗位及测试账号已经建立，可作为数据隔离验收基础。

### 2.2 当前缺口

- 四张学习相关表目前均无业务数据。
- 课程管理未按部门过滤，部门管理员存在跨部门访问风险。
- 超级管理员当前拥有课程增删改发布权限，与需求文档中“只看全局统计，不修改部门业务实例”的规则不一致。
- 课程编辑只支持课程主信息，不支持章节和文档/视频单项编排。
- 岗位使用数字 ID 输入，未接入岗位下拉和部门自动匹配。
- 发布操作未校验章节、学习单项、文件和完成规则是否完整。
- 发布后的课程仍可破坏性修改或删除，可能损坏历史学习记录。
- 尚无实习生课程列表、课程详情、打开记录、进度保存、完成确认和学习汇总接口。
- 工作台中的课程完成率、课程列表和学习待办仍是静态展示。
- `Course.avgCompletionRate` 已预留，但当前查询没有计算该字段。

## 3. 本阶段范围

### 3.1 P0：本轮必须实现

1. 部门管理员管理本部门对应岗位的课程。
2. 课程包含多章节，章节包含文档或视频学习单项。
3. 草稿课程可编辑、排序、删除；完整性校验通过后才可发布。
4. 实习生只能看到本人岗位下已发布的课程。
5. 文档滚动到末尾并主动确认后完成。
6. 视频保存播放位置和最高播放进度，达到配置阈值后完成，默认阈值为 100%。
7. 学习记录按用户和单项唯一保存，重复请求不产生重复记录。
8. 自动汇总单项、章节、课程和个人总体完成率。
9. 工作台学习统计、最近课程和待学习事项接入真实数据。
10. 正式实习生继续保留课程及历史学习记录，并可继续查看已发布课程。
11. 部门管理员查看本部门课程学习统计和个人明细。
12. 超级管理员只查看全局学习统计，不提供课程编辑、发布或删除操作。

### 3.2 P1：页面骨架完整，本轮不做真实业务

- 章节测试：保留类型和页面区域，点击提示“功能开发中”。后续与题库/模拟考试引擎复用。
- 学习任务、作业提交和任务讨论：保留工作台入口或静态页，不计入本轮课程完成率。
- 线下学习、签到和补课：静态展示。
- 课程批量导入、复制课程、课程版本对比和定时发布。
- DOCX/PPT 在线解析、倍速限制、防拖动、防挂机和复杂学习行为分析。
- 对象存储、CDN、转码、病毒扫描。本轮保留存储接口边界。

## 4. 角色与权限

| 角色 | 数据范围 | 查看课程 | 编辑课程 | 学习课程 | 查看统计 |
|---|---|---:|---:|---:|---:|
| 超级管理员 | 全局 | 是 | 否 | 否 | 全局聚合和下钻只读 |
| 部门管理员 | 本部门 | 是 | 是 | 否 | 本部门及本部门人员明细 |
| 预备实习生 | 个人 + 本人岗位 | 已发布课程 | 否 | 是 | 仅本人 |
| 正式实习生 | 个人 + 本人岗位 | 已发布及历史课程 | 否 | 是 | 仅本人 |

权限必须在后端校验，前端隐藏按钮只能作为界面优化，不能代替鉴权。

部门管理员新增课程时：

- `dept_id` 强制取当前登录管理员所属部门，不接收前端任意传值。
- `position_id` 必须是当前部门通过 `dept_position` 绑定的岗位。
- 查询、详情、修改、发布、停用和统计均重复校验部门范围。

## 5. 页面与信息架构

### 5.1 部门管理员：课程管理与学习统计（DA-04）

建议路由：`/assessment/department/courses`

页面使用三个页签，不增加重复一级菜单：

#### 课程列表

- 顶部指标：课程总数、草稿、已发布、应学人数、平均完成率。
- 筛选：课程名称、岗位、必修/选修、状态、发布时间。
- 列表字段：课程名称、岗位、必修/选修、章节数、应学人数、已学习人数、平均完成率、状态、发布时间。
- 操作：查看、编辑、发布、停用、查看统计。
- 草稿允许删除；已发布课程不允许物理删除。

#### 课程编辑

采用独立页面或全宽抽屉，避免继续使用狭窄弹窗：

- 基础信息：名称、所属部门（只读）、适用岗位（下拉）、必修/选修、简介、封面。
- 章节编排：新增、重命名、排序、删除章节。
- 单项编排：新增文档、视频；设置标题、文件、时长、排序、必修和完成规则。
- 发布检查：显示缺失项清单，通过后才允许发布。
- 已发布且产生学习记录后，结构和文件锁定；可停用，但不能破坏历史记录。

#### 学习统计

- 筛选：课程、章节、单项、人员、学习状态和时间范围。
- 汇总：应学人数、已开始、已完成、未完成、平均完成率、累计学习时长。
- 人员表：姓名、账号、岗位、课程完成率、已完成章节、最近学习时间、累计学习时长。
- 点击人员打开学习明细抽屉，展示每个章节和单项的状态、进度及完成时间。

### 5.2 实习生：在线学习列表（IN-03）

建议路由：`/assessment/intern/learning`，作为工作台下钻页，不出现在一级菜单。

- 顶部采用参考页“认证简介”的结构，改为岗位培养概览：岗位、部门、总体完成率、已完成课程和累计学习时长。
- 概览下直接展示课程列表，不设置多层分类导航。
- 状态使用“全部 / 学习中 / 已完成”分段筛选；必修/选修作为轻量下拉筛选。
- 课程列表：课程名称、简介、必修/选修、章节数、完成率、累计时长、最近学习时间、状态。
- 主操作根据状态显示“开始学习”“继续学习”或“回看课程”。
- 无课程时展示空状态，不把“暂无课程”显示为 0 分或失败。
- 初版不做课程推荐、排行榜、收藏、评分、评论和证书营销区块。

### 5.3 实习生：课程学习详情

建议路由：`/assessment/intern/learning/course/:courseId`

- 顶部课程概览：课程名、岗位、必修/选修、章节数、完成率、最近学习时间，以及一个主要“继续学习”按钮。
- 概览下使用“全部 / 文档 / 视频”分段控件，只过滤当前课程目录，不切换到新页面。
- 中部参考腾讯云页面使用章节折叠目录；每章显示完成单项数，支持“展开全部”。
- 单项行展示类型图标、编号、标题、预计时长、状态和一个操作按钮。
- 点击单项后进入同一路由的学习状态：左侧保留紧凑章节目录，右侧显示 PDF 阅读器或原生视频播放器。
- 学习状态顶部显示课程完成率和最近保存状态，底部只保留“上一项 / 下一项”和文档的“确认完成阅读”。
- 离开页面前保存最后位置；网络异常时本地暂存，恢复后自动重试。

文档初版建议只支持 PDF 在线预览，使用 PDF.js 实现可控的滚动末尾检测。视频初版支持 MP4/WebM，使用浏览器原生播放器。

简化页面结构：

```text
返回工作台
课程名称                         完成率 42%   [继续学习]
岗位 / 必修 / 3 章 / 最近学习时间

[全部] [文档] [视频]                         [展开全部]

第一章  入职与保密                         1 / 2
  1.1  [文档] 岗位规范与保密要求     已完成    [回看]
  1.2  [视频] 信息安全操作说明       42%       [继续]

第二章  岗位实操流程                       0 / 1
  2.1  [视频] 开发交付流程           未开始    [开始]
```

初版一个页面只保留一个主按钮；备考资料、正式考试、成绩和常见问题不在这里重复展示，统一返回工作台进入对应模块。

### 5.4 工作台接入

替换当前静态数据：

- “课程完成率”取本人必修课程实时汇总。
- “在线学习”显示最近学习的 3 个单项。
- “当前任务”生成待学习项和即将完成课程提示。
- 培养档案中的在线学习进度取真实统计。
- 无课程时显示“暂无已发布课程”，不显示虚构的 62%。

## 6. 状态与完成规则

### 6.1 课程状态

`DRAFT（草稿） -> PUBLISHED（已发布） -> DISABLED（已停用）`

- 草稿：仅本部门管理员可见，可编辑和删除。
- 已发布：对应岗位实习生可见；产生学习记录后禁止破坏性修改。
- 已停用：新进入者不再看到；已经产生记录的用户仍可查看历史及完成情况。

### 6.2 学习单项状态

`NOT_STARTED（未开始） -> IN_PROGRESS（学习中） -> DONE（已完成）`

- 首次打开创建或更新学习记录，并写入 `first_open_time`。
- 后续学习更新 `last_study_time`、累计时长和最近位置。
- `DONE` 为幂等终态，重复完成请求不重复累计时长或生成记录。

### 6.3 文档完成

1. 打开文档后记录首次打开和学习开始时间。
2. PDF.js 检测最后一页进入可视区域，前端点亮完成按钮。
3. 用户点击“确认完成阅读”。
4. 后端记录 `read_confirm=1`、`status=DONE`、`progress=100` 和完成时间。
5. 未达到末尾时，前端不可提交；后端仍需校验确认参数和当前材料类型。

### 6.4 视频完成

1. `timeupdate` 更新最近播放位置；每 10 至 15 秒节流保存一次。
2. 页面隐藏、切换单项或离开页面时补充保存。
3. `progress` 保存历史最高进度，`last_position_seconds` 保存续播位置。
4. 达到单项完成阈值后自动标记完成，默认 100%。
5. 本期允许快进和拖动，不做防作弊判断。

### 6.5 汇总口径

- 单项完成：学习记录状态为 `DONE`。
- 章节完成：章节内全部必修单项完成。
- 单门课程完成率：已完成必修章节数 / 必修章节总数 × 100%。
- 单门课程完成：完成率为 100%。
- 个人总体完成率：本人岗位全部已发布必修课程的完成率简单平均。
- 部门课程平均完成率：课程对应全部有效实习生的个人课程完成率简单平均。
- 选修课程展示进度，但不计入正式考核资格和个人总体必修完成率。
- 分母为 0 时返回 `null` 和“暂无课程”，不得伪装为 0%。

正式考核资格本轮只输出计算结果供工作台展示；考试模块接入后再作为入口拦截条件。

## 7. 数据模型调整建议

保留现有四张表，新建幂等迁移脚本 `intern_assessment_learning_core.sql`，不直接重建业务库。

### course 增补

- `dept_id`：课程所属管理部门，用于稳定的数据权限边界。
- `sort_no`：课程排序。
- `version_no`：发布版本，初版默认 `V1.0`。
- `version`：乐观锁版本号。

### course_chapter 增补

- `intro`：章节说明。
- `version`：乐观锁版本号。

### study_item 增补

- `file_name`、`file_size`、`file_ext`：文件元数据。
- `content_version`：材料版本快照来源。
- `completion_threshold`：视频完成阈值，默认 100。
- `version`：乐观锁版本号。

### study_record 增补

- `last_position_seconds`：视频续播位置。
- `content_version`：完成时的材料版本。
- `completed_by`：完成来源，初版固定 `SELF`，为后续管理员补录预留。

不新增课程完成汇总表。初版由学习单项记录实时汇总，数据量增加后再引入统计快照或缓存。

## 8. 文件存储与迁移

- 复用平台统一上传接口，但数据库只保存相对存储键或可迁移 URL，不保存 `E:\...` 等本机绝对路径。
- 本地开发文件存入配置目录，部署服务器时通过配置切换根目录。
- 文件访问必须经过登录和业务权限校验，不能仅凭 URL 绕过岗位和个人范围。
- 初版白名单建议：文档 `PDF`，视频 `MP4/WebM`，封面 `PNG/JPG/WebP`。
- 文件大小限制从系统参数读取，前后端同时校验，以后可平滑替换为 MinIO/OSS。

## 9. API 规划

### 9.1 部门管理员课程管理

- `GET /business/course/list`：按部门过滤的课程列表。
- `GET /business/course/{id}`：课程主信息及发布状态。
- `POST /business/course`：创建草稿课程。
- `PUT /business/course/{id}`：更新草稿基础信息。
- `PUT /business/course/{id}/structure`：事务保存章节和单项结构。
- `POST /business/course/{id}/publish`：执行发布校验并发布。
- `POST /business/course/{id}/disable`：停用课程。
- `DELETE /business/course/{id}`：仅删除未发布且无记录的草稿。
- `GET /business/course/{id}/publish-check`：返回发布检查清单。

### 9.2 学习统计

- `GET /business/course/{id}/statistics`：课程汇总统计。
- `GET /business/course/{id}/students`：人员学习列表。
- `GET /business/course/{id}/students/{userId}`：个人课程学习明细。
- `GET /business/learning/statistics/global`：超级管理员全局只读统计。

### 9.3 实习生学习

- `GET /business/learning/summary`：本人学习汇总，供工作台和列表页复用。
- `GET /business/learning/courses`：本人岗位已发布课程列表。
- `GET /business/learning/courses/{courseId}`：课程结构和本人进度。
- `POST /business/learning/items/{itemId}/open`：首次打开/开始学习。
- `PUT /business/learning/items/{itemId}/progress`：保存时长、视频位置和进度。
- `POST /business/learning/items/{itemId}/complete`：文档确认或视频完成。

所有写接口启用平台重复提交保护；进度更新使用 `study_record.version` 做并发控制，服务端只接受合法范围内的进度和时长。

## 10. 发布校验

课程发布前必须同时满足：

- 课程名称、岗位、部门、必修属性和简介完整。
- 至少有一个章节。
- 每个必修章节至少有一个必修学习单项。
- 文档单项已上传有效 PDF。
- 视频单项已上传有效视频，完成阈值在 1 至 100 之间。
- 章节和单项排序无重复或由服务端重新标准化。
- 当前岗位确实属于当前管理员部门。

发布动作写入操作日志，记录课程、版本、操作人、部门、发布时间和校验结果。

## 11. 初始演示数据

建议为五类岗位各建立一门草稿课程，便于部门管理员自行补充材料后发布：

- 实施实习生：项目实施与交付规范
- 开发实习生：开发流程与代码交付规范
- 设计实习生：设计规范与成果交付
- 质检实习生：质量检查与缺陷处理规范
- 建模实习生：建模流程与文件规范

每门课程预置两个空章节：“岗位基础规范”“岗位实操流程”。空章节课程不能发布，从而同时验证发布校验。

首个端到端验收使用 `test_dev_admin` 和 `test_dev_intern`：开发部门管理员补齐并发布课程，开发实习生完成学习，其他岗位账号不可见。

## 12. 验收标准

### 12.1 管理端

1. 开发部门管理员只能新增和管理开发实习生岗位课程。
2. 课程无章节、章节无单项或单项无文件时无法发布，并显示具体原因。
3. 发布后开发实习生可见，其他岗位实习生不可见。
4. 已产生学习记录的课程不能破坏性删除或修改结构。
5. 管理员可查看本部门人员完成率、时长和最近学习时间。
6. 超级管理员可查看全局统计，但页面无新增、编辑、发布和删除按钮，直接调用写接口返回无权限。

### 12.2 实习生端

1. 未签署保密协议时不能进入学习页面。
2. 只返回本人岗位的已发布课程和本人学习记录。
3. 文档未滚动到底时完成按钮不可用；完成后刷新仍为已完成。
4. 视频进度刷新后保留，重新打开可从最近位置继续播放。
5. 完成全部必修单项后章节自动完成，课程和工作台完成率同步更新。
6. 同一完成请求重复提交不会生成重复记录或重复累计。
7. 正式实习生仍可查看课程和历史学习数据。

### 12.3 技术验证

- 后端 Maven 构建通过，核心服务包含单元测试或集成测试。
- 前端生产构建通过。
- 使用桌面宽屏和 390px 宽度截图检查目录、正文、播放器和按钮无重叠。
- MySQL 验证 `study_record` 唯一约束、状态、时长、进度和完成时间正确。
- 后端接口验证跨部门、跨岗位和跨用户访问均被拒绝。

## 13. 推荐开发顺序

1. 建立学习模块迁移脚本、演示数据和角色菜单权限。
2. 完成课程部门范围、发布校验和发布后保护规则。
3. 完成章节/单项领域对象及课程结构保存接口。
4. 重做部门管理员课程列表和课程编辑页。
5. 完成实习生课程列表、详情和学习记录接口。
6. 接入 PDF.js、视频进度保存和断线重试。
7. 实现完成率汇总和部门学习统计。
8. 将工作台静态学习数据替换为真实接口。
9. 完成角色级接口测试、浏览器交互验证和数据库回查。

## 14. 本轮完成定义

以下场景全部通过，才算在线学习核心闭环完成：

> `test_dev_admin` 创建一门开发岗位课程，添加一个 PDF 文档和一个视频单项并发布；`test_dev_intern` 登录后只看到该岗位课程，完成文档和视频后刷新页面进度仍保留，课程完成率变为 100%，工作台同步更新；其他岗位实习生看不到该课程；超级管理员只能查看全局统计，不能修改课程。
