# 在线学习课程管理接口与存储设计

> 版本：V1.0  
> 适用分支：`feature/online-learning-phase2`  
> 目标：部门管理员编排本部门岗位课程，实习生只查看本人岗位已发布课程。

## 1. 本轮前端框架

课程管理路由仍为 `/assessment/department/courses`，不新增一级菜单。当前页面分为三个工作区：

- 课程目录：课程基本信息、适用岗位、草稿/已发布状态、章节数、学习统计入口。
- 课程内容抽屉：章节拖拽排序、资料拖拽排序、新增/编辑/删除章节和学习资料；文档、视频、章节测试使用不同的完成规则。
- 学习记录抽屉：按人员查看课程完成率和最近学习时间，后续接真实统计接口。

当前前端已经提供：

- 发布前检查：至少一个章节、每个章节至少一个学习资料、文档/视频必须绑定文件。
- 视频完成阈值：默认 100%，可在 80% 到 100% 之间配置。
- 文件选择元数据：文件名、大小、扩展名、待上传状态；当前不会把二进制写入浏览器 LocalStorage。
- 章节和资料拖拽排序；当前演示数据保留在本地，接口接入后改为保存到服务器。
- 章节测试保留入口，但题目编排复用考核题库，本阶段点击后提示功能开发中。

## 2. 数据权限

后端必须把部门范围作为强制条件，不能依赖前端隐藏按钮：

- 超级管理员：可查看全局课程和统计，不允许新增、修改、发布、停用或删除部门课程。
- 部门管理员：只能操作当前 `dept_id` 绑定岗位的课程。
- 预备/正式实习生：只能查看本人 `position_id` 对应的已发布课程；正式实习生保留历史课程和学习记录。
- `dept_id` 不接受前端传入作为权限依据，新增和修改时始终从登录账号读取；`position_id` 必须存在于 `dept_position` 有效绑定中。

## 3. 课程内容接口

接口前缀：`/business/course`。所有管理接口均需要登录、权限注解和部门范围校验。

| 方法 | 路径 | 权限 | 作用 |
|---|---|---|---|
| GET | `/{courseId}/contents` | `business:course:query` | 返回章节、学习资料和文件元数据 |
| PUT | `/{courseId}/contents` | `business:course:edit` | 草稿整体保存，适合拖拽排序和批量保存 |
| POST | `/{courseId}/chapters` | `business:course:edit` | 新增章节 |
| PUT | `/chapters/{chapterId}` | `business:course:edit` | 修改章节名称、必修标记、排序 |
| DELETE | `/chapters/{chapterId}` | `business:course:edit` | 草稿章节逻辑删除 |
| POST | `/chapters/{chapterId}/items` | `business:course:edit` | 新增学习资料 |
| PUT | `/items/{itemId}` | `business:course:edit` | 修改资料标题、类型、完成规则、排序 |
| DELETE | `/items/{itemId}` | `business:course:edit` | 草稿资料逻辑删除 |
| POST | `/items/{itemId}/asset` | `business:course:edit` | 上传或替换资料文件 |
| GET | `/{courseId}/study-records` | `business:course:record` | 部门管理员查看课程学习统计 |

### 3.1 查询内容响应建议

```json
{
  "courseId": 12,
  "versionNo": "V1.0",
  "status": "DRAFT",
  "chapters": [
    {
      "id": 101,
      "chapterName": "第一章 入职与岗位规范",
      "sortNo": 1,
      "isRequired": 1,
      "items": [
        {
          "id": 1001,
          "itemTitle": "保密要求说明",
          "itemType": "DOC",
          "sortNo": 1,
          "isRequired": 1,
          "completionRule": "SCROLL_END",
          "completionThreshold": null,
          "asset": {
            "id": 9001,
            "fileName": "保密要求说明.pdf",
            "fileExt": "pdf",
            "fileSize": 1834200,
            "mimeType": "application/pdf",
            "status": "READY",
            "previewUrl": null
          }
        }
      ]
    }
  ]
}
```

### 3.2 上传流程

初版可使用 `multipart/form-data` 上传到后端，后端完成鉴权、大小校验、摘要计算和存储；视频较大时改为两阶段预签名上传：

1. `POST /business/file-assets/prepare`：传课程、资料类型、文件名、大小、SHA-256，返回 `assetId` 和上传地址。
2. 浏览器把文件直传本地文件服务、MinIO、腾讯云 COS 或 S3，不经过应用内存。
3. `POST /business/file-assets/{assetId}/complete`：后端核对大小和摘要，写入 `READY`。
4. `POST /business/course/items/{itemId}/asset`：将资料绑定到资源项；旧文件进入 `REPLACED`，不立即物理删除。

文件下载/预览不返回永久公开地址。后端根据用户权限返回短时效签名 URL，或由文件下载接口流式转发。

## 4. 数据库存储

现有 `course`、`course_chapter`、`study_item`、`study_record` 表继续使用，不把 PDF、DOCX、MP4 二进制写入 MySQL。

### 4.1 建议增量字段

`course`：

- `dept_id BIGINT`：稳定保存课程所属部门，避免岗位将来重新绑定后历史课程数据范围漂移。
- `sort_no INT DEFAULT 0`：课程目录排序。
- `version_no VARCHAR(32) DEFAULT 'V1.0'`：对外展示的发布版本。
- `content_version INT DEFAULT 1`、`version INT DEFAULT 0`：内容版本和乐观锁。

`course_chapter`：

- `intro VARCHAR(500)`：章节说明。
- `version INT DEFAULT 0`：并发编辑保护。

`study_item`：

- `asset_id BIGINT`：关联文件元数据表。
- `file_name VARCHAR(255)`、`file_ext VARCHAR(16)`、`file_size BIGINT`：列表展示和校验冗余。
- `completion_threshold DECIMAL(5,2) DEFAULT 100`：视频完成阈值。
- `content_version INT DEFAULT 1`、`version INT DEFAULT 0`。
- 现有 `content_url` 只保留兼容，不再作为权限依据。

`study_record`：

- `last_position_seconds INT DEFAULT 0`：视频续播位置。
- `content_version INT DEFAULT 1`：完成记录对应的资料版本。
- 保留现有 `UNIQUE(user_id, item_id)`，所有进度保存使用幂等更新。

### 4.2 文件元数据表

建议新增 `file_asset`，只存文件描述，不存文件内容：

```sql
CREATE TABLE file_asset (
  id BIGINT NOT NULL AUTO_INCREMENT,
  storage_provider VARCHAR(24) NOT NULL DEFAULT 'LOCAL',
  bucket_name VARCHAR(128) DEFAULT NULL,
  object_key VARCHAR(512) NOT NULL,
  file_name VARCHAR(255) NOT NULL,
  file_ext VARCHAR(16) NOT NULL,
  mime_type VARCHAR(128) NOT NULL,
  file_size BIGINT NOT NULL,
  sha256 CHAR(64) NOT NULL,
  asset_type VARCHAR(24) NOT NULL COMMENT 'COURSE_DOC/COURSE_VIDEO/COURSE_COVER',
  status VARCHAR(24) NOT NULL DEFAULT 'UPLOADING' COMMENT 'UPLOADING/READY/REPLACED/DELETED/FAILED',
  preview_object_key VARCHAR(512) DEFAULT NULL,
  duration_seconds INT DEFAULT NULL,
  page_count INT DEFAULT NULL,
  width INT DEFAULT NULL,
  height INT DEFAULT NULL,
  create_by VARCHAR(64) DEFAULT NULL,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  UNIQUE KEY uk_asset_sha256 (sha256),
  KEY idx_asset_status (status),
  KEY idx_asset_type (asset_type)
) ENGINE=InnoDB COMMENT='课程文件元数据';
```

### 4.3 文件实际位置

本地开发先使用可配置目录，例如 `${user.home}/intern-assessment-storage`，目录结构只使用数据库生成的 ID 和随机对象键：

```text
course/{deptId}/{courseId}/v{contentVersion}/{itemId}/{uuid}.{ext}
```

服务器迁移时将 `storage_provider` 切换为 `COS`、`S3` 或 `MINIO`，数据库中的 `object_key` 不变，业务代码通过 `FileStorageService` 抽象切换实现。不要把本机绝对路径写进数据库，也不要把用户上传文件放在前端 `public` 目录。

## 5. 格式处理策略

- PDF：初版直接在线预览；文件元数据保存页数，阅读完成由前端检测滚动到底并由后端再次校验确认请求。
- DOCX/PPTX/TXT：初版允许下载，后续可由异步转换服务生成 PDF 预览文件；原始文件始终保留。
- MP4/WebM：初版使用原生播放器，保存最高播放进度和续播位置；后续可增加转码后的 HLS 分片和封面图。
- QUIZ：不上传文件；题目由考核题库接口管理，`study_item` 只保存题库模板/考试入口标识。小型草稿可以放 JSON，大量题目不要塞进 `quiz_json`。
- 图片封面：作为 `COURSE_COVER` 文件单独存储，课程表只存 `cover_asset_id` 或兼容 `cover_url`。

## 6. 发布与版本规则

- 草稿可以增删章节、替换文件和调整顺序。
- 发布前后端都必须执行完整性校验，不能只信前端提示。
- 已发布课程如果没有学习记录，可以创建新草稿版本；已有学习记录时，不直接覆盖原章节和文件，只创建新版本。
- 旧版本保留给已有学习记录继续查看；新进入学习中心的实习生只看到最新发布版本。
- 删除采用逻辑删除，文件采用延迟清理，待无学习记录引用且超过保留期后再物理删除。

## 7. 安全和运维要求

- 校验扩展名、MIME、文件头、大小和 SHA-256，不能只信浏览器传来的类型。
- 上传完成前状态为 `UPLOADING`，失败必须可重试；病毒扫描或转码失败为 `FAILED`，不允许发布。
- 文件访问每次检查用户岗位、课程状态和版本权限，并返回短时效地址。
- 应用服务器只保存临时上传片段；视频和大文件禁止一次性读入 JVM 内存。
- 初版本地环境记录清理任务和存储根目录；服务器阶段再接对象存储生命周期、CDN、断点续传和转码队列。