# 实习生端改版 · 「前端优先」可行性分析

> 编制日期：2026-09-16　基线：设计稿 V1.2（`实习生端整体设计稿.html`）+ 方案 V1.0（`实习生端目录与页面布局设计方案.md`）
> 目的：回答一个问题 —— **设计稿能不能"尽量少动后端、主要改前端"落地？**

---

> **硬约束（用户 2026-09-16 明确）**：**已实现的后端接口一律不改**（不改签名、不改返回结构、不改权限注解）。尚未实现的功能可以后续再做，不阻塞本次改版。

## 零、一句话结论

**能。设计稿的「结构层」100% 可以零后端改动落地；「数据层」约 7 成有真实接口支撑。**

即：**本次改版对现有后端接口零改动** —— 全部改动集中在前端（新增页面文件 + 追加式路由/菜单）。

原因是一次核查里发现的两件事：

1. **设计稿要的表，库里一张都不缺。** 全库 36 张业务表里，`material`(备考资料)、`certificate`(证书)、`profile_snapshot` + `ability_dimension` + `stage_evaluation`(能力画像)、`notification`(通知)、`practical_submission`(实操提交)、`assessment_result`(成绩)、`exam_rule_snapshot`(资格规则) 全部已存在 —— 但 **0 行数据、且没有对应的 Java 层**。
2. **四组核心接口早就通了。** 在线学习、模拟考核、正式考核、成绩明细这四块的 Controller + 前端 API 封装都已就绪，前端直接调就行。

所以最省后端的路线是：**先把能跑通的四块 + 工作台做成真页面，把四块"没数据"的区域按设计稿自己的规矩做成 `--` + 去向说明的空态**。这样后端**一行不动**就能看到完整改版效果；之后再按需补"只读接口"（表已在，不建表）。

---

## 一、核查基线（当前系统实况）

| 项 | 现状 |
|---|---|
| 后端控制器 | **12 个**，覆盖 course / course-content / learning / practice / practice-subject / exam / answer-sheet / question / question-bank / register / auth / position |
| 实体 + 服务层 | 22 个 domain、12 个 service —— **只覆盖上面 12 个控制器**；画像/证书/备考资料/通知**无任何 Java 类** |
| 前端 API 封装 | `learning.js`、`practice.js`、`practiceSubject.js`、`exam.js`（含 answer-sheet 全套：`startExam`/`submitExam`/`myExamList`/`sheetDetail`）、`questionBank.js`、`question.js` —— **齐**；`portrait`/`certificate`/`material`/`notification` —— **无** |
| 实习生路由骨架 | `router/index.js` 的 `dynamicRoutes` 下 `/assessment/intern` **已有 8 条**：learning、learning/course、guide、exam、mock-exam、mock-exam-record、practice、practice-subject 详情 |
| 题库数据 | 总 **145 题**：开发部门 48（模拟 24 + 正式 24）、设计部门 97（模拟 49 + 正式 48）、其余三部门 **0**；题型 SINGLE 73 / MULTI 36 / JUDGE 36；知识点 12 类以上 |
| 已跑通闭环 | 在线学习（章节/资料/上传/进度/完成率）、模拟考核（start→submit→my→record）、正式考核与答卷（exam CRUD + answer-sheet start/submit/my/detail/grading/grade/publish）、注册审核、岗位、协议 |
| 空表（有表无数据无接口） | material、certificate、notification、profile_snapshot、ability_dimension、stage_evaluation、practical_submission、assessment_result、promotion_application、learning_task、task_assignment、exam_rule_snapshot、exam_paper_question |

---

## 二、设计稿区块 → 后端现状 → 改动量（总表）

| # | 设计稿区块 | 需要的接口 | 后端现状 | 改动量 |
|---|---|---|---|---|
| 1 | 一级目录（工作台 + 学习与考核） | 菜单 / 路由 | 菜单脚本需改（现有是"只留工作台+消息中心"） | **纯前端 + 菜单 SQL** |
| 2 | 页签壳 `shell.vue` | 无 | 不存在，需新建 | **纯前端** |
| 3 | 工作台 ①身份条 | 登录用户信息 | `/getInfo` 已返回 `mentorName`/`mentorPhone`，前端 store 已接 | **纯前端** |
| 4 | 工作台 ②KPI×6 | learning + practice | 完成率/时长/单项/待完成从 `/business/learning/courses` 算；模拟正确率从 `/business/practice/my` 算；**能力综合值无** | **纯前端**（1 项空态） |
| 5 | 工作台 ③完成率趋势（近6周） | 时间序列 | `study_record` 有 `last_study_time`+`progress`，但需按周聚合 | **纯前端可算**（或补 1 个聚合接口更准） |
| 6 | 工作台 ④能力画像雷达 | profile 快照 | **表在、0 行、无接口** | 空态 → 后续补只读接口 |
| 7 | 工作台 ⑤课程完成分布 | learning | `/business/learning/courses` 可算 | **纯前端** |
| 8 | 工作台 ⑥学习时长分布 | study_record | 同 #5 | **纯前端可算** |
| 9 | 工作台 ⑦考核成绩 | answer-sheet | `/business/answer-sheet/my` | **纯前端** |
| 10 | 工作台 ⑧培养进度四步 | 协议+学习+考核+转正 | 前三步可得；**第四步"转正发证"无** | **纯前端**（1 步空态） |
| 11 | 工作台 ⑨下一步待办 | 无（前端推导） | 可从上面各数据推导 | **纯前端** |
| 12 | 工作台 ⑩通知 | notification | **表在、0 行、无接口** | 空态 → 后续补只读接口 |
| 13 | 工作台 ⑪协议与证书 | 协议 + 证书 | 协议 `/business/auth/agreement` ✅；**证书无** | **纯前端**（证书空态） |
| 14 | 在线学习列表 | learning | ✅ 全通 | **纯前端（仅 template/style）** |
| 15 | 课程学习页 `detail.vue` | learning | ✅ 全通，现有结构已符合设计稿 | **不动** |
| 16 | 模拟考核 · 理论 | practice | ✅ `/practice/start`、`/submit`、`/my`、`/record/{id}` | **纯前端** |
| 17 | 模拟考核 · 实操题库 + 详情页 | practice-subject | ✅ `/practice-subject/published`、`/published/{id}` | **纯前端** |
| 18 | 备考资料页 | material | **表在、0 行、无接口** | 空态 → 后续补只读接口 |
| 19 | 正式考核（只读消费） | exam + answer-sheet | ✅ `/exam/list`、`/exam/{id}`、`/answer-sheet/start/{examId}` | **纯前端** |
| 20 | 正式考核 · 资格校验三条件 | exam_rule_snapshot | 表在（含 `need_course_done`/`course_done_threshold`/`protocol_required`/`retake_count`）、**0 行** | **纯前端可先算**（前端拿完成率+协议），后续接规则接口 |
| 21 | 成绩与证书页 · 明细表 | answer-sheet | ✅ `/answer-sheet/my`、`/detail/{sheetId}` | **纯前端** |
| 22 | 成绩页 · 部门平均分/排名 | 聚合 | **无** | 需补 1 个统计接口 |
| 23 | 成绩页 · 薄弱模块分析 | answer-sheet detail + `question.knowledge_point` | **实习生账号读题库实测 403**（`business:question:list` 未授予 PRE_TRAINEE）→ 拿不到知识点，前端算不了 | 需补 1 个只读统计接口 |
| 24 | 成绩页 · 证书区 | certificate | **表在、0 行、无接口** | 空态 → 后续补只读接口 |
| 25 | 能力画像详情页 | profile + dimension | **表在、无接口** | 空态 → 后续补只读接口 |
| 26 | 管理端「考核配置与发布」 | exam 写接口 | exam CRUD/publish ✅（管理员端，本次不在范围） | 不在本次范围 |

**统计：26 项中，16 项纯前端（零后端改动）、6 项"空态占位 + 后续只读接口"、2 项需补聚合接口、2 项不在本次范围。**

---

## 三、三梯队（按后端改动量排序）

### 梯队 A · 零后端改动，纯前端即可完整跑通 —— **占大头**

- 一级目录与页签壳（路由 + 菜单 SQL + 新建 `shell.vue`）
- 在线学习列表重排（**只动 template + style，保留 script 数据层**）
- 课程学习页（现有 `detail.vue` 已符合设计稿，**不动**）
- 模拟考核：理论抽题（practice）+ 实操题库卡片与详情页（practice-subject）
- 正式考核只读页（exam + answer-sheet start）
- 成绩与证书页的**成绩明细表**（得分 / 通过线 / 结果 / 提交时间 / 用时 / 逐题明细弹窗）
- 工作台 ②③⑤⑥⑦⑧⑨⑪（KPI、趋势、课程分布、时长分布、成绩、培养进度、待办、协议入口）

> 这一梯队做完，设计稿的**框架、导航、交互、样式**全部到位，四块核心业务有真实数据。

### 梯队 B · 表已存在，只需补"只读接口"（不建表、不写业务）

只需在一个新 Controller 里加几个 GET，或复用现有实体：

| 内容 | 表 | 需要的接口 |
|---|---|---|
| 备考资料 | `material` | `GET /business/material/published?positionId=` |
| 能力画像 | `profile_snapshot` + `ability_dimension` + `stage_evaluation` | `GET /business/profile/my`（须过滤 `visible_scope='OPEN'`） |
| 证书 | `certificate` | `GET /business/certificate/my` |
| 通知 | `notification` | `GET /business/notification/my` |
| 部门平均分 | `answer_sheet` 聚合 | `GET /business/answer-sheet/dept-stat/{examId}` |
| 薄弱模块分析 | `answer_sheet_item` + `question.knowledge_point` 聚合 | `GET /business/answer-sheet/module-stat/{sheetId}` |

> 关键点：**这些表数据库里已经有了**（说明早期初始化脚本已建过全量 schema），所以这一步是纯"读"，不改表结构、不动已有业务。

### 梯队 C · 真业务后端（无法只读绕过，改动重）

- 实操提交（`practical_submission` 上传 + 批阅）
- AI 阅卷（`answer_sheet_item.ai_*` 字段在，但无阅卷服务）
- 转正审批流（`promotion_application`）
- 阶段评价写入（`stage_evaluation`）
- 正式考核卷生成（`exam_paper_question` / `exam_rule_snapshot` 落库）

> 这一梯队不属于"改版"，属于**功能开发**，建议独立排期。

---

## 四、最省后端的落地路径（建议顺序）

**Phase 1 —— 纯前端，零后端改动（推荐先做）**
1. 新建 `views/assessment/learning/shell.vue`（页头 + `el-tabs` + `<router-view>`）
2. 调整 `router/index.js` 的 `dynamicRoutes`：把 8 条 `activeMenu` 从 `/index` 改为 `/intern/learning/*`
3. 改菜单归属（新增幂等迁移脚本，见第五节约束）
4. 工作台重构（template + style 大改，保留 script 的 API 调用与字段绑定）
5. 在线学习列表重排（只动 template/style）
6. 模拟考核页（新建，接 practice + practice-subject）
7. 正式考核只读页（新建，接 exam + answer-sheet）
8. 成绩与证书页（新建，接 answer-sheet，证书区空态）

**Phase 2 —— 补只读接口（一次性，工作量小）**
新建 1 个 `InternPortalController` 覆盖 material / certificate / profile / notification / dept-stat 五个 GET。

**Phase 3 —— 真业务后端（独立排期）**
实操提交、AI 阅卷、转正审批。

---

## 五、不可碰的约束（用户已明确）

| 约束 | 说明 |
|---|---|
| 不改公共骨架页 | `views/assessment/index.vue` 是**路由驱动的万能骨架页**（约 30 个 pageKey 复用），改它影响面极大 → **新页面一律走新文件** |
| 不碰已联动的后端 | `ruoyi-business` 已完成部分（course/learning/practice/exam/answer-sheet）不动；`api/business/*.js` **只加不改** |
| 工作台/学习页改造 | **只动 template + style，保留 script 中 data/computed/methods 与 API 调用、字段绑定** |
| `router/index.js` 与菜单 SQL | 属**共同登记区**（同事的正式考核也要动）→ 改动必须**追加式**，并提前协调 |
| 空态规矩 | 按设计稿要求：未生成数据一律 `--` + 去向说明，**不以 0 占位**（如"理论掌握 -- 考试后生成"） |
| 菜单脚本 | 改"只留工作台+消息中心"→"只留工作台+学习与考核"，须写**新的幂等迁移脚本**，勿改老的一次性初始化脚本 |

### 5.1 冻结接口清单（本次改版**一行不动**）

| 控制器 | 前缀 | 被哪些设计稿区块消费 |
|---|---|---|
| `LearningController` | `/business/learning` | 在线学习列表/详情、进度上报、工作台 KPI/趋势/分布 |
| `CourseController` | `/business/course` | 课程数据源（管理端） |
| `CourseContentController` | `/business/course` | 课程章节/资料/附件、学习记录 |
| `PracticeController` | `/business/practice` | 模拟考核理论（start/submit/my/record） |
| `PracticeSubjectController` | `/business/practice-subject` | 模拟考核实操题库（published / published/{id}） |
| `ExamController` | `/business/exam` | 正式考核只读页（list / {id}） |
| `AnswerSheetController` | `/business/answer-sheet` | 正式考核答题、成绩明细（start/my/detail…） |
| `QuestionBankController` / `QuestionController` | `/business/question-bank`、`/business/question` | 题库数据源（管理端） |
| `InternAuthController` | `/business/auth` | 协议签署与查询（工作台 ⑪） |
| `RegisterApplicationController` | `/business/register` | 注册审核（管理端） |
| `PositionController` | `/business/position` | 岗位数据源 |

> 前端对应封装 `api/business/{learning,practice,practiceSubject,exam,questionBank,question}.js` 亦**只加不改**。

---

## 六、顺带查出的 3 个数据问题（建议一并处理）

1. **`question_bank.question_count` 冗余字段不同步**
   - bank 5（设计模拟）实际 49 题、bank 12（设计正式）实际 48 题，但 `question_count` 字段都是 **0**；bank 2/3 是对的（24/24）。
   - 影响：前端若读 `questionCount` 显示题量会显示 0。
   - 修法：`UPDATE question_bank SET question_count=(SELECT COUNT(*) FROM question WHERE bank_id=id AND deleted=0)`。

2. **测试残留数据未清理**
   - `practice_subject` 仅 1 行：dept=105（设计部门）、`content='fty'`、附件是一个 docx —— 明显是 UI 试传残留。
   - `exam` 2 行：`exam_name` 分别是 `'m'` 和 `'l'`，dept=105、PUBLISHED —— 同属试填。
   - 建议：要么补成正式内容，要么清掉，避免实习生端出现"名字是 m 的正式考核"。

3. **题库分布不均**
   - 只有开发部门（48 题）和设计部门（97 题）有题，交付/质检/建模三部门 **0 题**。
   - 影响：这三个部门的模拟考核点开会失败或空卷（`practice/start` 会取不到题）。
   - 建议：先确认演示/验收用哪个部门；若要覆盖五部门需补题。

---

## 七、结论复述

- **想最快看到改版效果**：只做 Phase 1，后端**零改动**。
- **想让设计稿 100% 有数据**：再补 Phase 2 的 5 个只读 GET（表已在，不建表）。
- **实操提交 / AI 阅卷 / 转正审批**：属功能开发，不在"改版"范畴，独立排期。
