# 超管端 · 业务数据多层级下钻分析 —— 设计方案

> 需求来源：2026-09-18 用户口述 ——
> 「业务相关的管理，可以有**所有部门集体宽泛的对比可视化统计**，然后**分部门下钻看各部门的统计**，再**下钻具体业务数据**，可以**多个层级页面**。」
>
> 交付物：本文（方案）+ `超管端业务数据下钻分析设计稿.html`（可视化稿，逐页可指哪说哪）
>
> 与既有文档的关系：这是 `超管端能力扩展方案.md` / `超管端整体设计稿-v3.html` 的 **P2+P3 段专题细化**，把原来「一页 + 下钻栈」的设想，落成 **5 个独立路由页面**。不推翻 V3，只把 §5~§9 具象化。

---

## 〇、先看数据真相（★ 决定设计能不能成立）

设计稿写「部门横向对比」，前提是**每个部门都有数据**。实测后必须先说清这件事。

### 0.1 真实数据规模（`SELECT COUNT(*)`，非估算值）

| 表 | 行数 | 说明 |
|---|---|---|
| `exam` | **3** | id=1/2 是测试残留（`m`/`l`，设计部门 105）；id=25 是「开发部门模拟理论自测配置」 |
| `exam_participant` | **0** | ⚠️ **应考名单未落库** → L2 的「应考 / 缺考」暂时算不出来 |
| `answer_sheet` | **2** | 1 张已发布（`final_score=0`）、1 张进行中（`exam_id=12`，**exam 表里没有 12 → 孤儿行**） |
| `answer_sheet_item` | 20 | 逐题明细（`is_correct` / `ai_score` / `manual_score`） |
| `practice_record` | **8** | 开发 104 有 7 条、设计 105 有 1 条 |
| `practice_record_item` | 80 | 逐题明细，**可算题目级正确率** |
| `study_record` | **11** | 只覆盖 3 个部门（交付 3 / 开发 5 / 设计 3），**完成 1 条**（`status='DONE'`）、进行中 10 条 |
| `question` | 145 | **知识点填充率 145/145 = 100%**，共 **17** 个知识点 |
| `assessment_result` | **0** | ⚠️ 加权综合成绩台账**无数据** |
| `stage_evaluation` | **0** | 阶段评价无数据 |
| `certificate` | **0** | 已发证书无数据 |

### 0.2 部门分布（这决定了图会长什么样）

| 部门 | 在培实习生 | 报名已通过 | 模拟记录 | 学习记录 |
|---|---|---|---|---|
| 交付 103 | 8 | 1 | **0** | 3 |
| 开发 104 | 4 | 3 | **7** | 5 |
| 设计 105 | 2 | 1 | 1 | 3 |
| 质检 106 | 2 | 1 | **0** | **0** |
| 建模 107 | 1 | 0 | **0** | **0** |

> **结论**：5 个部门里只有 **开发 104** 数据是全的；设计 105 半全；交付/质检/建模三列在「模拟考核」「学习完成率」上是**真实空态**。
> 所以设计稿里的对比图必须**如实呈现稀疏**，不做"看起来热闹"的假图 —— 这符合本项目「完成率分母 0 → `--`，不伪装 0%」的既有铁律。

### 0.2.1 ★ 更关键：每个部门实际只有 1 个活跃实习生

| 部门 | 在培人数 | 有模拟记录的人 | 有学习记录的人 |
|---|---|---|---|
| 开发 104 | 4 | **1 人**（开发部门实习生，7 次） | **1 人**（5 项） |
| 交付 103 | 8 | 0 人 | 1 人（3 项，其中 1 项 `DONE`） |
| 设计 105 | 2 | 1 人（1 次） | 1 人（3 项） |
| 质检 106 | 2 | 0 人 | 0 人 |
| 建模 107 | 1 | 0 人 | 0 人 |

> 当前演示数据下，「**部门横向对比**」实质 ≈ 「**4 个活跃个人的对比**」。这**不改变设计**（真实使用后会自然填满），但决定了 L1 的个人明细表里 **4 行会有 3 行是「暂无记录」** —— 空态必须设计得能看，而不是让人以为页面坏了。

### 0.3 ★ 四个被推翻的既有假设（原文档写错了，需同步修正）

| # | 原文档说法 | **实测真相** | 证据 |
|---|---|---|---|
| 1 | `REFERENCE.md §三`：「`exam.exam_type` 与 `exam_mode` **语义重叠**，代码只用 `exam_type`」 | **错**。二者是**两个正交维度**：`exam_type` = `THEORY`/`PRACTICAL`（**环节**），`exam_mode` = `FORMAL`/`PRACTICE`（**性质**）。前端 `dashboard/index.vue:54` 已在用 `examType === 'PRACTICAL'` 判环节，`examMode: 'FORMAL'` 也在传 | `Exam.java:30` 注释「类型：THEORY理论 / PRACTICAL实操」；`SELECT exam_type, exam_mode, COUNT(*) FROM exam GROUP BY 1,2` → `THEORY/FORMAL:2`、`THEORY/PRACTICE:1` |
| 2 | `answer_sheet.sheet_type` 列注释写「PRACTICE模拟 / FORMAL正式理论」 | **注释与实际不符**。真实取值是 `THEORY`（同 `exam_type` 的枚举体系） | `SELECT sheet_type, status, COUNT(*) FROM answer_sheet GROUP BY 1,2` → `THEORY/PUBLISHED:1`、`THEORY/IN_PROGRESS:1` |
| 3 | 设计稿 §4.2 写「规则存在 `assessment_rule_snapshot`」 | 该表 **0 行且零代码引用**；真正的规则是 **`assessment_config`** —— **单行配置表**（`id` 固定为 1，18 列，含 `alert_dept_done_floor` 部门完成率下限、`alert_bank_gap_factor` 等预警阈值） | `information_schema.columns` where table_name='assessment_config'；`assessment_rule` 表**不存在** |
| 4 | 以为「成绩台账可以直接读」 | **`assessment_result` 存在但 0 行**（19 列：`theory_score` / `practice_score` / `total_score` / `pass_flag` / `publish_status` / `eval_comment` / `eval_visible`）。**表结构够用，缺的是数据与读接口** | `SELECT COUNT(*) FROM assessment_result` → 0 |

> 顺带：`exam_paper_question`（组卷快照）、`exam_rule_snapshot` **都是 0 行** —— 意味着**题目级分析应走 `answer_sheet_item` / `practice_record_item`（这两张有数据），不要走组卷快照表**。

### 0.4 已有的可复用能力（零新增，直接用）

| 能力 | 位置 | 口径 |
|---|---|---|
| 课程 `student_count` / `expected_student_count` / `avg_completion_rate` | `CourseMapper.xml:64/73/74-93` | **岗位级**；含 `internRoleExists` 角色存在性判断；完成率 = `SUM(progress) / (人数 × 必学项数)`，分母 0 → `NULLIF` 兜 `0` |
| 课程级学习记录名单 | `StudyRecordMapper.xml:109/116` | 按 `user_id, course_id` 分组，带 `position_name` |
| 正式考核逐场逐题 | `GET /business/answer-sheet/grading/{examId}`、`/detail/{sheetId}` | 权限 `business:bank:list`，**超管可读** |
| 全量列表 | `/business/exam/list`、`/course/list`、`/question/list`、`/question-bank/list`、`/register/list` | 超管命中 `isGlobalReadOnly()` → 部门过滤为 `null` → 天然全量 |
| 部门↔岗位绑定 | `GET /business/position/dept-bindings` | 2026-09-17 新增的只读接口 |

> **部门维度全项目没有任何一条聚合 SQL** —— `grep -rn "GROUP BY" --include=*.xml` 全仓只有 **2 处**，都在 `StudyRecordMapper.xml`，且是按 `user_id/course_id` 分组。所以「部门横向对比」**必须新建接口**，不能靠前端拼。

---

## 一、层级模型：5 层，5 个独立路由页面

用户说「可以多个层级页面」→ 采用**独立路由**而非单页切换。理由：URL 可分享 / 刷新不丢位置 / 面包屑天然 / 浏览器前进后退可用（本项目刚在 `views/message/index.vue` 踩过「同 path 只换 query → 组件不重建 → 点了没反应」的坑，见 REFERENCE §十二）。

```
L0  业务总览          /super/ops/analysis                     所有部门横向对比 · 排头兵
      │  点某部门
      ▼
L1  部门详情          /super/ops/analysis/dept/103            该部门内部统计（岗位分布 / 完成率 / 成绩 / 薄弱点）
      │  点某场次或某课程
      ▼
L2  场次 / 课程明细    /super/ops/analysis/exam/25             全量名单 · 分数段 · 环节对比
      │  点某个人
      ▼
L3  个人 360          /super/ops/analysis/intern/127          学习进度 + 模拟逐题 + 正式逐题 + 阶段评价
      │  点某道错题
      ▼
L4  题目详情          /super/ops/analysis/question/{id}       正确率 · 选项分布 · 出现在哪些场次
```

**横切页（不占层级，可从任意层进入）**：

```
     知识点矩阵        /super/ops/analysis/knowledge          部门 × 知识点 热力矩阵（全局档）
```

**面包屑**：每层顶部显示 `业务总览 › 开发部门 › 开发部门模拟理论自测 › 张某`，每一段可点回。**不做固定 5 级硬编码** —— 面包屑由 URL 段动态生成。

---

## 二、完整目录体系（一级 / 二级 / 三级 / 四级）

> 这是**侧栏、路由、视图目录三者的一致性基准** —— 表里没有的页面不该存在，表里有的页面必须有路由。
> 对应设计稿第 ② 节。

### 2.1 四条分层原则

1. **侧栏最多两级**。三级一律做成**页面内的页签 / 区块**，不做第三级菜单 —— 本项目的侧栏是**前端硬编码**（`store/modules/permission.js` 的 `buildSuperSidebar()`），层级越深，「改了没生效」的概率越高（改 `sys_menu` 是零效果的）。
2. **下钻页不进侧栏**。它们不是「功能」而是「某个数据对象的详情」，数量随数据增长（部门 × 场次 × 人 × 题）。只通过 **URL + 面包屑**到达；侧栏当前项恒为父页 `activeMenu`。
3. **单项目录自动折叠**。全局工作台 / 任务与通知 / 规则与配置 / 审计与合规各只有一页，侧栏里折叠成一级链接（不显示展开箭头）。
4. **路径三段固定** `/super/{组}/{页}`，下钻页在页后追加 `/{类型}/{id}`，**不出现第 4 段固定菜单**。

### 2.2 一级 / 二级 / 三级 全表

| 一级目录 | 二级 · 页面 | 三级 · 页签 / 视图 / 区块 | 路由 | 状态 |
|---|---|---|---|---|
| **① 全局工作台**（单项目录） | 全局工作台（目录即页面） | 6 区块：全局 KPI · 部门分布 · 考核概览 · 题库健康度 · 异常预警 · 最近动态（**无页签**） | `/super/dashboard` | ✅ 已实现 |
| **② 组织与人员**（分组） | 组织岗位 | ① 部门树　② 岗位绑定（`dept_position`）　③ 在培人数 | `/super/org/organization` | ✅ 已实现 |
| | 角色权限 | ① 角色列表　② 菜单权限　③ 数据权限 | `/super/org/roles` | ✅ 原生复用 |
| | 账号管理 | ① 用户列表　② 新增/编辑　③ 重置密码　④ 导入导出 | `/super/org/accounts` | ✅ 复用系统页 |
| | **部门管理员** 🆕 | 页签：**全部** / **有待办** / **已停用**；行内「一键催办」<br/>四级：部门管理员详情（待办明细） | `/super/org/dept-admins` | 🆕 新设计（P1） |
| | **人员总览** 🆕 | 筛选条（部门/岗位/状态/关键字）+ 花名册列表<br/>四级：个人 360（复用 L3） | `/super/org/interns` | 🆕 新设计 |
| **③ 培养运营**（分组） | **业务总览** 🆕 | 作用域视图：**全局** / **多部门** / **单部门**（切换即换全部卡片口径） | `/super/ops/analysis` | 🆕 **本稿核心** |
| | **知识点矩阵** 🆕 | 视图：**按得分率** / **按题量**；单部门档自动降级为条形图 | `/super/ops/knowledge` | 🆕 **本稿核心** |
| | 课程与题库总览 | 页签：① 课程　② 题库　③ 题目 | `/super/ops/courses` | ✅ 已实现 |
| | 考核运营总览 | 页签：① 场次列表　② 场次详情 | `/super/ops/exams` | ✅ 已实现 |
| | ~~成绩与统计分析~~ | 骨架页，内容已被「业务总览」覆盖 | `/super/ops/scores` | ⚠️ 建议下线 |
| **④ 任务与通知**（单项目录） | 任务与通知（目录即页面） | 页签：① 发送（公告/通知 × 四档范围）　② 我发送过的（回执）　③ 未读名单 | `/super/notify` | ✅ 已实现 |
| **⑤ 规则与配置**（单项目录） | 规则与配置（目录即页面） | 页签：① 考核规则默认值　② 协议模板版本　③ 预警阈值（`alert_*`）　④ 证书规则 | `/super/rule` | ✅ 已实现 |
| **⑥ 审计与合规**（单项目录） | 审计与合规（目录即页面） | 页签：① 操作日志　② 登录日志　③ **导出留痕** 🆕 | `/super/audit` | ✅ 部分实现 |
| **⑦ 系统管理**（原生） | 用户 / 角色 / 菜单 / 部门 / 岗位 / 字典 / 参数 / 通知公告 / 日志 | 由 RuoYi 原生页提供，不改造 | `/system/**` | 原生保留 |
| **⑧ 系统监控**（原生） | 在线用户 / 定时任务 / 数据监控 / 服务监控 / 缓存监控 | 含本项目 2 个 Quartz 任务（任务逾期标记 · 任务到期提醒） | `/monitor/**` | 原生保留 |
| **⑨ 系统工具**（原生） | 表单构建 / 代码生成 / 系统接口 | — | `/tool/**` | 原生保留 |

**计数**：一级 **9** 个（业务 6 + 原生 3）；二级页面 **21** 个；**侧栏可见的二级项只有 12 个**（4 个单项目录折叠成一级链接，不计二级）。

### 2.3 四级 · 下钻页（挂在「业务总览」之下，不进侧栏）

| 下钻页 | 路由 | 从哪进 |
|---|---|---|
| 部门详情 | `/super/ops/analysis/dept/:deptId` | L0 的部门行 |
| 场次明细 | `/super/ops/analysis/exam/:examId` | 部门详情的场次名 |
| 个人 360 | `/super/ops/analysis/intern/:userId` | 名单里的人名 |
| 题目详情 | `/super/ops/analysis/question/:questionId` | 逐题明细的题号（链路终点） |

**为什么「侧栏当前项恒为业务总览」**：下钻页保持父页的 `activeMenu`，用户始终知道「我还在业务总览这个功能里」。若每层都在侧栏高亮自己，侧栏就得为下钻页留位置 —— 那正是要避免的。

### 2.4 路径 / 视图目录 / 侧栏 三联对照

| 一级目录 | 路由前缀 | 视图目录 | 侧栏写法（`buildSuperSidebar()`） |
|---|---|---|---|
| 全局工作台 | `/super/dashboard` | `views/super/dashboard/` | `single('/super', 'dashboard')` —— **子项写绝对路由** |
| 组织与人员 | `/super/org/*` | `views/super/org/*/` | `group('/super/org', …, ['org/organization','org/roles','org/accounts','org/dept-admins','org/interns'])` —— **子项写相对段** |
| 培养运营 | `/super/ops/*` | `views/super/ops/*/` | `group('/super/ops', …, ['ops/analysis','ops/knowledge','ops/courses','ops/exams','ops/scores'])` |
| 任务与通知 | `/super/notify` | `views/super/notify/` | `single('/super/notify', 'notify')` |
| 规则与配置 | `/super/rule` | `views/super/rule/` | `single('/super/rule', 'rule')` |
| 审计与合规 | `/super/audit` | `views/super/audit/` | `single('/super/audit', 'audit')` |
| 下钻页（不进侧栏） | `/super/ops/analysis/{类型}/{id}` | `views/super/ops/analysis/`（与 L0 同目录） | 不写进 sidebar；路由带 `meta.activeMenu = '/super/ops/analysis'` |

### 2.5 ★ 一条路径规则（本项目已踩过两次）

侧栏拼接用的是 `path.resolve(basePath, childPath)`，`basePath` 取**目录自身**的 path：

| 目录类型 | 目录路径 | 子项该怎么写 | 为什么 |
|---|---|---|---|
| **分组目录** `group()` | `/super/ops` | **相对末段** `'ops/analysis'` | 「组路径 + 末段」正好等于真实路由 → `/super/ops` + `analysis` = `/super/ops/analysis` ✓ |
| **单项目录** `single()` | `/super/rule`（本身就是真实路由） | **绝对真实路由** | 子项若写相对段会被拼成 `/super/rule/rule` → **404** |

> **所以本稿所有分析页都挂在「培养运营」组下，前端路由是 `/super/ops/analysis`，而不是 `/super/analysis`** —— 后者是多拼/少拼一层都会 404 的写法。设计稿 14 处前端路由已按此统一。

---

## 三、作用域与筛选：一个选择器管全页

沿用 V3 §4 的三档语义，但**只放在 L0**，下钻后继承：

| 档位 | 选择结果 | 视图语义 | 横向卡片 |
|---|---|---|---|
| **① 全局** | 不选部门（默认） | 各部门并列 + 排名 + 部门均值参考线 | 全开 |
| **② 多部门** | 选 ≥2 个 | 只在选中部门之间比对 | 全开 |
| **③ 单部门** | 恰好 1 个 | 该部门内部统计 | **自动降级**（部门×部门柱 → 本部门 vs 全局均值） |

**统一规则**（避免同页出现打架的口径）：
1. **一个页面只用一个作用域选择器**（页头工具栏），切换后所有卡片同步刷新；不做「每张卡自己筛」。
2. 部门多选，与 **岗位 / 考核场次 / 时间窗 / 关键字** 四个筛选器**叠加**。
3. 横向比对类卡片在单部门档下**自动降级**，**不显示空表**。
4. 部门内统计类卡片（岗位分布、个人排名）在全局档下**按部门分组**展示，不跨部门混算成一个数。
5. L1~L4 的 URL 已固化部门，不再重复放选择器，只放**时间窗 + 关键字**。

---

## 四、逐层页面规格

### L0 `业务总览` `/super/ops/analysis`

**页头**：作用域选择器（部门多选）+ 时间窗 + 关键字 + 「导出当前视图」。

**6 个全局 KPI**（真实可算，非示例）：

| KPI | 公式 | 实测值 |
|---|---|---|
| 在培实习生 | 持角色 ∧ `user_status ∈ (PRE_TRAINEE, FORMAL_TRAINEE, PENDING_PROMOTE)` 按部门分组 | **17**（交付 8 / 开发 4 / 设计 2 / 质检 2 / 建模 1） |
| 覆盖部门 | `parent_id = 100` 的直属子部门 | **5** |
| 模拟考核人次 | `COUNT(practice_record)` | **8** |
| 模拟平均分 | `AVG(practice_record.score)` | **4.13**（满分 10 题；开发 4.43 / 设计 2.00） |
| 学习记录条数 | `COUNT(study_record)` | **11**（完成 1 条 / 进行中 10 条） |
| 正式答卷数 | `COUNT(answer_sheet)` | **2**（其中 1 张为孤儿行） |

**6 张对比卡**：

| # | 卡片 | 图形 | 依赖 | 现状 |
|---|---|---|---|---|
| 1 | 部门在培规模对比 | 横向条形 + 排名 | 新接口 `overview` | ✅ 可出真图（5 部门） |
| 2 | **部门学习进度对比** | 分组柱 + 部门均值参考线 | 新接口 | ⚠️ 只有 3 个部门有记录；完成率接近 0，故本卡改用「平均学习进度」口径（见下） |
| 3 | 部门模拟考核均分对比 | 分组柱 + 排名 | 新接口 | ⚠️ **2/5 部门有值**，其余显示「暂无数据」 |
| 4 | 分数段分布 | 直方图 + 及格线参考线 | 新接口 | ⚠️ 样本仅 8 条 |
| 5 | 岗位 × 指标 | 分组柱 | 新接口 | ✅ 可出（5 岗位 × 17 人） |
| 6 | 异常预警 | 列表 | **可复用** `/business/rule/config`（`assessment_config` 的 `alert_*` 阈值） | ✅ 已有（`dashboard` 在用） |

**卡片 2 为什么用「平均学习进度」而不是「完成率」**：
> 完成率的正式口径是 `Σprogress / (应学人数 × 必学项数)`（`CourseMapper.xml:74-93`）；当前 11 条记录里**只有 1 条 `DONE`**（交付部门），算出来接近 0、看不出部门差异。改用**平均进度**更能反映真实推进度 —— 实测：交付 **68.7%**（3 项：6 / 100 / 100）、开发 **44.8%**（5 项：100 / 18 / 6 / 100 / 0）、设计 **0%**（3 项全 0）。
> 质检 / 建模**无任何学习记录** → 分母为 0 → 显示 `--`，**不伪装 0%**。

### L1 `部门详情` `/super/ops/analysis/dept/:deptId`

**页头**：面包屑 + 部门名 + 该部门在培人数；右侧「本部门 vs 全局均值」小对比。

**4 个部门 KPI**：在培人数 / 学习完成率 / 模拟均分 / 正式通过率。

**5 张卡**：

| # | 卡片 | 说明 | 现状 |
|---|---|---|---|
| 1 | 本部门岗位分布 | 5 个岗位各几人（真实：交付 8 人全在岗 1 实施） | ✅ |
| 2 | 本部门课程完成率明细 | 逐门课：预计人数 / 已学人数 / 完成率 | ✅ 口径复用 `CourseMapper` |
| 3 | 本部门模拟成绩分布 | 直方图 + 个人明细表（可点进 L3） | ⚠️ 开发 7 条 / 设计 1 条 |
| 4 | **本部门薄弱知识点 Top5** | 按 `is_correct` 聚合的得分率升序 | ✅ **可出真数据**：开发 104 实测 **Redis 0/11、MySQL 0/9、Linux 0/5、ECharts 0/4、Java 0/4** |
| 5 | 本部门考核场次 | 该部门 `exam.dept_id` 的场次列表 → 点进 L2 | ⚠️ 仅设计 105 有 2 条（测试残留） |

> 卡片 4 是**本页最有价值的一张**：开发部门实测 `MySQL 0/9`、`Redis 0/11`、`ECharts 0/4`、`Java 0/4` —— 一眼看出该部门哪块最弱。

### L2 `场次 / 课程明细` `/super/ops/analysis/exam/:examId`

**页头**：面包屑 + 场次名 + 环节（理论/实操）+ 状态 + 及格线。

| 区块 | 内容 | 现状 |
|---|---|---|
| 场次信息 | `exam` 行直出：题库 / 题量 / 及格线 / 起止时间 / 状态 | ✅ |
| **全量名单** | 应考 / 实考 / 缺考 / 通过，逐人分数 | ⚠️ **`exam_participant` 0 行 → 应考名单无来源** |
| 分数段分布 | 直方图叠及格线 | ⚠️ 1 张有效答卷 |
| 环节横向对比 | 理论 vs 实操（`exam_type` 区分） | ⚠️ 库里 `exam_type` 全为 `THEORY`，无实操场次 |

**「应考名单」的处置（必须拍板，见 §九 决策 2）**：
- 现状：`exam_participant` 表存在但**零行**，`exam` 也没有「应考范围」列。
- 三个可选口径：① 该部门全部在培实习生（按 `dept_id` + 角色）；② `exam_participant` 显式名单（需先有写入路径）；③ 按 `position_id` 命中 `dept_position` 的岗位实习生。
- **设计稿先按 ① 渲染**，并在表头标注「应考 = 本部门全部在培实习生（口径 A，待拍板）」。

### L3 `个人 360` `/super/ops/analysis/intern/:userId`

**四段一次拉齐**（对应 V3 §8）：

| 段 | 内容 | 依赖 | 现状 |
|---|---|---|---|
| 身份条 | 姓名 / 部门 / 岗位 / 培养状态 / 导师 / 协议 / 证书 | `sys_user` + `agreement_signature` + `certificate` | ✅（证书 0 行） |
| ① 学习进度（逐项） | 逐课程 → 逐章节 → 逐单项，进度与完成时间 | `study_record` + `study_item` | ⚠️ **超管读不到**（`/business/learning/**` 类级 `@PreAuthorize('PRE_TRAINEE,FORMAL_TRAINEE')`）→ **必须新接口** |
| ② 模拟考核（逐场逐题） | 每场：得分 / 题数 / 逐题对错 | `practice_record(_item)` | ⚠️ **超管读不到**（`/business/practice/my`、`/record/{id}` 同注解）→ **必须新接口** |
| ③ 正式考核（逐场逐题） | 每场：理论分 / 实操分 / 通过 | `answer_sheet(_item)` | ✅ 走 `/grading/{examId}` 可读 |
| ④ 阶段评价 | 优势 / 待提升 / 建议 / 可见性 | `stage_evaluation` | ⚠️ **0 行**；且 `visible_scope='ADMIN_ONLY'` 的**不进实习生端画像** |

### L4 `题目详情` `/super/ops/analysis/question/:questionId`

| 区块 | 内容 | 依赖 | 现状 |
|---|---|---|---|
| 题干 / 选项 / 答案 / 解析 | `question` 行 + `options_json` | ✅ |
| 正确率 | `SUM(is_correct) / COUNT(*)`，跨模拟 + 正式 | ✅ **可算**（两表都有数据） |
| 选项分布 | 按 `user_answer` 分组计数 | ⚠️ `practice_record_item.user_answer` 是快照文本，可聚合；正式卷 `answer_sheet_item.user_answer` 是 TEXT |
| 出处场次 | 该题出现在哪些 `exam` / `practice_record` | ✅ |
| 作答者名单 | 谁答对 / 谁答错 | ✅ |

### 横切页 `知识点矩阵` `/super/ops/analysis/knowledge`

**行 = 部门，列 = 知识点，值 = 得分率** —— 一眼看出"哪个部门哪块弱"。
- ✅ **可出真数据**：`question.knowledge_point` 填充率 100%（145/145，17 个知识点），`practice_record_item` 有 80 条逐题明细。
- 单部门档下退化为「本部门知识点得分率」条形图。

---

## 五、后端新增接口清单（9 项，全部为**新增**，不改既有签名与注解）

| # | 接口 | 层级 | 依赖表 | 已核实的可行性 |
|---|---|---|---|---|
| 1 | `GET /business/super/analysis/overview` | L0 | `sys_user`+`sys_user_role` / `study_record` / `practice_record` / `answer_sheet` / `register_application` | ✅ 全部表有数据 |
| 2 | `GET /business/super/analysis/dept-stats?deptId=` | L1 | 同上 + `course`+`course_chapter`+`study_item`+`dept_position` | ✅ 口径复用 `CourseMapper` |
| 3 | `GET /business/super/analysis/dept-trainees?deptId=` | L1/L2 | `sys_user`+角色 | ✅（可直接复用 `TaskMapper.selectDeptTrainees`，**超管 deptId=null 看全部**） |
| 4 | `GET /business/super/analysis/exam-sessions?deptId=&examId=` | L2 | `exam` / `answer_sheet` / `sys_user` | ⚠️ 「应考」口径待拍板（§九 决策 2） |
| 5 | `GET /business/super/analysis/interns?deptId=&positionId=&status=&keyword=` | L1→L3 入口 | `sys_user`+角色+`register_application` | ✅ |
| 6 | `GET /business/super/analysis/intern/{userId}` | L3 | `study_record` / `practice_record(_item)` / `answer_sheet(_item)` / `stage_evaluation` | ✅ **补 `learning` / `practice` 两个读缺口**（不改那两处的类级注解） |
| 7 | `GET /business/super/analysis/questions?bankId=&deptId=&sort=` | L4 | `question` + `answer_sheet_item` + `practice_record_item` | ✅ |
| 8 | `GET /business/super/analysis/question/{questionId}` | L4 | `question` + 两张 item 表 | ✅ |
| 9 | `GET /business/super/analysis/knowledge-matrix?deptIds=` | 横切 | `question.knowledge_point` × 两张 item 表 | ✅ **17 个知识点 / 145 题全有标注** |

**统一实现要点**：
- 放在**新 Controller** `SuperAnalysisController`（`ruoyi-business/.../controller/`），前缀 `/business/super`，与现有 17 个控制器**零冲突**。
- 权限注解用**已有的** `business:bank:list`（超管已授权），**不新增权限行**、不改任何既有注解。
- 部门过滤**不用** `currentScopeDeptId()`（那会按登录人部门收窄），改为**接口参数显式传入** —— 超管看全量、传 `deptId` 则只看该部门。
- 统计口径统一加 `internRoleExists` 角色存在性判断（`CourseMapper` 已有可复用 `<sql>` 片段）。
- 分数段/矩阵类聚合在 **SQL 里 GROUP BY**，不要拉全量到前端算。

---

## 六、前端改动（追加式，不碰既有页面）

**新增 6 个文件**（本稿分析相关；另有 2 个组织侧新页——部门管理员、人员总览——位置登记在 §二 目录体系表里，不在本稿展开画稿）：

| 文件 | 说明 |
|---|---|
| `views/super/ops/analysis/overview.vue` | L0 |
| `views/super/ops/analysis/dept.vue` | L1（读 `$route.params.deptId`） |
| `views/super/ops/analysis/exam.vue` | L2（读 `$route.params.examId`） |
| `views/super/ops/analysis/intern.vue` | L3（读 `$route.params.userId`） |
| `views/super/ops/analysis/question.vue` | L4（读 `$route.params.questionId`） |
| `views/super/ops/analysis/knowledge.vue` | 横切（知识点矩阵） |

**共享组件 1 个**：`components/DeptScopeBar/index.vue` —— 作用域选择器（部门多选 + 时间窗 + 关键字），带 `change` 事件抛给父页。理由：L0/L1/横切三页共用同一套筛选语义，**复制三份必然分叉**。

**改 2 处既有文件（追加式）**：
1. `router/index.js` 的 `/super` children **追加 6 条**（带 `:param` 的用 `props: true` 或页面内读 `$route.params`）；**绝不整文件覆盖**（同事分支曾把 `BusinessCourse` 的 component 回退过）。
2. `store/modules/permission.js` 的 `buildSuperSidebar()` 在「培养运营」组**追加 2 个 sec**：`analysis`（业务总览）、`analysis/knowledge`（知识点矩阵）。

> ⚠️ **单独项 vs 分组的路径规则**（本文件已踩过两次）：
> - 分组目录（`group()`）子项写**相对末段** —— `'/super/ops'` + `'analysis'` = `/super/ops/analysis` ✓
> - 若做成单项目录（`single()`），子项必须写**绝对路由**，否则拼成 `/super/ops/analysis/analysis` → 404。

**与既有页面的关系**：
- `/super/ops/scores`（现有**骨架页**）→ 建议**改造成 L0 的第二个视图**（或直接跳转），消除「示例数据」标注。
- `/super/dashboard`（全局工作台）→ **不动**，它继续做「概览入口」，卡片可加「查看分析 →」跳到 L0。

---

## 七、落地顺序（每步都能独立验收）

| 阶段 | 内容 | 后端 | 前端 | 验收标准 |
|---|---|---|---|---|
| **S1** | L0 总览 + `overview` 接口 | 1 接口 | 1 页 + 选择器 | 5 个部门的在培规模 / 模拟均分出真图；空态如实显示 |
| **S2** | L1 部门详情 + `dept-stats` | 1 接口 | 1 页 | 开发部门薄弱知识点 Top5 出真数据（MySQL 0/9、Redis 0/11） |
| **S3** | L3 个人 360 + `intern/{id}` | 1 接口 | 1 页 | **补上 `learning`/`practice` 两个读缺口**，某实习生四段齐全 |
| **S4** | 横切 知识点矩阵 + `knowledge-matrix` | 1 接口 | 1 页 | 部门×知识点热力出真图（17 知识点） |
| **S5** | L2 场次明细 + L4 题目详情 | 4 接口 | 2 页 | 依赖「应考口径」拍板 |
| **S6** | `/ops/scores` 骨架页合并/下线 | 0 | 改 1 页 | 全站无「示例数据」标注 |

**建议从 S1 + S2 起步**：这两步的接口最独立、数据最实（模拟考核 + 知识点都是真数据），能最快看到「部门横向对比 → 下钻到薄弱知识点」的完整闭环。

### 7.1 ★ 顺带补一个「设计漏洞」（零新表、零新接口）

设计稿的**联动映射表**（超管动作 → 落库 → 另一角色在哪儿看到）暴露出一个真漏洞：

> 本方案把「开发部门 Redis 0/11、MySQL 0/9」算得清清楚楚，**但超管看完了只能自己知道** —— 没有任何机制把它变成部门管理员的行动。

补法：在 L1 薄弱知识点卡与知识点矩阵上加一个「**通知该部门管理员**」按钮，**复用已经通的定向通知能力**：

- `scope_type='USER'` → 定向到该部门具体管理员；或
- `scope_type='POSITION'` + 虚拟「部门管理员岗」（`scope_id = 0`，`Notice.POSITION_DEPT_ADMIN`）

正文自动带上薄弱知识点清单与得分率。**零新表、零新接口** —— 只是把两个已经完成的能力接起来（通知四档 + 知识点聚合）。这条建议纳入 S4 一起做。

---

## 八、必须复用的既有约定（避免踩重复的坑）

1. **列表返回**：`rows` / `total` 在**顶层**，不在 `data` 里（本项目踩过 3 次）。
2. **失败 ≠ 空**：区分「加载失败」与「确实没有」两个状态、两段文案，失败给原因 + 重试。`utils/request.js` 的三种拒绝形态：500 → `err.message`；其它非 200（如 403）→ 拿到字符串 `'error'`；网络层 → `'Network Error'`（要换成中文）。
3. **完成率分母 0 → `null` + 「暂无课程」**，不得伪装 0%（`CourseMapper` 已用 `NULLIF` 实现）。
4. **`user_status` 不是权限**：凡「捞人 / 数人」的 SQL 必须带角色 `EXISTS`（`internRoleExists` 片段）。
5. **`review_status` 是三值**（`PENDING`/`PASSED`/`REJECTED`），**不能当布尔用**。
6. **`user_status IN (PRE, FORMAL)` 的统计**：`PENDING_PROMOTE`（转正审批中）**仍持角色**，要留在名单里。
7. **图表库**：项目已用 ECharts（`v-charts`/原生），新增图表沿用既有引入方式，不引新依赖。
8. **样式**：`@import '~@/assets/styles/super-module.scss'`（超管端基线，类前缀 `s`）；`@import` 进 `<style scoped>` 时**不能用 `:root`**，令牌一律用 SCSS 变量。
9. **`view` 命名**：新页面用**新文件**，不改 `views/assessment/index.vue`（公共骨架页）。

---

## 九、待你拍板的 4 个决策点

| # | 决策点 | 选项 A | 选项 B | 我的建议 | 影响页面 |
|---|---|---|---|---|---|
| **1** | **L2「应考 / 缺考」怎么算** | 按 `dept_id` + 实习生角色 = 全部在培人（口径 A，**零 DDL**） | 走 `exam_participant` 显式名单（需先补「发布时展开名单」写入路径） | **A**：`exam_participant` 现在 0 行且无写入路径，B 要动发布流程 | L2 |
| **2** | **`assessment_result`（加权台账）要不要做** | 不做，L2/L3 直接读 `answer_sheet.final_score`（理论分） | 做：补齐「理论+实操加权」台账，L0「正式通过率」走它 | **A**：`assessment_result` 0 行且组卷快照表也空，做了没数据；先跑通理论链路 | L0/L2/L3 |
| **3** | **学习完成率的口径** | 岗位级（复用 `CourseMapper` 现有 `avg_completion_rate`，**口径一致**） | 部门级新口径（按 `dept_id` 重新聚合） | **A**：与课程页数字**必须一致**，另起口径会让同一指标在两页显示不同值 | L0/L1 |
| **4** | **`/super/ops/scores` 骨架页怎么处置** | 改造成 L0 的一个视图（保留侧栏位置） | 下线该页，侧栏只留新的「业务总览」 | **B**：两页并存会让用户分不清哪个是真的；下线后侧栏更干净 | 侧栏 |

---

## 十、诚实标注汇总（写进 HTML 稿）

| 标注 | 用在哪 |
|---|---|
| ✅ 真数据 | 部门在培规模、岗位分布、模拟考核明细、知识点得分率、题目级正确率 |
| ⚠️ 数据稀疏 | 学习完成率（全 0）、正式考核（1 张有效卷）、质检/建模两部门多数指标为空 |
| ⚠️ 后端缺口 | 9 个新接口；其中 `learning`/`practice` 两处是**既有类级注解挡超管**所致 |
| ⚠️ 待拍板 | 应考口径、`assessment_result`、完成率口径、骨架页处置 |
