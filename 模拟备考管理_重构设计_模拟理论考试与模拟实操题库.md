# 模拟备考管理 重构设计：模拟理论考试 + 模拟实操题库

> 2026-09-21　依据：实测现有表与数据（模块几乎未被使用、模拟考核未挂模块、17 条实操题全挂模块）
> 用户口径：**「备考模块」这个层级去掉**，替换为两个独立功能：
> ① **模拟理论考试**（从理论题库的「模拟题库 + 通用题库」抽题；管理员按阶段配置套卷，像正式理论考试一样配置/保存/发布；实习生选不同阶段套卷**重复练习**）
> ② **模拟实操题库**（管理员从实操题库里选「通用 + 模拟」类型 → 实习生可**查看这些库里全部题目列表**）

---

## 一、目标模型

```
模拟备考管理（部门管理员 / 超管）
├─ 页签① 备考资料        ← 保持现状（material）
├─ 页签② 模拟理论考试     ← 取代「备考模块」的位置
│    阶段（第一阶段 / 第二阶段 / 第三阶段 …，可自增）
│      └─ 套卷 exam(mode=PRACTICE, type=THEORY)
│            · 抽题来源 = bank_kind='THEORY' × bank_type ∈ {PRACTICE, COMMON}   ★ 只能用模拟库/通用库
│            · 配置方式 = 与「正式理论考核」同一套：组卷规则（题库×题型配额）+ 试抽校验 + 每题分值 + 时长/通过线 + 发布
│            · 实习生：可选任一阶段套卷，**可重复练习**（不计正式成绩，逐次留 practice_record）
└─ 页签③ 模拟实操题库      ← 取代原「实操题」区块
     管理员：从 bank_kind='PRACTICAL' × bank_type ∈ {PRACTICE, COMMON} 的题库中**勾选启用**
     实习生：查看已启用题库内的**全部实操题列表**（只读：题名/方向/难度/建议用时/题目描述/考核要点/提交要求/附件）
```

### 数据流（一图看清）
```
理论题库(bank_kind=THEORY)          实操题库(bank_kind=PRACTICAL)
   ├─ bank_type=PRACTICE  ←──┐                  ├─ bank_type=PRACTICE ──┐
   └─ bank_type=COMMON    ←──┤                  └─ bank_type=COMMON   ──┤
                            │（候选）                                  │（勾选启用）
                    ┌───────┴────────┐                        ┌───────┴────────┐
                    │ 模拟理论考试套卷 │                        │ 模拟实操题库浏览 │
                    │ (exam + 组卷规则)│                        │ (practice_enabled)│
                    └───────┬────────┘                        └───────┬────────┘
                            │抽题                                       │列表
                  实习生：练习答卷（可重复）                     实习生：查看题目
```

---

## 二、★ 四个关键决策（我给了推荐，请拍板）

### 决策 1：「阶段」怎么存？→ **推荐 A（零改表）**
| 方案 | 做法 | 代价 |
|---|---|---|
| **A（推荐）** | **把 `practice_module` 重新定位为「阶段」**：表不动、语义变成"套卷分组"，UI 只显示「第一阶段/第二阶段」 | **零改表、零迁移**；现有 4 个模块可直接改名用（`11`/`12`/`默认模块` → 改成阶段名） |
| B | 给 `exam` 加 `stage` 字段，阶段变成套卷上的标签 | 加列 + 把 `module_id` 平移成 `stage` + 页面改造 |

> 理由：**"阶段"与"模块"在结构上本来就是同一件事** —— 都是"给套卷分组"。复用最省，而且以后要"跨阶段对比"也不吃亏。

### 决策 2：模拟理论考试的配置是否**完全复用正式**那套？→ **推荐是**
- 复用 `exam_bank_rule`（题库 × 题型配额）+ 发布校验（通过线不超卷面满分等）+ 试抽一套
- 前端直接复用 `ExamConfigCard`（考核配置卡），只改：候选库过滤条件、文案（考核→套卷）
- ⇒ 改动集中在**一处**：`bankOptions` 补 `bankKind` 条件（形态 × 用途）

### 决策 3：「实操题库对实习生开放」怎么存？→ **推荐 A**
| 方案 | 做法 |
|---|---|
| **A（推荐）** | `question_bank` 加 **`practice_enabled tinyint`**（管理员勾选=1）⇒ 实习生端查 `bank_kind='PRACTICAL' AND bank_type IN ('PRACTICE','COMMON') AND practice_enabled=1` |
| B | 新表 `practice_bank_scope(dept_id, bank_id, enabled)` —— 支持"同一库对不同部门分别开放" |

> 现在题库本身就是按部门建的，A 够用且零新表；将来要"公司级共享库"再升级到 B。

### 决策 4：存量 17 条模拟实操题（全挂 `module_id`）怎么办？→ **推荐搬进实操题库**
- 现状：17 条题挂 `module_id`（默认模块/11/12），`bank_id` 全空 ⇒ 新模型下不再按模块展示 → **这些题会"看不见"**
- 推荐：一次性脚本 —— **建/复用对应实操题库，把 17 条按所属模块映射迁到 `bank_id`**（幂等 + 可回滚 + 迁移前后逐条核对）
- 备选：暂不迁，在页签③加一个"历史题（未归库）"分组兜底

---

## 三、改造清单（确认后按此实施）

### 3.1 后端（全部**扩展式**：只加不改）
| # | 改动 | 说明 |
|---|---|---|
| 1 | `bankOptions` 增加可选 `bankKind` | 候选库 = **形态匹配 × 用途匹配**（已实现用途那一半）|
| 2 | `question_bank` 加 `practice_enabled` | 幂等迁移（默认 0；管理员勾选置 1）|
| 3 | 新增「实习生查看已启用实操题库题目」接口 | `GET /business/practice/banks`（启用的库列表）＋ `GET /business/practice/banks/{bankId}/questions`（只读题目列表，仅校验 `practice_enabled=1` 与部门范围）|
| 4 | 模拟理论考试复用既有接口 | 建卷/组卷/发布/试抽全用 `exam` + `exam_bank_rule` 那套（**不改签名**）|
| 5 | 重复练习 | 确认 `PracticeServiceImpl.startPractice` **不限制次数**（现状：模拟可重复，正式才限制）并保留 `practice_record` 每次记录 |

### 3.2 前端
| 页面 | 改动 |
|---|---|
| `department/study/prep/index.vue`（模拟备考管理） | 页签改为 **备考资料 / 模拟理论考试 / 模拟实操题库**；删除"模块→内容"两级导航 |
| 页签② 模拟理论考试 | 左：**阶段列表**（新建/改名/启停）；右：该阶段下的**套卷列表**（复用正式考核列表的行样式 + 状态/已作答）；点套卷 → 复用 `ExamConfigCard` 配置；`＋新建套卷` |
| 页签③ 模拟实操题库 | 上部：**可选题库多选**（显示库名/用途标签/题量）＋「保存启用范围」；下部：**题目预览**（选中库后列出其全部实操题，复用实操题库详情页的列表样式） |
| 实习生端 | ① 模拟理论考试：按**阶段分组**列出套卷 →「开始练习」（**可重复**，交卷后给分数与逐题解析）② 模拟实操题库：列出已启用题库 → 查看题目列表（只读） |

### 3.3 迁移（幂等、可回滚）
1. `question_bank.practice_enabled` 加列（默认 0）
2. 存量 17 条 `practice_subject` 归入实操题库（按模块→库映射；迁移前后核对条数）
3. 现有 4 个模块改名为阶段名（或停用测试模块 `11`/`12`）

---

## 四、风险与注意

1. **抽题候选必须严格过滤**：模拟理论套卷只能选 `THEORY × {PRACTICE, COMMON}`；否则会抽到"正式题库"的题（泄露正式考题）——这是**业务红线**，建议后端在保存组卷时也校验一次，不只靠前端过滤。
2. **重复练习的数据量**：允许重复 → `practice_record` 会增长；建议按"实习生×套卷"限制保留最近 N 次，或列表分页。
3. **正式/模拟成绩隔离**：模拟只进 `practice_record`，**绝不进** `answer_sheet`（正式成绩表）——沿用现状。
4. `practice_module` 复用为「阶段」后，**旧接口 `listPracticeExams(moduleId)` 语义不变**（仍按"阶段"过滤），前端只是改叫法 ⇒ 改动面小。
5. 存量测试模块（103 的 `11`/`12`）建议清理或改名为正式阶段名。

---

## 五、实施顺序（建议）

| 阶段 | 内容 | 风险 |
|---|---|---|
| **S1** | `bankOptions` 补 `bankKind`；`practice_enabled` 加列 | 低 |
| **S2** | 模拟备考页重构三页签：阶段 + 套卷列表（复用配置卡）+ 实操题库勾选与预览 | 中（页面重写，但接口复用） |
| **S3** | 实习生端：套卷练习（可重复）+ 实操题库浏览 | 中 |
| **S4** | 存量 17 条实操题归库脚本（幂等+核对） | 中（涉及数据，先备份） |
