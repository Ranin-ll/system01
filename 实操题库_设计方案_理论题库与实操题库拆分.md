# 题库管理：拆分为「理论题库 / 实操题库」设计方案

> 2026-09-21　依据：实测表结构（`practice_subject` 21 列 / 19 条、`exam_subject_item` 10 列 / 0 条）、页面与接口清单
> 诉求：题库管理分成**理论题库**与**实操题库**；实操题库**参考现有「模拟实操题」的逻辑**。

---

## 一、现状：两种实操题，各有一套，互不相通

| | 模拟实操题 `practice_subject` | 正式实操题 `exam_subject_item` |
|---|---|---|
| 归属 | `dept_id` + **`module_id`**（挂在「备考模块」下） | `exam_id`（**随考核而生**） |
| 字段 | **21 列，很完整**：题名、方向 direction、方向说明、难度、**建议用时**、**交付要求**、**开发约束**、**提交格式**、**命名规则**、题干 content、参考图、附件、排序、状态 | **10 列，极简**：seq、题干、题目描述、满分、参考图、附件 |
| 谁用 | 模拟备考页「实操题」区块（实习生自学/练手） | 正式实操考核（实习生逐题交文件、管理员逐题打分） |
| 复用性 | 题目可复用，但绑死在"备考模块"下 | **不可复用**：每场考核都要把题干重录一遍 |
| 数据量 | **19 条**（真实内容） | 0 条 |

⇒ 问题正是你说的：**实操题没有"题库"这一层**。模拟侧的内容资产（19 条）无法被正式考核复用；正式侧只能逐题手写，且无法沉淀。

---

## 二、目标模型：题库容器统一，形态分「理论 / 实操」

```
question_bank（题库容器）
 ├─ bank_kind = THEORY     理论题库  ── question（题目：题型/难度/章节/选项/答案）
 └─ bank_kind = PRACTICAL  实操题库  ── practice_question（实操题：题干/方向/难度/建议用时/交付要求/附件/建议满分）

（正交）bank_type = FORMAL 正式考核题库 / PRACTICE 模拟考核题库 / COMMON 通用题库
```
**两个维度正交**，共 2×3 = 6 种组合，语义都成立：
- 「Java 后端基础 · 理论 · 通用」
- 「后端开发实操 · 实操 · 正式」
- 「机房布线与上架 · 实操 · 通用」

### 2.1 实操题模型（沿用 `practice_subject` 的字段，加 3 个）

| 字段 | 来源 | 说明 |
|---|---|---|
| `id` / `bank_id` | 新增归属 | **所属实操题库**（`question_bank.id`） |
| `title` | ✅ 沿用 | 题名 |
| `direction` / `direction_desc` | ✅ 沿用 | 技能方向（如"后端接口开发"）+ 方向说明 |
| `difficulty` / `estimated_minutes` | ✅ 沿用 | 难度 / 建议用时 |
| `content` | ✅ 沿用 | 题干（作业背景与要求） |
| `deliverables` / `dev_constraints` | ✅ 沿用 | 交付要求 / 开发约束（换行分隔） |
| `submit_format` / `naming_rule` | ✅ 沿用 | 提交格式 / 命名规则 |
| `reference_images` / `attachments_json` | ✅ 沿用 | 参考图 / 附件素材 |
| **`suggest_score`** | 🆕 新增 | **建议满分** —— 挑进考核时带出默认分值（参考理论题的"每题分值"）|
| **`chapter`** | 🆕 新增 | **章节**（与理论题库统一口径，便于按章节组织；可复用 `direction` 也可独立）|
| `sort_no` / `status` / `create_by/time` / `deleted` | ✅ 沿用 | 排序 / 启用停用 / 审计 / 软删 |

### 2.2 三种消费方式（关键：实操不做"随机抽题"）

| 场景 | 理论题库 | 实操题库 |
|---|---|---|
| 组卷/选题 | **按规则抽题**：题库×题型配额（主链路）或章节配比（旧配比） | **人工挑题**：从实操题库选 N 条 → 复制成考核题目快照 |
| 落库 | `answer_sheet_item`（含题干/满分**快照**） | **`exam_subject_item`**（含题干/描述/满分/参考附件 = 天然快照） |
| 考生 | 线上答题、交卷即自动判分 | 逐题上传文件、管理员逐题打分 |

> 为什么实操不抽题：实操题**不可互换**（每道题都是一项作业），必须由管理员指定；"抽"会导致考的人做的不是一回事。

---

## 三、两种落库路线（请二选一）

| | **路线 1：复用 `practice_subject` 加列**（推荐先做） | 路线 2：新建 `practice_question` 表 |
|---|---|---|
| 做法 | `practice_subject` 加 `bank_id`（可空）、`suggest_score`；`module_id` 改为**可选**。语义升级为"实操题：可归题库、可挂备考模块" | 新建 `practice_question`（全字段 + bank_id），`practice_subject` 保留为"备考模块内的引用" |
| 存量数据 | **19 条零迁移** ✓ | 需迁移 19 条（幂等脚本） |
| 表名语义 | 表名叫 `practice_subject` 但已不只服务模拟（略别扭，可后续改名） | 名称更准 |
| 改动量 | **小**：加 2 列 + 放开 `module_id` | 中：新表 + 迁移 + 两处读路径 |
| 风险 | 低 | 中（两表并存期间易出现"同一题两份"） |
| 结论 | ✅ **先做路线 1**；若未来要继续扩展实操题体系，再平滑改名为 `practice_question`（改名不影响数据） | ⏸ 暂不必要 |

---

## 四、接口与页面改动清单

### 4.1 后端（全部为**扩展式**：只加不删）

| # | 改动 | 类型 |
|---|---|---|
| 1 | `question_bank` 加 `bank_kind`（`THEORY`/`PRACTICAL`，默认 `THEORY`）；`QuestionBank` 加字段；`insertBank`/`updateBank` 归一化 + 校验 | 加列 |
| 2 | `practice_subject` 加 `bank_id`、`suggest_score`；`module_id` 允许为空 | 加列 |
| 3 | `selectBankList` 支持按 `bankKind` 过滤；新增 `selectPracticeBankByDept` 的同类方法（按 kind 取） | 加参数 |
| 4 | `bankOptions` 增加可选 `bankKind`（与已实现的 `examMode` 并列：候选＝形态匹配 × 用途匹配） | 加参数 |
| 5 | 实操题 CRUD 接口：`GET/POST/PUT/DELETE /business/practice-question`（按 `bankId` 分页） | 新增 |
| 6 | **从实操题库挑题**：`POST /business/exam/{examId}/subject-items/pick`（body: `{questionIds:[...]}`）→ 复制为 `exam_subject_item` 快照（带 `suggest_score`、参考图、附件） | 新增 |
| 7 | `exam/detail` 的 `subjectItems[]` 增加来源标记 `fromQuestionId`（可选，用于溯源"这题来自哪条实操题"） | 加字段 |

### 4.2 前端

| 页面 | 改动 |
|---|---|
| **题库管理** `questionBank/index.vue` | 顶部加**形态页签**：「理论题库 / 实操题库」（或列 + 筛选）；新建题库弹窗加**形态**（理论/实操）+ 用途标签（已实现）；实操库列表列改为「题名/方向/难度/建议满分/状态」 |
| **实操题库详情**（新增页 `views/business/practiceBank/detail.vue`） | 复用 `practiceSubject/index.vue`（644 行）的表单字段与卡片布局，改为按 `bankId` 管理；列表显示：题名、方向、难度、建议用时、建议满分、附件数、状态 |
| **正式实操考核配置** `SubjectItemEditor.vue` | 顶部加「**＋ 从实操题库挑题**」按钮 → 弹窗（按部门/形态筛选、多选、可预览）→ 挑入后成为可编辑的考核题目（保留"逐题手写"作为兜底入口） |
| **模拟备考管理** `prep/index.vue` | 保持现状（`practice_subject` 仍按模块展示）；后续可加「引用实操题库」的批量挂载 |

---

## 五、落地顺序（建议）

| 阶段 | 内容 | 风险 |
|---|---|---|
| **S1（先做，见效快）** | ①`question_bank.bank_kind` + 题库管理页「理论/实操」分类 ②`practice_subject` 加 `bank_id`/`suggest_score` ③实操题库详情页（搬 practiceSubject 表单） | 低（加列 + 新增页面） |
| **S2** | ④`SubjectItemEditor` 的「从实操题库挑题」+ 挑题接口 ⑤`bankOptions` 加 `bankKind` 过滤 | 中（动了正式实操考核的配置流程，但入口是**新增**） |
| **S3（可选）** | ⑥ 模拟备考侧改为"引用实操题库"（模块变成挂载关系） ⑦`practice_subject` 平滑改名为 `practice_question` | 中 |

---

## 六、待你拍板的 3 点

1. **落库路线**：路线 1（复用 `practice_subject` 加列，**推荐**）／路线 2（新建 `practice_question`）？
2. **实操题要不要"章节"**：我建议加 `chapter` 与理论题库统一口径（便于按章节组织与统计）；若你觉得"方向 direction"就够，可以先不加。
3. **S1 是否现在就做**（加列 + 分类 + 实操题库详情页）？

> 备注：本方案与既有已落地能力**不冲突**——`bank_type` 三值标签（正式/模拟/通用）与用途过滤已经生效，本方案只是再加一个正交维度 `bank_kind`（形态）。

---

## 七、★ 确认（2026-09-21 14:43，用户）

> **实操题也沿用理论题那套「通用 / 模拟 / 正式」的逻辑。**

⇒ 结论：**不引入任何新的标签体系**。`bank_type`（FORMAL 正式考核题库 / PRACTICE 模拟考核题库 / COMMON 通用题库）
对**两类题库一视同仁**，因为两类库都住在同一张 `question_bank` 表里。

### 7.1 完整组合矩阵（2 形态 × 3 用途 = 6 种，全部合法）

| 形态 `bank_kind` \ 用途 `bank_type` | 正式考核题库 | 模拟考核题库 | 通用题库 |
|---|---|---|---|
| **理论题库** THEORY | 「Java 后端基础·正式」 | 「Java 后端基础·练习」 | 「Java 后端基础」 |
| **实操题库** PRACTICAL | 「后端开发实操·正式」 | 「后端开发实操·练习」 | 「机房布线上架」 |

### 7.2 候选库过滤矩阵（一处逻辑，两个条件）

```
候选库 = 形态匹配( bank_kind = 考核的题目形态 )  AND  用途匹配( bank_type ∈ 考核性质对应集合 )
```

| 考核 | 形态条件 | 用途条件 |
|---|---|---|
| 正式 · 理论考核 | `bank_kind = 'THEORY'` | `bank_type IN ('FORMAL','COMMON')` |
| 正式 · 实操考核 | `bank_kind = 'PRACTICAL'` | `bank_type IN ('FORMAL','COMMON')` |
| 模拟 · 理论考核 | `bank_kind = 'THEORY'` | `bank_type IN ('PRACTICE','COMMON')` |
| 模拟 · 实操题（自学） | `bank_kind = 'PRACTICAL'` | `bank_type IN ('PRACTICE','COMMON')` |

> **已实现的 `bankOptions(examMode)` 已覆盖"用途"那一半**；S1/S2 只需在同一处补上"形态"条件，不新增接口语义。

### 7.3 顺带的语义澄清（避免歧义）

- `bank_type` 表达的是**"这个库给哪种考核用"**（用途 / 适用场景），**不是**题目形态；
- `bank_kind` 表达的是**"这个库里装的是理论题还是实操题"**（形态 / 题目结构）；
- 两者**正交、互不影响**：改了用途标签不会影响库里的题目；理论库永远不会出现在实操考核的候选里（因为 `bank_kind` 不匹配）。

