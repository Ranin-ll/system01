# 实习生学习考核系统 - 项目代码包（已含业务模块）

> 📦 **打包时间**：2026-09-08
> 📁 **项目路径**：`/workspace/ruoyi-dev/ruoyi-vue/`

---

## ✅ 已完成的代码生成

### 后端（ruoyi-business 业务模块）

| 模块 | 类 | 说明 |
|---|---|---|
| **岗位管理** | `Position.java` | 实体 |
| | `PositionMapper.java` | MyBatis-Plus Mapper |
| | `IPositionService.java` + `PositionServiceImpl.java` | Service 层 |
| | `PositionController.java` | REST 接口 `/business/position/*` |
| **注册申请** | `RegisterApplication.java` | 实体 |
| | `RegisterApplicationMapper.java` + XML | MyBatis 复杂查询（含JOIN sys_dept/position） |
| | `IRegisterApplicationService.java` + Impl | Service（含状态机+审核历史） |
| | `RegisterApplicationController.java` | REST 接口 `/business/register/*` |
| | `AuditRecord.java` + Mapper + XML | 审核/审批历史记录 |
| **课程管理** | `Course.java` | 实体 |
| | `CourseMapper.java` + XML | MyBatis 复杂查询（统计章节数/学员数） |
| | `ICourseService.java` + `CourseServiceImpl.java` | Service |
| | `CourseController.java` | REST 接口 `/business/course/*` |
| **配置** | `MybatisPlusConfig.java` | MyBatis-Plus 配置（分页插件） |

### 前端（ruoyi-ui 业务页面）

| 页面 | 路径 | 说明 |
|---|---|---|
| **API** | `/api/business/position.js` | 岗位 API |
| | `/api/business/register.js` | 注册申请 API |
| | `/api/business/course.js` | 课程 API |
| **视图** | `/views/business/position/index.vue` | 岗位列表+CRUD |
| | `/views/business/register/index.vue` | 注册审核（含审核/驳回弹窗、详情抽屉） |
| | `/views/business/course/index.vue` | 课程管理（含发布） |
| **路由** | `router/index.js` 新增业务管理菜单 | `/business/position` `/business/course` `/business/register` |

### SQL 脚本

| 脚本 | 路径 | 说明 |
|---|---|---|
| 业务表融合 | `sql/intern_assessment_business.sql` | 32张业务表 + 角色初始化 + 岗位数据 |
| 菜单权限 | `sql/intern_assessment_menu.sql` | 业务菜单 + 按钮权限 + 角色绑定 |
| 固定主数据 | `sql/intern_assessment_seed.sql` | 5 个部门、5 个岗位、岗位部门映射、4 个固定角色 |
| 测试账号 | `sql/intern_assessment_test_accounts.sql` | 1 个超管、5 个部门管理员、5 个部门实习生（覆盖预备/正式角色） |

---

## 🚀 启动步骤（VSCode 打开后）

### 1. 打开项目

```bash
# 用 VSCode 打开
code /workspace/ruoyi-dev/ruoyi-vue
```

### 2. 初始化数据库（4 个脚本按顺序执行）

```bash
mysql -uroot -proot
source /workspace/ruoyi-dev/ruoyi-vue/sql/ry_20240629.sql;
source /workspace/ruoyi-dev/ruoyi-vue/sql/quartz.sql;
source /workspace/ruoyi-dev/ruoyi-vue/sql/intern_assessment_business.sql;
source /workspace/ruoyi-dev/ruoyi-vue/sql/intern_assessment_menu.sql;
```

### 3. 修改数据库连接

编辑 `ruoyi-admin/src/main/resources/application-druid.yml`：

```yaml
master:
    url: jdbc:mysql://localhost:3306/intern_assessment?...
    username: root
    password: root    # 改成你的密码
```

### 4. 编译后端

```bash
cd /workspace/ruoyi-dev/ruoyi-vue
export JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64
export PATH=$JAVA_HOME/bin:$PATH
mvn clean install -DskipTests
```

### 5. 启动后端

**方式A - VSCode Run 面板**：
- 打开 `融谷Application.java`
- 点击 main 方法上方的 ▶ Run 按钮

**方式B - 终端**：
```bash
cd /workspace/ruoyi-dev/ruoyi-vue/ruoyi-admin
mvn spring-boot:run
```

### 6. 启动前端

```bash
cd /workspace/ruoyi-dev/ruoyi-vue/ruoyi-ui
npm install
npm run dev
```

### 7. 访问系统

| 入口 | 地址 |
|---|---|
| 前端 | http://localhost:80 |
| 后端 API | http://localhost:8080 |
| Swagger | http://localhost:8080/swagger-ui/ |
| Druid 监控 | http://localhost:8080/druid/（ruoyi/123456） |

**登录账号**：`admin / admin123`

---

## 🎯 业务模块核心接口

### 岗位管理
- `GET  /business/position/list` - 分页查询
- `GET  /business/position/{id}` - 详情
- `POST /business/position` - 新增
- `PUT  /business/position` - 修改
- `DELETE /business/position/{ids}` - 删除
- `PUT  /business/position/changeStatus/{id}/{status}` - 修改状态

### 注册申请
- `GET  /business/register/list` - 分页查询（自动注入本部门）
- `GET  /business/register/{id}` - 详情
- `POST /business/register/submit` - 提交申请（公开）
- `PUT  /business/register/resubmit/{id}` - 重新提交
- `POST /business/register/audit` - 部门审核（pass/reject）

### 课程管理
- `GET  /business/course/list` - 分页查询（含岗位/章节/学员统计）
- `GET  /business/course/{id}` - 详情
- `POST /business/course` - 新增
- `PUT  /business/course` - 修改
- `DELETE /business/course/{ids}` - 删除
- `PUT  /business/course/publish/{id}` - 发布

---

## 📝 后续开发建议

### P0 - 立即可做（已有基础设施）
1. ✅ 岗位管理 CRUD
2. ✅ 课程管理 CRUD（章节/单项开发后即可挂接）
3. ✅ 注册申请审核流程
5. 接入 MyBatis-Plus 通用 Service/Mapper 简化业务代码

### P1 - 短期（1周内）
6. 章节管理（course_chapter）CRUD
7. 学习单项管理（study_item）CRUD
8. 学习记录跟踪（study_record）
9. 保密协议签署（agreement_signature）
10. 题库管理（question + Excel导入）

### P2 - 中期
11. 模拟考试（exam + answer_sheet）
12. 正式考试发布（含规则快照）
13. 成绩管理（assessment_result）
14. 转正审批流程

---

## 📁 项目结构总览

```
ruoyi-vue/
├── ruoyi-admin/         # 启动入口（已引入 ruoyi-business）
├── ruoyi-business/      # ★ 业务模块（MyBatis-Plus）
│   ├── src/main/java/com/ruoyi/business/
│   │   ├── controller/  # PositionController/RegisterApplicationController/CourseController
│   │   ├── domain/      # Position/RegisterApplication/AuditRecord/Course
│   │   ├── mapper/      # Mapper 接口 + XML
│   │   ├── service/     # Service 接口和实现
│   │   └── config/      # MyBatis-Plus 配置
│   └── pom.xml
├── ruoyi-common/        # 通用工具
├── ruoyi-framework/     # 框架核心
├── ruoyi-generator/     # 代码生成器
├── ruoyi-quartz/        # 定时任务
├── ruoyi-system/        # 系统模块
├── ruoyi-ui/            # 前端（已新增业务页面）
│   └── src/views/business/
│       ├── position/
│       ├── register/
│       └── course/
└── sql/
    ├── ry_20240629.sql
    ├── quartz.sql
    ├── intern_assessment_business.sql
    └── intern_assessment_menu.sql      ← 新增
```

---

## 🎉 项目环境搭建完成！

现在你可以：
1. 在 VSCode 中打开 `/workspace/ruoyi-dev/ruoyi-vue`
2. 按照启动步骤运行项目
3. 在前端左侧菜单看到 **「业务管理」** 一级菜单
4. 三个子菜单：岗位管理 / 课程管理 / 注册审核

**下一步建议**：
- 用平台代码生成器继续生成其他 29 张业务表的代码
- 实现注册申请通过后自动修改 `sys_user.user_status = 'PRE_TRAINEE'`
- 接入保密协议签署流程
- 开发学习中心和题库管理
