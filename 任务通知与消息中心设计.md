# 任务通知与消息中心 · 三角色前端与全链路设计

> 需求：2026-09-17 用户 —— 「帮我设计三种角色任务通知和消息中心的前端和全链路逻辑」
> 三种角色：**超级管理员 / 部门管理员 / 实习生**
> 本文只出设计，**未动代码**。确认后按 §七 分期落地。

---

## 〇、先摆现状（2026-09-17 实地核查，有证据）

| 层 | 现状 | 结论 |
|---|---|---|
| **表** | `notice`（公告表，**0 行**，含 `scope_type=ALL/DEPT/POSITION` + `scope_id` + `is_top` + `effective_to`）<br>`sys_notice`（0 行，平台原生公告）<br>`learning_task` / `task_assignment` / `task_submission` / `task_post`（**均 0 行**） | 公告骨架有了，**任务域表齐全但零数据** |
| **缺表** | ⚠️ **没有 `notice_target`**（多选人员接收关系）、⚠️ **没有 `notice_read`**（已读回执）<br>⚠️ **`notification` 表根本不存在** —— 部门端页面文案「notification / notice 表待后端」是**一厢情愿**，实际只有 `notice` / `sys_notice` | 未读数、已读回执**当前无法实现** |
| **后端** | `notice` / `learning_task` / `task_assignment` / `task_submission` / `task_post` **全部零 Java 层**（无 Controller/Service/Mapper） | 整条链路要新建 |
| **实习生端** | `/messages` → 指向**公共骨架页** `views/assessment/index.vue`（假数据：未读 2 / 系统通知 4 / 业务提醒 3） | 纯占位 |
| **部门管理员端** | `/department/messages/tasks`（359 行）+ `/notices`（258 行），页面自带标注「演示数据」「发送只更新本地列表」 | 排版已就绪，**接上接口即可** |
| **超管端** | **没有消息中心**；只有「系统管理 › 通知公告」（`sys_notice`，超管已授权 5 条菜单） | 需新增 |
| **顶栏铃铛** | 只在**实习生**可见（`Navbar.vue` 的 `isIntern`）；无未读数 | 需扩到全角色 |

---

## 一、职责矩阵：三类角色各自「收什么 / 发什么 / 要做什么」

| 角色 | 收（消息中心） | 发（通知 / 任务） | 待办（任务） |
|---|---|---|---|
| **超级管理员** | 异常预警、**督办回执**（谁处理了）、全局通知的送达/已读统计 | 全员 / 部门 / 岗位 / **指定人员**通知；**催办**（对部门管理员/实习生） | 无个人任务；只有「督办」动作 |
| **部门管理员** | 超管催办、**待审报名**、**待批作业**、任务逾期汇总、本部门通知 | 本部门通知；**发布学习任务**（按岗位分发） | 审核报名、批阅作业、发布/结束任务 |
| **实习生** | 任务分配、**截止提醒**、审核结果、成绩发布、协议提醒、被催办 | 任务讨论区发帖、作业提交 | 我的任务（NOT_STARTED/IN_PROGRESS/DONE/OVERDUE）+ 交作业 |

> 关键区分：**通知（广播语义）** 与 **任务（点对点待办语义）** 是两套东西 —— 一个「我告诉你」，一个「你要做」。设计上**消息中心分两个页签**呈现，但**入口同一个铃铛**。

---

## 二、数据模型

### 2.1 主表：复用 `notice`，不另立 `message`

`notice` 已有 `scope_type / scope_id / is_top / effective_to / publisher_id / publish_time`，语义正好覆盖「按范围发布」。
新建 `message` 会与它重复，且平台已有 `sys_notice` 三者打架。**决定：扩展 `notice` 为唯一消息主表**（DDL 见 §2.3）。

补的列解决三件事：**消息类型**（区分公告/任务/考核/审核/催办）、**业务锚点**（点击去哪）、**生命周期**（草稿/已发布/已撤回）。

### 2.2 两张关系表（当前完全缺失）

| 表 | 作用 | 为什么必须 |
|---|---|---|
| `notice_target(notice_id, user_id)` | `scope_type='USER'` 时的**多选人员**接收关系 | `scope_id` 是单值，存不下多人（设计稿 §12 已指出） |
| `notice_read(notice_id, user_id, read_time)` | **已读回执** | 没有它就没有「未读红点」「送达/已读统计」，两端联动断链 |

### 2.3 DDL 草案（幂等迁移脚本）

```sql
ALTER TABLE notice
  ADD COLUMN msg_type  VARCHAR(24)  NOT NULL DEFAULT 'ANNOUNCE',  -- ANNOUNCE/TASK/EXAM/AUDIT/URGE/SYSTEM
  ADD COLUMN biz_type  VARCHAR(24)  DEFAULT NULL,                 -- TASK/EXAM/REGISTER/PROMOTION
  ADD COLUMN biz_id    BIGINT       DEFAULT NULL,                 -- 业务主键（点击跳转用）
  ADD COLUMN link_url  VARCHAR(255) DEFAULT NULL,                 -- 显式跳转路径（优先 biz_* 推导）
  ADD COLUMN sender_id BIGINT       DEFAULT NULL,                 -- 发送人；系统消息为空
  ADD COLUMN status    VARCHAR(16)  NOT NULL DEFAULT 'PUBLISHED'; -- DRAFT/PUBLISHED/REVOKED
-- （上述每列均需先查 information_schema 再 ADD，保证幂等）

CREATE TABLE IF NOT EXISTS notice_target (
  id        BIGINT NOT NULL AUTO_INCREMENT,
  notice_id BIGINT NOT NULL,
  user_id   BIGINT NOT NULL,
  PRIMARY KEY (id),
  UNIQUE KEY uk_notice_user (notice_id, user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通知定向接收人';

CREATE TABLE IF NOT EXISTS notice_read (
  id        BIGINT NOT NULL AUTO_INCREMENT,
  notice_id BIGINT NOT NULL,
  user_id   BIGINT NOT NULL,
  read_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  UNIQUE KEY uk_notice_user (notice_id, user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通知已读回执';
```

### 2.4 ★ 送达规则（全链路的唯一口径）

「谁该收到」必须收敛成**一个可复用的 SQL 谓词**，三端查询共用，否则未读数与列表必然对不上：

```
可见（送达） =
      status = 'PUBLISHED'
  AND (effective_to IS NULL OR effective_to > NOW())
  AND (
        (scope_type = 'ALL'      AND 用户 status='0' 且 del_flag='0')
     OR (scope_type = 'DEPT'     AND sys_user.dept_id     = notice.scope_id)
     OR (scope_type = 'POSITION' AND sys_user.position_id = notice.scope_id)
     OR (scope_type = 'USER'     AND EXISTS (SELECT 1 FROM notice_target t
                                             WHERE t.notice_id = notice.id AND t.user_id = 当前用户))
      )
```

**未读数** = 命中上述谓词的通知数 − 其中已存在于 `notice_read` 的通知数（`LEFT JOIN ... IS NULL` 一次查出）。

### 2.5 任务域不另建消息表

`task_assignment` 本身就是 **per-user** 的（`task_id + user_id + status`），「我的任务」直接查它即可；
`deadline` / `status` 支持**实时**算「将到期 / 已逾期」，**不需要**为每条任务写一行消息。

但任务的**事件**（发布、批阅完成、逾期）要落 `notice`（`msg_type='TASK'` / `'URGE'`），这样它们才会出现在消息中心的「通知」页签里。

---

## 三、全链路（三条主链，含触发点与落库）

### 链 ① 部门管理员发布任务 → 实习生收到

```
部门管理员「任务管理」点发布
  → learning_task: DRAFT → PUBLISHED
  → 按 position_id 展开：为每个本部门在培实习生 INSERT task_assignment(NOT_STARTED)   ← 任务页签的数据
  → INSERT notice(msg_type='TASK', scope_type='DEPT', scope_id=本部门,
                  biz_type='TASK', biz_id=taskId, sender_id=部门管理员)              ← 通知页签的数据
实习生顶栏铃铛 轮询 /business/message/unread-count → +1
  → 点铃铛 → 消息中心「通知」→ 看到「新任务：…」
  → 点进详情 → 后端写 notice_read（幂等）→ 未读 −1
  → 点「去处理」→ 由 biz_type/biz_id 跳 /assessment/intern/tasks/{id}
```

### 链 ② 实习生交作业 → 批阅人收到 → 回执给实习生

```
实习生提交 task_submission(SUBMITTED)
  → INSERT notice(msg_type='TASK', scope_type='USER',
                  notice_target=[learning_task.reviewer_id], biz_type='TASK', biz_id=taskId)
部门管理员铃铛 +1 → 批阅（REVIEWED + 评语）
  → UPDATE task_assignment → DONE
  → INSERT notice(scope_type='USER', target=[该实习生], msg_type='TASK') 告知结果
```

### 链 ③ 超管催办 → 部门管理员收到（与「督办」打通）

```
超管在「督办看板」勾选若干待办 → 点催办
  → INSERT notice(msg_type='URGE', scope_type='USER', target=[该部门管理员], sender_id=超管)
  → INSERT audit_record(biz_type='URGE', reason='催办 2 项待审报名')     ← 留痕，被催办人可见
  → 部门管理员铃铛 +1
  → 该管理员处理后，回执写 notice(scope_type='USER', target=[超管])      ← 超管的「督办回执」
```

> **三条链的共同点**：都是「业务表写主记录 → `notice` 写一条事件 → 铃铛未读 +1 → 详情已读回执 → 按 biz 跳转」。这套骨架一次做好，三条链共用。

---

## 四、接口清单（全部新增，前缀 `/business/message`，不改既有接口）

| # | 接口 | 用途 | 可见角色 |
|---|---|---|---|
| 1 | `GET /business/message/unread-count` | 铃铛未读数（可按 `msg_type` 分组返回） | 全部登录用户 |
| 2 | `GET /business/message/list` | **我的**消息（分页；`msgType` / 已读未读 / 关键字筛选） | 全部 |
| 3 | `GET /business/message/{id}` | 详情，**并顺带写已读**（幂等） | 接收人 |
| 4 | `POST /business/message/{id}/read` | 标记已读 | 接收人 |
| 5 | `POST /business/message/read-all` | 全部已读（可按类型） | 接收人 |
| 6 | `POST /business/message` | **发送通知**（scope ALL/DEPT/POSITION/USER） | 超管（全范围）/ 部门管理员（**限本部门**，后端强制） |
| 7 | `GET /business/message/sent` | 我发过的（带**送达数 / 已读数 / 未读名单**） | 超管 / 部门管理员 |
| 8 | `POST /business/message/{id}/revoke` | 撤回（`status='REVOKED'`，撤回后不再可见） | 发送人 / 超管 |

**任务域接口**（表已存在，零 Java，按现有 `/business/xxx` 风格补齐）：

| # | 接口 | 用途 | 角色 |
|---|---|---|---|
| 9 | `GET/POST/PUT /business/task/list|add|edit` | 任务 CRUD | 部门管理员 |
| 10 | `POST /business/task/{id}/publish` / `/end` | 发布（**展开 assignment**）/ 结束 | 部门管理员 |
| 11 | `GET /business/task/my` | 我的任务（含 assignment 状态、剩余天数） | 实习生 |
| 12 | `GET /business/task/{id}` | 任务详情（目标/学时/截止/我的提交） | 三端（超管只读） |
| 13 | `POST /business/task/{id}/submit` | 提交作业 | 实习生 |
| 14 | `GET /business/task/{id}/submissions` + `POST /review` | 提交列表 + 批阅 | 部门管理员 |
| 15 | `GET /business/task/{id}/posts` + `POST` | 讨论区（`task_post`） | 实习生 / 部门管理员 |

**权限总口径**：超管**只读**（`isGlobalReadOnly()` 保持业务实例只读），仅 **催办/发通知** 可写；部门管理员限本部门；实习生仅本人。

---

## 五、前端设计

### 5.1 共享组件（新增 `src/components/Message/`，三端复用）

| 组件 | 职责 | 要点 |
|---|---|---|
| `BellBadge.vue` | 顶栏铃铛 + 未读红点 | 未读数来自 `/unread-count`；**路由切换刷新 + 60s 轮询**；`0` 时不显示红点 |
| `MessageList.vue` | 消息列表 | 类型标签（公告/任务/考核/审核/催办）、未读小圆点、来源人、相对时间；支持「只看未读」 |
| `MessageDetail.vue` | 详情抽屉 | 打开即写已读；底部「去处理」按 `biz_type/biz_id` 跳转；底部显示发送人与范围 |
| `SendNoticeDialog.vue` | 发送通知 | **四档范围选择器**（全体 / 部门多选 / 岗位多选 / 指定人员搜索多选）+ 置顶 + 有效期；超管与部门管理员共用（后者范围锁本部门） |
| `TaskCard.vue` | 任务卡 | 任务名 / 目标 / 学时 / 截止倒计时 / 状态色（进行中·将到期·逾期） |
| `RecipientStat.vue` | 送达与已读 | 「送达 N · 已读 M · 未读 K」+ 未读名单（可一键再提醒） |

### 5.2 三角色页面

| 角色 | 路由 | 结构 |
|---|---|---|
| **实习生** | `/messages`（**替换现有骨架页**，新建独立文件） | 页签「通知 / 我的任务」：<br>· 通知 = `MessageList` + `MessageDetail`<br>· 我的任务 = `TaskCard` 列表（按截止排序）+ 提交入口 + 讨论区入口 |
| **部门管理员** | `/department/messages/notices`（**改造现有页**） | 左：`SendNoticeDialog` 触发的发送区；右：已发送列表 + `RecipientStat`（送达/已读/未读名单） |
| **部门管理员** | `/department/messages/tasks`（**改造现有页**） | 任务列表 + 新建（按岗位分配）+ 提交情况与批阅 + 讨论区 |
| **超级管理员** | `/super/org/notify`（**新增**，即原「通知中心」） | 四档范围发送 + 已发列表 + 送达/已读统计；**与督办看板打通**（催办即发定向通知） |
| **超级管理员** | 顶栏铃铛 | 收「督办回执」「异常预警」 |

> 现有部门端两页（359 + 258 行）排版已经按最终形态做过，**接接口为主，不大改结构** —— 符合「已完成的板块尽量不动」的约束。

### 5.3 顶栏铃铛：从「仅实习生」改为**全角色**

`Navbar.vue` 当前 `v-if="isIntern"`。改为全部角色可见（未读数来自接口），下拉里的「消息中心」项同样放开。这与上一轮刚做的「用户胶囊」不冲突（胶囊在右，铃铛在其左侧）。

---

## 六、与现有实现的接线点（不重复造）

| 已有 | 本次怎么用 |
|---|---|
| `sys_notice` + 平台原生「通知公告」 | **保持原样**做系统级公告（超管已授权 5 条菜单）；业务通知走 `notice`。两者并存、不互相覆盖 |
| `views/department/messages/{tasks,notices}.vue` | 改造接接口（只动 script 的数据层 + 少量 template） |
| `views/assessment/index.vue`（骨架页） | **不改**它（公共骨架页，被 4 条路由复用）→ 新建独立文件 + 追加路由 |
| `audit_record` | 催办/发送留痕复用（已有 `insertAuditRecord`） |
| `isGlobalReadOnly()` | 超管只读边界**保持不变**；仅催办与发通知属可写 |
| 上一轮的 `assessment_config` | 预警阈值、`protocol_required` 等可驱动 SYSTEM 类消息的生成 |

---

## 七、分期落地

| 阶段 | 内容 | 后端 | 前端 |
|---|---|---|---|
| **P0** | 表 + 收到（先把「看得到」做通） | `notice` 扩展 + `notice_target` / `notice_read` 两表；接口 1~5 | 铃铛全角色 + 实习生端消息中心（新建文件） |
| **P1** | 发出（管理端能发、能看回执） | 接口 6~8 + 发送范围校验 | 超管 `/super/org/notify`；部门端 `notices` 改造 |
| **P2** | 任务域闭环 | 接口 9~14（任务 CRUD / 发布展开 assignment / 提交 / 批阅） | 部门端 `tasks` 改造；实习生「我的任务」 |
| **P3** | 增强 | 讨论区（15）、逾期自动标记（定时任务）、督办与通知打通、订阅偏好 |

---

## 八、待你拍板的 5 个决策点

| # | 决策点 | 选项 A（我的推荐） | 选项 B | 影响 |
|---|---|---|---|---|
| 1 | 消息主表 | **扩展 `notice`**（少一张表，scope 语义已有） | 新建 `message` 表（与 `notice`/`sys_notice` 三表并存） | 影响全部接口与查询 |
| 2 | 消息中心结构 | **分「通知 / 任务」两页签** | 混在一个流里按类型标签区分 | 影响实习生端与部门端页面 |
| 3 | 铃铛范围 | **全角色** | 维持仅实习生 | 影响 `Navbar.vue` 与未读接口调用频率 |
| 4 | 已读口径 | **打开详情即已读** + 支持「全部已读」 | 手动点「标记已读」 | 影响 `notice_read` 写入时机 |
| 5 | 任务域节奏 | **P2 一并做**（表已齐、零 Java，属部门端范围） | 先只做通知，任务下期 | 影响 P0/P1 交付时间 |

> 你回「按 A」或指出要改的点，我就从 **P0** 开始落地（P0 不动任何既有接口，只在 `notice` 上补列 + 加两张新表）。

---

## 九、后端全链路实现规格（可直接照着写代码）

> 配套可视化稿：**`消息中心设计稿.html`**（10 节，含三端页面 + 本节内容的图示）

### 9.1 新增文件清单（与既有分层同构，全部放 `ruoyi-business`）

| 层 | 文件 | 职责 |
|---|---|---|
| 迁移 | `ruoyi-vue/sql/intern_assessment_message_migration.sql` | `notice` 扩列（**逐列先查 `information_schema`**）+ 建 `notice_target` / `notice_read`；幂等可重跑 |
| domain | `Notice.java`（扩字段）、`NoticeTarget.java`、`NoticeRead.java` | 与表一一对应 |
| mapper | `NoticeMapper.java`（**送达谓词只写这里一处**）、`NoticeTargetMapper.java`、`NoticeReadMapper.java` | 列表 / 未读数 / 送达数 / 已读 upsert |
| service | `IMessageService` + `MessageServiceImpl` | 范围校验、发送、已读、撤回、统计 |
| service | `ITaskService` + `TaskServiceImpl` | 任务 CRUD、发布展开、提交、批阅、讨论区 |
| controller | `MessageController`（`/business/message`）、`TaskController`（`/business/task`） | 只做参数校验 + `@PreAuthorize` |
| 定时 | `OverdueTaskJob`（可挂平台 `sys_job`） | 逾期标记 + 逾期通知 |

> **不改动**：任何既有 Controller / Service / Mapper 的签名、返回结构、权限注解。

### 9.2 核心 SQL（三处，全部复用同一谓词）

```sql
-- ① 统一送达谓词（抽成 Mapper 里的公共片段，三处引用，改一处全局生效）
       n.status = 'PUBLISHED'
  AND (n.effective_to IS NULL OR n.effective_to > NOW())
  AND (
        (n.scope_type = 'ALL'      AND u.status = '0' AND u.del_flag = '0')
     OR (n.scope_type = 'DEPT'     AND u.dept_id     = n.scope_id)
     OR (n.scope_type = 'POSITION' AND u.position_id = n.scope_id)
     OR (n.scope_type = 'USER'     AND EXISTS (SELECT 1 FROM notice_target t
                                                WHERE t.notice_id = n.id AND t.user_id = u.user_id))
      )

-- ② 未读数（一次查完，避免 N+1；铃铛接口就调它）
SELECT COUNT(*) FROM notice n
  LEFT JOIN notice_read r ON r.notice_id = n.id AND r.user_id = #{userId}
 WHERE <谓词> AND r.id IS NULL

-- ③ 已读写入（唯一键兜幂等：重复打开详情不会写两行）
INSERT INTO notice_read (notice_id, user_id, read_time) VALUES (#{id}, #{userId}, NOW())
  ON DUPLICATE KEY UPDATE read_time = read_time
```

**为什么必须收敛成一处**：铃铛未读数、消息列表、发送方的送达统计、实习生工作台的「未读消息」KPI —— 这四处若各写各的，迟早出现「铃铛 3、列表 2」的对不上。

### 9.3 事务边界与幂等点（最容易写错的地方）

| 操作 | 必须同事务 | 幂等手段 |
|---|---|---|
| **发布任务** | ① `learning_task` DRAFT→PUBLISHED ② 按 `position_id` 批量写 `task_assignment` ③ 写 `notice` | `UNIQUE(task_id, user_id)` + `INSERT IGNORE`，重复发布不重复分配 |
| **批阅** | ① `task_submission` 更新评语/状态 ② `task_assignment.status='DONE'` ③ 写回执 `notice`(USER→实习生) | `task_submission.version + 1` |
| **催办** | ① 写 `notice`(USER, target=被催办人) ② 写 `audit_record` 留痕 | 同一事项 24h 内限 1 次（查最近一条 URGE） |
| **标记已读** | 仅写 `notice_read` | 唯一键 + `ON DUPLICATE KEY` |
| **撤回** | `notice.status='REVOKED'` | 已撤回再撤回直接返回成功 |

### 9.4 权限校验点（后端强校验，前端 disabled 只是体验）

| 校验 | 位置 | 规则 |
|---|---|---|
| 范围 | `MessageServiceImpl.send()` | 部门管理员提交 `scope_type='ALL'` → **拒绝**；`='DEPT'` 时 `scope_id` 必须是自己的 `dept_id`；`='USER'` 时 target 必须都在本部门 |
| 收件人 | `MessageController` | 只能读**送达到自己**的消息（谓词里带当前用户）；不是收件人访问详情 → 统一返回「消息不存在」 |
| 任务归属 | `TaskServiceImpl` | 部门管理员的 `learning_task.position_id` 必须是本部门绑定岗位；实习生只能看/交自己的 `task_assignment` |
| 超管边界 | `MessageServiceImpl.send()` | 超管**可以写** `notice`（发通知/催办），但任务与作业仍受既有 `isGlobalReadOnly()` 约束 |

### 9.5 状态机（只有两处，别扩散）

```
learning_task    : DRAFT ──publish──> PUBLISHED ──end/到期──> ENDED
task_assignment  : NOT_STARTED ──首次打开──> IN_PROGRESS ──批阅通过──> DONE
                                                              └──超期未交──> OVERDUE（仍可补交，补交后回 IN_PROGRESS）
task_submission  : SUBMITTED ──批阅──> REVIEWED          （逾期提交 = OVERDUE）
notice           : DRAFT ──发布──> PUBLISHED ──撤回──> REVOKED
```

### 9.6 定时任务（P3，可挂平台 `sys_job`）

| 任务 | 频率 | 动作 |
|---|---|---|
| 逾期标记 | 每日 00:10 | `task_assignment` 中 `deadline < NOW()` 且未 DONE → `OVERDUE`；写一条 `notice`(URGE) 给本人 + 其部门管理员 |
| 到期提醒 | 每日 09:00 | 剩 ≤3 天的任务 → 定向提醒本人（同一任务只提醒一次，用 `notice` 去重） |
| 未读回收 | 每周一 | 统计上周已读率，写进超管「通知中心」的统计卡 |

### 9.7 迁移脚本注意点

1. **`notice` 扩列必须逐列幂等**：先查 `information_schema.COLUMNS`，存在则跳过（本项目已有此惯例）。
2. **两张新表用 `CREATE TABLE IF NOT EXISTS`**，唯一键名固定（`uk_notice_user`），便于后续引用。
3. **`scope_type` 增加 `USER` 不需要改列定义**（现有就是 `varchar`），但**要在注释里补上**。
4. 迁移**不重建库、不动数据**；`notice` 现有 0 行，无需回填。
5. 上线顺序：迁移 → 后端 → 前端（前端拿不到新接口时会走 `.catch()` 降级，不会白屏）。

---

# V2 · 三角色统一化

> 2026-09-17 追加。V1（§一~§九）定的是"有什么"；V2 定的是"三端怎么共用一套"，并把前后端设计收敛到**可实现的规则**上。
> **原则一句话：页面、组件、接口、状态机、文案全部统一；差异只落在「能力」这一件事上。**

## 十、统一内核：一份能力矩阵定三端差异

### 10.1 三端真正不同的只有「能力」，不是「逻辑」

| 能力 | 实习生 | 部门管理员 | 超级管理员 |
|---|---|---|---|
| 读自己的消息 / 标记已读 | ✅ | ✅ | ✅ |
| 发通知 · 全体 | ❌ | ❌ | ✅ |
| 发通知 · 本部门 | ❌ | ✅（范围锁本部门） | ✅（任意部门） |
| 发通知 · 按岗位 | ❌ | ✅（本部门岗位） | ✅ |
| 发通知 · 指定人员 | ❌ | ✅（限本部门人） | ✅（任意人） |
| 撤回通知 | ❌ | ✅（自己发的） | ✅（全部） |
| 查看送达 / 已读名单 | ❌ | ✅（本部门） | ✅（全部） |
| 催办 | ❌ | ❌（被催办方） | ✅ |
| 发布 / 结束任务 | ❌ | ✅（本部门岗位） | ❌（只读） |
| 批阅作业 | ❌ | ✅（自己是批阅人） | ❌（只读） |
| 交作业 / 讨论区发帖 | ✅（仅自己） | ❌ | ❌ |
| 我的任务 | ✅ | ❌（看"提交情况"） | ❌（看汇总） |

**实现口径**：
- 前端：按钮由 `CapabilityButton` 读同一份矩阵决定**隐藏/禁用**（只是体验优化）。
- 后端：`CapabilityResolver.allows(role, capability, targetDeptId)` 是**唯一判定处**，所有 `@PreAuthorize` 之外的范围校验都走它 —— **前端隐藏 ≠ 后端放行**。

### 10.2 数据范围推导复用既有口径（不新造）

| 角色 | `scopeDeptId` | 来源 |
|---|---|---|
| 超管 | `null`（= 全量） | 既有 `currentScopeDeptId()` 在 `isGlobalReadOnly()` 时返回 null |
| 部门管理员 | 本人 `dept_id` | 同上 |
| 实习生 | 不适用（只按 `user_id` 过滤） | — |

> 这样「超管看到全部、部门管理员看到本部门」这条既有规则**自动适用于消息与任务**，不需要为消息再写一套范围逻辑。

## 十一、页面统一：2 个共享页 + `mode` 适配

### 11.1 四页收敛为两页

| V1（4 个页面） | V2（2 个共享页 + 3 个 mode） |
|---|---|
| 实习生消息中心 / 超管通知中心 / 部门端通知管理 | **`MessageCenterPage.vue`**（`mode: intern / deptAdmin / superAdmin`） |
| 部门端任务管理 / 实习生我的任务 | **`TaskCenterPage.vue`**（`mode: intern / deptAdmin`） |

三端路由只是"壳"，指向同一个组件：

| 角色 | 路由 | mode | 差异（仅此三处） |
|---|---|---|---|
| 实习生 | `/messages` | `intern` | 页签=通知/我的任务；无发送区；有"交作业" |
| 部门管理员 | `/department/messages/notices` · `/tasks` | `deptAdmin` | 页签=已发/收件；有发送区（锁本部门）；有"批阅/结束任务" |
| 超管 | `/super/org/notify` | `superAdmin` | 页签=已发/回执；四档范围；只读业务实例 + 催办 |

### 11.2 共享组件（V1 六个 + V2 新增一个）

| 组件 | V2 变化 |
|---|---|
| `BellBadge` | 不变（三端一致） |
| `MessageList` | 加 `mode`：决定列与批量动作 |
| `MessageDetail` | 不变（打开即已读 + biz 跳转） |
| `SendNoticeDialog` | 加 `scopeLimit`：部门管理员传自己的 `dept_id`，范围选择器自动置灰越权项 |
| `TaskCard` | 加 `mode`：`intern` 显示"交作业"，`deptAdmin` 显示"提交情况/批阅" |
| `RecipientStat` | 不变 |
| **`CapabilityButton`**（新） | 传 `capability` 名，内部读矩阵 → 自动 `disabled` 或 `hidden`，并带"为什么不可用"的 tooltip |

### 11.3 统一空态与文案（三端同一句，避免各写各的）

| 场景 | 文案 |
|---|---|
| 无消息 | 暂无消息（区别「筛选后为空」→ 没有符合条件的消息） |
| 无任务 | 暂无任务 |
| 无权操作 | 当前角色没有该权限（而不是"按钮消失"） |
| 已被撤回 | 该通知已撤回 |

## 十二、事件 → 通知：统一触发清单（唯一真源）

> **规矩：业务代码里不允许散落 `new Notice()`**。所有通知都通过 `NoticePublisher.publish(eventCode, bizType, bizId, targets)` 发出，
> 事件码在这张表里登记；新功能要发通知，**只往这张表加一行**。

| # | 事件码 | 触发点 | 类型 | 范围 | 收件人 | biz 锚点 | 前端跳转 | 阶段 |
|---|---|---|---|---|---|---|---|---|
| 1 | `TASK_PUBLISHED` | 部门管理员发布任务 | TASK | DEPT | 本部门该岗位实习生 | TASK/taskId | `/messages?tab=task` | P2 |
| 2 | `TASK_DUE_SOON` | 定时（剩 ≤3 天） | TASK | USER | 任务未完成者 | TASK/taskId | 同上 | P3 |
| 3 | `TASK_OVERDUE` | 定时（已过截止） | URGE | USER | 本人 + 其部门管理员 | TASK/taskId | 同上 | P3 |
| 4 | `SUBMISSION_SUBMITTED` | 实习生交作业 | TASK | USER | `learning_task.reviewer_id` | TASK/taskId | `/department/messages/tasks` | P2 |
| 5 | `SUBMISSION_REVIEWED` | 批阅完成 | TASK | USER | 该实习生 | TASK/taskId | `/messages?tab=task` | P2 |
| 6 | `REGISTER_WAIT_AUDIT` | 报名提交 | AUDIT | USER | 本部门管理员 | REGISTER/id | `/department/people/register-review` | P1 |
| 7 | `REGISTER_APPROVED` | 审核通过 | AUDIT | USER | 该实习生 | REGISTER/id | `/messages` | P1 |
| 8 | `REGISTER_REJECTED` | 审核驳回 | AUDIT | USER | 该实习生 | REGISTER/id | `/messages` | P1 |
| 9 | `PROMOTION_APPROVED` | 转正终审通过 | AUDIT | USER | 该实习生 | PROMOTION/id | `/assessment/intern/learning/result` | P2 |
| 10 | `EXAM_RESULT_PUBLISHED` | 成绩发布 | EXAM | DEPT | 该场次考生 | EXAM/examId | `/assessment/intern/learning/result` | P2 |
| 11 | `PROTOCOL_REQUIRED` | 首登未签协议 | SYSTEM | USER | 本人 | — | `/index`（协议门） | P1 |
| 12 | `SUPER_URGE` | 超管催办 | URGE | USER | 被催办人 | — | 对应待办页 | P1 |

> 1/4/5/9/10 依赖任务域与考核域；6~8、11、12 用现有业务表即可发出。

## 十三、前后端优化项（相对 V1）

| # | 项 | V1 | V2 优化 | 收益 |
|---|---|---|---|---|
| 1 | 页面数 | 4 个独立页 | **2 个共享页 + mode** | 少写两套 UI，逻辑天然不分叉 |
| 2 | 能力判定 | 散在各页 `v-if` | **`CapabilityResolver` 一处** | 改权限只动一处；前后端同一份矩阵 |
| 3 | 未读算法 | 多处各写 SQL | **一处谓词**（§9.2） | 杜绝"铃铛 3、列表 2" |
| 4 | 通知产生 | 各业务里 `new Notice()` | **`NoticePublisher.publish(eventCode,…)`** | 触发点可枚举、可审计、可关停 |
| 5 | 接口数 | 15 个 | **11 个**（下表） | 少 4 个往返 |
| 6 | 列表返回 | 只回 `rows/total` | 加 `extra.unreadByType` | 一次请求同时拿到列表与未读分组，省一次调用 |
| 7 | 轮询 | 固定 60s | **页面隐藏暂停 + 失败退避（60→120→300s）** | 少打后端、断网不刷屏 |
| 8 | 错误文案 | 各写各的 | 统一（§11.3） | 三端体验一致 |

**接口归并（15 → 11）**：① `read` 与 `read-all` 合并为 `POST /read`（body 带 `ids` 或 `all:true`）；② 任务 `add|edit` 合并为 `POST /save`；③ `publish|end` 合并为 `POST /{id}/status`（body 带目标状态）；④ 发送与重发（再提醒）共用同一发送接口。

## 十四、统一接口契约

| 项 | 约定 |
|---|---|
| 分页 | 入参 `pageNum/pageSize`；出参 RuoYi `TableDataInfo`（`rows/total`），额外数据放 `extra` |
| 非分页 | `AjaxResult.success(data)`；写操作 `AjaxResult.success("已保存（已留痕）", data)` |
| 权限注解 | 统一 `@ss.hasAnyRoles('…')`；**范围与归属校验一律在 Service 层**（`CapabilityResolver` / 部门归属） |
| 时间 | 统一 `yyyy-MM-dd HH:mm`（`application.yml` 已配 `spring.jackson.date-format`） |
| 已读 | 只有两个入口：`GET /{id}`（顺带已读）与 `POST /read`；其余接口**不得**改 `notice_read` |
| 撤回 | 撤回后从所有人列表消失、已读记录保留（审计需要）；再次撤回直接返回成功 |
| 幂等 | `notice_read` 唯一键、`task_assignment` 唯一键、`task_submission.version` 三处 |

### 14.1 待你拍板（V2 新增 2 点）

| # | 决策点 | 选项 A（推荐） | 选项 B |
|---|---|---|---|
| 6 | 超管端目录命名 | **也改成一级「任务与通知」**（下挂 通知中心 / 督办看板），三端命名一致、用户跨角色不用重新认位置 | 保持 v3 定稿：通知中心挂「组织与人员」下（三端目录名不统一） |
| 7 | 消息保留与归档 | **永久保留**（`notice` 行数可控，历史可审计） | 设置保留期（如 180 天），过期归档到冷表 |

---

## 十五、公告 vs 通知：两种载体 + 「公告位」（V2.1 补）

> 2026-09-17 用户补充：**「超级管理员还能发全局或指定人员公告通知」**。
> 这句话把 V1/V2 里被我混着说的「公告」与「通知」**分开了** —— 它们是两种**载体**，展示位置与生命周期都不同。

### 15.1 系统里的既有约定（不是新造的概念）

| 证据 | 位置 |
|---|---|
| RuoYi 原生字典 `sys_notice_type` = **1 通知 / 2 公告** | `views/system/notice/index.vue` 的 `dict.type.sys_notice_type` |
| 部门端通知管理页的类型枚举含 **「公告」** | `views/department/messages/notices.vue:157`（考核提醒 / 课程更新 / 公告 / 任务提醒） |
| 实习生工作台有独立区块 **「10 通知」** | `views/index.vue:152-156`（当前标注"暂无接口，为示例数据"） |
| 部门端页面自述"对应实习生端顶栏铃铛与**工作台公告位**" | `notices.vue:11` |

→ 结论：**「公告」要有自己的展示位**，不能只丢进消息中心列表。

### 15.2 两种载体：判据表

| 维度 | **公告**（`msg_type='ANNOUNCE'`） | **通知**（`msg_type` = TASK/EXAM/AUDIT/URGE/SYSTEM 或人工定向） |
|---|---|---|
| 语义 | 面向一片人**公开告知** | 面向**具体的人**点对点告知 |
| 谁发 | **只有超管**（部门管理员只能发本部门通知） | 超管、部门管理员、**系统事件** |
| 范围 | **全局 / 部门 / 岗位 / 指定人员 四档全支持** | 同上（定向为主） |
| 展示位 | **① 工作台「公告位」 + ② 消息中心「通知」页签**（两处都有） | **只进消息中心 + 铃铛未读** |
| 置顶 `is_top` | ✅ 有意义（公告位排最前） | 少用 |
| 有效期 `effective_to` | ✅ **到期自动从公告位消失**（记录保留） | 一般不用 |
| 生命周期 | 可关闭、可撤回 | 可撤回 |
| 是否产生未读 | 进消息中心则计未读；公告位**不计未读红点** | 计未读 |

> **一句话判据：要"挂出来给人看"的是公告，要"戳到人"的是通知。**

### 15.3 ★ 新增展示位：工作台「公告位」（V2.1 的关键补强）

| 项 | 规则 |
|---|---|
| 位置 | 三端工作台（实习生 `views/index.vue` 的「10 通知」区块、部门/超管工作台） |
| 数量 | 最多 **3 条**；置顶优先，其余按 `publish_time` 倒序 |
| 过滤 | `msg_type='ANNOUNCE'` + `status='PUBLISHED'` + 命中我的送达谓词 + `effective_to` 为空或未过期 |
| 交互 | 点击展开详情（**不写已读**，公告位是"公示"不是"消息"）；右上「全部 ›」跳消息中心 |
| 过期 | 到 `effective_to` 自动消失（**记录保留在消息中心**，历史可查） |
| 空态 | 「暂无公告」，不占位、不拉高卡片 |

> 这条与实习生工作台现有的「10 通知」区块**直接对接** —— 那个区块现在是假数据，接上这个接口即变真实。

### 15.4 超管发送器 UI 变化（在 §6 超管通知中心上叠加一层）

发送前先选**载体**，再选**范围**：

```
载体：   [ 公告 ]  [ 通知 ]          ← 新增的第一层选择
范围：   [ 全体 ] [ 部门 ] [ 岗位 ] [ 指定人员 ]   ← 两档都支持四范围
附加：   置顶 ☑（仅公告）   有效期至：____（仅公告）
```

- 选「公告」→ 显示置顶与有效期；右侧出现**公告位预览**（"实习生工作台将显示前 3 条"）。
- 选「通知」→ 隐藏置顶/有效期，强调**收件人**与**点击去向**。
- **部门管理员看不到「公告」选项**（后端同样拒绝），只能发本部门通知 —— 能力矩阵里加一行。

### 15.5 能力矩阵补一行（§10.1 增量）

| 能力 | 实习生 | 部门管理员 | 超级管理员 |
|---|---|---|---|
| 发**公告**（全局 / 部门 / 岗位 / 指定人员） | ❌ | ❌ | ✅ |
| 发**通知**（本部门 / 指定人员） | ❌ | ✅（限本部门） | ✅（任意） |

### 15.6 与平台原生 `sys_notice` 的分工（需你确认）

| 方案 | 内容 | 评价 |
|---|---|---|
| **A（推荐）** | 业务公告与通知**统一走 `notice`**；`sys_notice` 保留（平台原生页仍在「系统管理」下），但**侧栏不给它做入口**，避免出现"两个发公告的地方" | 一个发放入口，公告位/未读/回执全部打通 |
| B | 继续双轨：`sys_notice` 发全局公告、`notice` 发业务通知 | 用户要在两个地方发，且 `sys_notice` 无公告位、无已读回执 |

> 选 A 的话，**之前"决策 2：`sys_notice` 保留原样"需要微调**：保留表与页面，但**不再作为公告的推荐入口**。

### 15.7 接口增量（在 §13 的 11 个基础上 +1）

| 接口 | 用途 |
|---|---|
| `GET /business/message/announcements` | **工作台公告位**：返回当前有效公告（≤3 条，`is_top` 优先），**不写已读** |

发送接口 `POST /business/message` 增加入参 `msgType`（`ANNOUNCE` / `NOTIFY`）—— 服务端据此校验「谁有资格发公告」。



