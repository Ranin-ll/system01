# 部门管理员端 目录与页面布局设计方案

> 版本 **V1.1**　2026-09-17（V1.0 → V1.1：三个决策点已定，全文按结论修订，新增 §八 落地实现方案）
> 实例基线：**开发部门 · 开发部门管理员**（与实习生端设计稿同基线，便于两角色对照）
> 配套视觉稿：**`实习生与部门管理员端整体设计稿.html`**（2026-09-17 起两角色稿合一，顶部切换；管理端部分为 `d1`~`d12` 锚点；原独立文件 `部门管理员端整体设计稿.html` 已并入）
> 上游文档：`实习生端目录与页面布局设计方案.md`（§6.8 考核配置与发布 沿用）、`后端实现现状盘点与考核补全方案.md`、`模拟考核与正式考核模块设计.md`
> ⚠️ 原「超管端整体设计稿.html」因「部门直接终审」决策 **已失去同步**（见 §7.5 ②），暂不作为上游。

---

## 决策已定（2026-09-17）

| # | 决策点 | **你的选择** | 与我建议的差异 |
|---|---|---|---|
| 1 | 转正终审权 | **部门管理员直接终审**（不再经超管） | 我原建议「部门推荐 → 超管终审」，已按你的选择改写 |
| 2 | 正式考核发布粒度 | **分环节发布**（理论、实操各自独立发布与时间窗） | 我原建议「整场统一发布」，已按你的选择改写 |
| 3 | 知识点模块与题库 | **拆多库**（每个知识点模块一个题库） | 我原建议「不拆，左树右表」，已按你的选择改写 |

> ⚠️ 第 3 点是三个决策里**唯一有硬技术约束**的（会撞现有 SQL 的 `LIMIT 1` 无 `ORDER BY`，且现有题量摊到每个模块后不足以抽题）。
> 我在 **§八 落地实现方案**给出了完整解法（3 处改动 + 1 个数据前置条件），**不解决就无法上线**，请重点看那一节。
> 另外：这三个决策都会**改动既有业务规则**（「预备→正式需超管审批」不再成立），以及**让超管端设计稿失去同步**，两处影响见 §八.5。

---

## 零、结论：你的四点结构成立，我做了 6 处修正

你的四目录划分（工作台 / 人员管理 / 学习与考核管理 / 任务通知）**方向是对的**，它对应管理者的四个真实心智：

| 一级目录 | 管理者心里在想的事 |
|---|---|
| 工作台 | 我这部门现在**什么状况**？有什么**等我处理**？ |
| 人员管理 | 人**从哪来、走到哪一步、能不能转正** |
| 学习与考核管理 | 我**给什么内容、怎么考、怎么判** |
| 任务通知 | 我要**找谁、让他做什么** |

比现在 DB 里的「部门运营 / 考核认证管理 / 系统运营」三目录（22 个菜单、14 个是骨架假数据页）干净得多。
它是**按管理者的工作流分目录**，而不是按系统模块分目录 —— 这一点和实习生端设计稿的第一条原则完全一致，两角色同构。

以下是需要修正的地方，请逐条过：

| # | 问题 | 修正 |
|---|---|---|
| **1** | **三层侧栏过深**。模拟备考管理 / 正式考核管理 / 成绩管理各自还带 2~4 个子项，落进侧栏就是三级展开 | **侧栏只到两级**，模块内的子项做成**页签**（与实习生端「学习与考核」用页签是同一套做法）。侧栏二级共 10 项，宽度和深度都健康 |
| **2** | **「题库管理」与「理论考试配置」职责会打架**。如果考试配置里也能录题，就会出现两套题源 | 明确：**「题库管理」是唯一的理论题源；所有考试配置只做「选题库 + 抽题规则 + 分值」，不录题**。✅ 现有后端就是这个模型（`exam.bank_id` + `single_count/multi_count/judge_count`），照这个口径设计**零后端改动**就能跑 |
| **3** | **「实操模拟题题库」的名字与定位**。它其实同时服务模拟实操和正式考核实操 | 保留在你的「模拟备考管理」下（尊重你的结构），但**改名为「实操题库」**，页面副标题写明「本库同时供模拟实操练习与正式考核实操环节引用」。正式考核的实操配置设计成**从本库选题**，避免管理员把同样的题录两遍 |
| **4** | **「任务通知」是动词堆叠，且缺"发完之后怎么样"** | 更名 **「任务与通知」**，二级两项：**任务管理** / **通知管理**。每页都是「上方新建发布区 + 下方已发布跟踪表」，任务能看到谁交了谁没交 |
| **5** | **「实习生管理」的统计下钻**需要后端聚合 | 现有接口只能拿到花名册，拿不到完成率/正确率。**本期先用示例数据撑起统计卡与列**（打「示例数据」标记），并按 `后端实现现状盘点与考核补全方案.md` 的 P0-1/P1-3 补聚合接口 |
| **6** | ⚠️ **「超管端」会不同步**。现有 `超管端整体设计稿.html` 写的是「超管 = 部门管理员同名功能的全局版」，业务管理下只列了 5 项（注册审核/课程管理/实习生管理/实习批阅/考核配置与发布） | 部门管理员端改完后，超管端的「业务管理」子项必须**同步扩成同一套 10 项**，否则「全局超集」这句话不成立。建议超管端保持 **4 个同构目录 + 1 个系统管理**（系统管理超管独有） |

### 另外两处命名建议（可选）

- **「学习与考核管理」** 与实习生端一级目录「学习与考核」重名，且是从**内容生产**视角出发的。可选短名：**「培养与考核」**（侧栏更省宽，也不与实习生端撞名）。我按你的原命名出稿，改名只需替换菜单 `menu_name`。
- ~~转正审核归属需要你拍板~~ → **已定：部门管理员直接终审**（见文档头「决策已定」）。

---

## 一、信息架构

```
部门管理员端（/department）
│
├─ 工作台                          /department/dashboard            单页
│
├─ 人员管理                        /department/people               DIR
│   ├─ 注册审核                    /department/people/register-review    ✅ 已完成
│   ├─ 实习生管理                  /department/people/students           ★ 新设计（含下钻）
│   │    └─ 个人全景档案           /department/people/students/:id       hidden 下钻页
│   └─ 实习转正审核                /department/people/promotion          ★ 新设计
│
├─ 学习与考核管理                  /department/study                DIR
│   ├─ 课程管理                    /department/study/courses             ✅ 已完成
│   ├─ 题库管理                    /department/study/banks               ★ 改造（按知识点模块）
│   ├─ 模拟备考管理                /department/study/prep                页签壳
│   │    ├─ 备考资料               /department/study/prep/materials     ★ 新
│   │    ├─ 模拟理论考核配置       /department/study/prep/theory        ★ 新
│   │    └─ 实操题库               /department/study/prep/subjects      ★ 改造
│   ├─ 正式考核管理                /department/study/exam                页签壳
│   │    ├─ 考核列表 / 发布记录    /department/study/exam/list           ★ 新（主视图）
│   │    ├─ 考试说明               /department/study/exam/:id/notice     ★ 新
│   │    ├─ 理论考试配置           /department/study/exam/:id/theory     ★ 沿用设计稿 §6.8
│   │    └─ 实操考试配置           /department/study/exam/:id/practice   ★ 沿用设计稿 §6.8
│   └─ 成绩管理                    /department/study/scores              页签壳
│        ├─ 考核批阅               /department/study/scores/grading     ★ 改造
│        └─ 成绩统计               /department/study/scores/stats       ★ 新（含下钻）
│
└─ 任务与通知                      /department/messages             DIR
    ├─ 任务管理                    /department/messages/tasks           ★ 新
    └─ 通知管理                    /department/messages/notices         ★ 新
```

**层级原则**：侧栏**只有两级**；凡「一个实体 + 几个编辑分区」的都收敛成页签壳（`模拟备考管理`/`正式考核管理`/`成绩管理`），与实习生端的 `学习与考核` 页签壳结构完全对称。

### 与现有 `sys_menu` 的迁移对照

| 新菜单 | 来源 | 动作 |
|---|---|---|
| 工作台 | `2032 部门工作台`(assessment/index 骨架) | 改 component → `business/dashboard/index`，**重建页面** |
| 人员管理 | 新建 DIR | `path='people'` |
| ├ 注册审核 | `2012 注册审核` | **移动** parent → 人员管理，component 不动 |
| ├ 实习生管理 | `2033 实习生管理`(assessment/index 骨架) | 改 component → `business/student/index` |
| └ 实习转正审核 | 新建 | `business/promotion/index` |
| 学习与考核管理 | 新建 DIR | `path='study'` |
| ├ 课程管理 | `2006 课程管理` | **移动** parent → 学习与考核管理，component 不动 |
| ├ 题库管理 | `2054 题库管理` | **移动** parent → 学习与考核管理，component 不动 |
| ├ 模拟备考管理 | `2049 模拟实操管理` 升级 | 改 component → 页签壳 |
| │　├ 备考资料 | 新建 | `business/prep/material` |
| │　├ 模拟理论考核配置 | 新建 | `business/prep/theory` |
| │　└ 实操题库 | `2049` 原页面下沉 | `business/prep/subject` |
| ├ 正式考核管理 | `2065 考核管理` 升级 | 改 component → 页签壳 |
| ├ 成绩管理 | `2034 实习批阅` 升级 | `business/score/index` |
| └ 考核批阅 | `2034` 下沉 | `business/score/grading` |
| 任务与通知 | 新建 DIR | `path='messages'` |

> **迁移原则**：一律用 `UPDATE sys_menu SET parent_id=…` **移动**已有菜单，**不新建重复菜单行** —— 保住 `menu_id` 就等于保住 `sys_role_menu` 里的授权关系，避免 DEPT_ADMIN 权限丢失。
> 一次性做不完的（`2043 考核批阅`、`2039 理论题库`、`2040 实操题库`、`2037 课程设计` 等骨架项）统一 `visible='1'` 隐藏，不删除，留作后续轨道。
> 现有 `2019 部门运营 / 2020 考核认证管理 / 2021 系统运营` 三个目录做完迁移后整体 `visible='1'`。

---

## 二、页面布局规格

### 2.1 工作台（`/department/dashboard`）— 我设计的部分

设计目标与实习生端工作台区分开：**实习生端工作台回答"我学得怎么样"，部门管理员工作台回答"我要先处理什么"**。
所以本页第一屏是**待办**，不是图表 —— 管理端最高频的动作是"清待办"。

自上而下 8 个区块：

| # | 区块 | 布局 | 内容 | 数据来源 |
|---|---|---|---|---|
| ① | **身份条** | 通栏 | 部门名 + 部门管理员姓名 + 本部门在册实习生数 + 累计已转正 | `/getInfo` + `sys_user` ✅ |
| ② | **待办中心** ★ | 通栏，5 张可点卡 | 待审核注册 / 待批阅答卷 / **待转正审批** / 待发布考核 / 进行中任务；每张显示数量 + 最早一条的时间，点击直达对应列表（带筛选参数） | 注册 ✅、批阅 ✅、考核 ✅、转正 ⚠️、任务 ⚠️ |
| ③ | **KPI 指标** | 6 列等宽 | 在册实习生 / 预备实习中 / 已转正 / 本部门学习完成率均值 / 本部门模拟正确率均值 / 正式考核通过率 | 前 3 项 `sys_user.user_status` ✅；后 3 项 ⚠️ 待聚合 |
| ④ | **培养漏斗** | 左 5 栏 | 注册待审 → 实习中(PRE) → 转正审核中 → 已转正(FORMAL)，每段显示人数与转化率 | `sys_user.user_status` ✅ **今天就能真数据** |
| ⑤ | **学习进度分布** | 左 7 栏 | 按完成率分档（≥90% / 70–89% / 50–69% / <50%）的实习生人数，横向条形 | ⚠️ 待聚合 |
| ⑥ | **考核概览** | 右 7 栏 | 最近 5 场考核：名称 / 类型 / 参与人数 / 平均分 / 通过率 / 状态；点行进成绩管理 | `exam` + `answer_sheet` ✅ |
| ⑦ | **最薄弱知识点 Top5** | 右 5 栏 | 本部门实习生整体错误率最高的 5 个知识点 + 错误率，点击带知识点筛选进题库管理 | ⚠️ 待聚合（`question.knowledge_point` 现成） |
| ⑧ | **最近动态** | 通栏或左 | 时间轴：注册审核 / 发布考核 / 完成批阅 / 转正审批，取最近 10 条 | `audit_record` ✅ 已有真实数据 |

> **不做的**：跨部门横向对比。设计稿 §6.6 已定「部门平均分仅对本部门范围计算，跨部门不可见」，所以部门管理员端**不出现其他部门数据**，也不出现"全公司排行"。这是权限边界，不是设计取舍。

---

### 2.2 人员管理 > 注册审核（`/…/people/register-review`）

**✅ 已完成，本次只做两处小改，不动数据层**

1. 页面顶部补一条与工作台 ② 呼应的**未审计数条**（`/business/register/summary` 已提供）。
2. 「通过」后弹出**分配导师 + 岗位确认**的确认弹层（若已有则保留）。

> 页面本体 `views/business/register/index.vue` 的 `script` 数据层与 API 调用**一律不动**，只调 template/style。

---

### 2.3 人员管理 > 实习生管理（`/…/people/students`）★ 新设计

**定位**：本部门所有实习生的花名册 + 统计下钻。

| 区块 | 布局 | 内容 |
|---|---|---|
| ① 统计卡 | 4 列 | 在册实习生 / 预备实习中 / 转正审核中 / 已转正（点击可当筛选用） |
| ② 筛选条 | 一行 | 岗位下拉 / 培养状态分段（全部·预备·审核中·已转正·已停用）/ 导师下拉 / 入职时间范围 / 关键字；右侧「导出」 |
| ③ 花名册表 | 通栏 | 列：姓名 / 岗位 / 培养状态 / 导师 / 保密协议 / 学习完成率 / 模拟正确率 / 正式考核 / 最近活跃 / 操作 |
| ④ 行操作 | — | 查看档案 / 发任务 / 发通知 / 调整导师 |

**表格列的数据可得性**（重要，决定先做哪些）：

| 列 | 可得 | 说明 |
|---|---|---|
| 姓名 / 岗位 / 培养状态 / 导师 / 最近活跃 | ✅ | `sys_user` + `dept_position` |
| 保密协议 | ✅ | `agreement_signature`（7 行真实数据） |
| 学习完成率 | ✅ | `/business/learning/courses` 按 user 逐人算（人多时需聚合接口） |
| 模拟正确率 | ✅ | `/business/practice/my` 聚合 |
| 正式考核 | ✅ | `/business/answer-sheet/my`（需按 exam 聚合，管理端需 `/grading/{examId}`） |

> 结论：**③ 表格可以全部用真实数据**，只有 ① 统计卡里的比率类需要聚合接口。所以我建议**统计卡先用示例数据打标，表格直接用真数据**。

**下钻：个人全景档案（`/…/people/students/:id`，hidden）**

⚠️ **本页直接复用 `超管端整体设计稿.html` 里的「④ 个人全景档案」**，两角色同款页面、只有作用域不同（部门管理员锁定本部门，超管可跨部门）。档案分区：

| 区块 | 内容 | 与实习生端的联动 |
|---|---|---|
| 身份头 | 姓名 / 岗位 / 导师 / 培养状态 / 入职日期 / 协议状态 | 实习生端工作台 ① 身份条 |
| 培养进度 | 协议签署 → 在线学习 → 正式考核 → 转正发证 四步进度条 | 实习生端工作台 ⑧ |
| 学习明细 | 课程完成率 + 各课程进度条 | 实习生端「在线学习」逐门课 |
| 模拟考核记录 | 每次抽题数 / 正确数 / 正确率 / 时间，可点开逐题回顾 | 实习生端「模拟考核」历史 |
| 转正审批 | 部门终审通过 → 即时生效并发证 | 实习生端「**考核成绩与转正申请**」页的申请状态时间线随之变为「审批通过 · 已转正」（本决策跳过超管终审） |
| 能力画像 | 四维度雷达 + 各维度来源与样本量；`visible_scope=ADMIN_ONLY` 的评价**只在此处可见** | 实习生端工作台 ④ 画像（仅 OPEN 的评价） |
| 管理操作 | 调整导师 / 发任务 / 发通知 / **提交转正审核** | — |

---

### 2.4 人员管理 > 实习转正审核（`/…/people/promotion`）★ 新设计

**定位**：把"预备实习生"变成"正式实习生"的审批台。

**两段式：列表 → 详情决策**

| 区块 | 内容 |
|---|---|
| ① 分段筛选 | 待处理 / 已通过 / 已驳回（带计数） |
| ② 待办列表 | 实习生 / 岗位 / 学习完成率 / 正式考核成绩 / 协议 / 提交时间 / 操作（审核） |
| ③ 审核详情页 | **资格核对清单**（逐项 ✓/✗，未通过项标红并说明差多少）+ **成绩事实区**（正式考核理论/实操/综合分）+ **能力画像**（四维度）+ **部门管理员评价**（优势 / 待提升 / 改进建议，对应 `stage_evaluation` + `assessment_result.eval_comment`）+ **结论**（推荐转正 / 驳回并填写原因） |

**资格核对清单的四项**（每项都有对应数据或规则落点）：

| 核对项 | 判定依据 | 现状 |
|---|---|---|
| 学习完成率 ≥ 阈值 | `learning` 完成率 + `exam_rule_snapshot.course_done_threshold`（默认 70） | ✅ 完成率已有；阈值待读快照 |
| 正式考核已通过 | `answer_sheet.pass_flag` | ✅ |
| 保密协议已签署 | `agreement_signature` | ✅ |
| 无未完成必修项 | 完成率明细 | ✅ |

**状态机（按你的决策：部门直接终审）**：

```
DEPT_PENDING（待部门审核）
    ├─ PASSED    （部门审批通过）→ 自动发证 → sys_user.user_status = FORMAL_TRAINEE
    └─ REJECTED  （驳回，填原因，可重新提交）
```

对应 `promotion_application.status` 的真实枚举：`DEPT_PENDING / SUPER_PENDING / PASSED / REJECTED`。
**本决策下 `SUPER_PENDING` 被跳过不再进入**（字段与列保留，不删表结构，便于日后回退或超管代操作留痕）。

**这一页的按钮语义**：不再是「推荐给超管」，而是 **「审批通过（直接生效并发证）」** / **「驳回」**。页面头部用一句话点明终审责任：
> 本页审批**即时生效**：通过后该实习生转为正式实习生并生成电子证书，**无二次复核环节**。

**⚠️ 三条必须配套的改动**（否则会出现"能审批但审不了"）：

| # | 改动 | 原因 |
|---|---|---|
| 1 | `sys_user.user_status` 的 `PENDING_PROMOTE → FORMAL_TRAINEE` 驱动方从 SUPER_ADMIN 改为 DEPT_ADMIN | 状态机归属变了 |
| 2 | 为 `DEPT_ADMIN` 补转正审批权限（新增 `sys_menu` 记录 + 角色授权） | 现有权限里没有这项 |
| 3 | 保留一条**纠错路径**：`operate_log` 留痕 + 超管可「撤回转正」 | 部门自己批自己人 + 自动发证不可逆，需要兜底 |

> 💡 好消息：`promotion_application` / `certificate` / `stage_evaluation` **全部零 Java 层**（表建好、无接口），整套都是**新增**，所以不受「已实现接口一律不改」的约束，改动成本主要在写新接口，不在改老代码。

> ⚠️ **业务规则变更备案**：原需求「预备→正式需通过正式考试**并经超管审批**」在本决策下作废，改为**部门审批即生效**。超管端设计稿里画的「终审」环节需同步删除（见 §八.5）。

---

### 2.5 学习与考核管理 > 课程管理（`/…/study/courses`）

**✅ 已完成**（`views/business/course/runtime.vue`）。本次**零改动**。仅在页面顶部加一行面包屑链接到工作台 ⑤ 学习进度分布。

---

### 2.6 学习与考核管理 > 题库管理（`/…/study/banks`）★ 拆多库（按知识点模块）

**你的要求**：部门管理员可以**根据不同的知识点类型模块创建题库**。
**你的决策**：**拆多库**（每个知识点模块一个独立题库）。

⚠️ **先说清三个硬约束**（都已在源码/真实数据上核实，不是推测）。不解决就无法上线：

**约束 1 — 模拟考核会"随机命中一个模块"**

`QuestionBankMapper.selectPracticeBankByDept`（`QuestionBankMapper.xml:87`）的真实 SQL：

```sql
SELECT ... FROM question_bank b
WHERE b.deleted = 0 AND b.bank_type = 'PRACTICE' AND b.dept_id = #{deptId}
LIMIT 1                       -- ← 没有 ORDER BY
```

调用点唯一：`PracticeServiceImpl:59`。**今天每部门恰好只有 1 条 PRACTICE 题库**（实测：dept 104/105/103/106/107 各 1 条），所以看不出问题。
一旦拆成多个模块库，同一个实习生每次点「开始模拟考核」**抽到的模块不可预期**（MySQL 无 `ORDER BY` 时返回行不确定）。

**约束 2 — 拆多库 ≠ 跨模块出卷**

三条抽题 SQL **全部是单库**（`AND q.bank_id = #{bankId}`）：
`selectQuestionsForExam`（模拟，`ORDER BY RAND() LIMIT 10`）、`selectQuestionsByType`（正式，按题型）、`selectSubjectQuestions`（实操）。
正式考核的题库绑定也是**单个**字段 `exam.bank_id`。
→ **拆库只改变"题目怎么归堆"，不会让一份卷自动覆盖多个模块**。要跨模块出卷，必须新增**组卷**能力。

**约束 3 — 现有题量摊到模块后不够抽** ⚠️ 最容易被忽略

实测每部门题库里 `question.knowledge_point` 的实际分布：

| 部门 | PRACTICE 库 | FORMAL 库 | 覆盖知识点 | 平均每模块题量 |
|---|---|---|---|---|
| 开发 104 | bank 2 · **24 题** | bank 3 · **24 题** | 14 类 | **约 1.7 题** |
| 设计 105 | bank 5 · **49 题** | bank 12 · **48 题** | 17 类 | 约 2.9 题 |
| 交付 103 / 质检 106 / 建模 107 | **0 题** | **0 题** | — | 0 |

拆库后开发部门的模块库会是「Docker 2 题 / Redis 2 题 / Java 1 题 …」。
而模拟考核固定 `LIMIT 10`、正式理论按 `single_count/multi_count/judge_count` 抽（默认配置够十几题）——
**单模块库抽不满，卷面会变短甚至是空卷**。

> ✅ **所以拆库前有一个必须先做的数据前置条件**：把每个模块的题量补到 **≥ 15 题**（够支撑 10 题模拟 + 15 题正式）。
> 17 个模块 × 15 题 ≈ 255 题/部门；开发/设计已在 24~49 题，**还需要大量补题**。
> 交付/质检/建模三部门是 0 题，等于要从零建。**这件事的工程量远大于页面改造**，请先确认谁来出题。

---

**在这三个约束下，"拆多库"的落地解法（三步）**

| 步 | 动作 | 具体做法 | 破坏性 |
|---|---|---|---|
| **①** | 题库表加模块标识 | `question_bank` 幂等新增 `knowledge_module VARCHAR(64)`；`bank_type` 新增取值 **`MODULE`**（现有取值 `FORMAL` / `PRACTICE` 不变） | 无（纯加列） |
| **②** | **保住模拟考核不坏** | 每部门**保留且仅保留 1 条** `bank_type='PRACTICE'` 的「综合模拟入口库」；所有模块库用 `bank_type='MODULE'`。`selectPracticeBankByDept` 因为只剩 1 条而**自动恢复确定性** | 无（**零后端改动**） |
| **③** | 跨模块出卷能力 | 正式考核理论卷走**组卷**：管理员选 N 个模块库 + 每模块抽几题 → 抽题结果**快照**写入 `exam_paper_question`（该表已建好、零 Java 层，字段含 `exam_id/seq/qtype/stem/options_json/answer/score/is_subjective`） | 新增，不动老接口 |

> 第 ② 步是关键取舍：**模块库与"模拟考核入口库"分离**，这样拆库完全不碰模拟考核链路。
> 代价是「模拟考核」默认仍从综合库抽题、不按模块——若你要**模拟考核也能按模块练**，需要额外一步：
> 给 `/business/practice/start` 增加**可选** `bankId` 参数（不传则走原逻辑，向后兼容），实习生端在模拟考核页先选模块。这一步属于**改已实现接口**，需你单独确认是否允许。

---

**页面布局（按"拆多库"改写：模块库列表 → 进库管题）**

主视图 = **模块库列表**（不再是左树右表）：

| 区块 | 布局 | 内容 |
|---|---|---|
| ① 顶部筛选 | 通栏 | 分段：`全部 / 综合模拟库 / 正式库 / 知识点模块库`；右侧「+ 新建模块库」 |
| ② 模块库表格 | 通栏 | 列：**模块名** / 类型 / 题量 / 题型分布（单·多·判）/ 状态 / 更新时间 / 操作（**进入管题** · 编辑 · 启停 · 删除） |
| ③ 题量健康度 | 每行内联 | 题量 < 15 显示橙色 `缺题` 徽标 + 悬浮提示「该模块仅 2 题，模拟考核需 10 题、正式考核需 15 题，请补题」 |
| ④ 汇总条 | 底部 | 「共 17 个模块 · 覆盖题量 145 · ⚠️ 交付/质检/建模三部门 0 题 · 开发部门 11 个模块题量不足」|

进库后的题目表：列 = 题号 / 题型 / 题干摘要 / 难度 / 分值 / 状态；行内编辑·删除。
批量操作保留现状：Excel 导入（`/business/question/import/{bankId}` ✅ 已有）、导出模板 ✅ 已有、批量启用停用。

**知识点字典**：新增/重命名模块 → 写 `question_bank.knowledge_module`，同时批量同步该库题目的 `question.knowledge_point`（两处保持一致，避免"库名与题目知识点对不上"）。

**数据迁移**：现有 145 题按 `question.knowledge_point` 自动归入对应模块库。
建议做法是**不移动题目、而是把现在的单条 PRACTICE/FORMAL 库改造成「模块库容器」**——但 `question_bank` 无自引用父子关系，所以实际做法是：
① 新建各模块库 → ② `UPDATE question SET bank_id=<模块库ID> WHERE knowledge_point=<模块名>` → ③ 原 PRACTICE 库保留为空壳作「综合模拟入口」（后续往里放综合题）。
脏值 `knowledge_point='7'`（1 条）统一归入「未分类」模块。详见 §八.3 的迁移脚本。

> ⚠️ 与现有题库管理页（`business/questionBank/index.vue`）的关系：本次是**改造**——现有页面是「题库列表 + 题目表」两层，拆库后层级不变，**只是题库从 2 条变成 N 条**，改动集中在列（加"模块名/题量健康度"）与详情页的题型分布。`script` 里的 API 调用可全部沿用（`/business/question-bank` CRUD + `/business/question` CRUD 都够用）。

---

### 2.7 学习与考核管理 > 模拟备考管理（`/…/study/prep`）★ 新 + 页签壳

页签壳：**备考资料 / 模拟理论考核配置 / 实操题库**（沿用实习生端 `learning/shell.vue` 的实现方式）

#### 页签 1 · 备考资料（`prep/materials`）

| 区块 | 内容 |
|---|---|
| ① 上传区 | 拖拽上传，支持 **PDF / Word / PPT / 视频 / 图片 / 压缩包**；上传时填：资料名称 / 类型（文档·视频·模拟题入口）/ 适用岗位 / 简介 / 有效期 |
| ② 资料列表 | 卡片或表格：文件名 + 类型图标 / 大小 / 适用岗位 / 版本 / 上传时间 / 状态（草稿·已发布·已停用）/ 操作（预览·替换·停用·删除） |
| ③ 生效范围 | 标注「发布后，本部门**对应岗位**实习生在其「正式考核」页可见」 |

> 数据落点：`material` 表（15 列，含 `material_type` DOCUMENT/VIDEO/PRACTICE、`status` DRAFT/PUBLISHED/DISABLED、`valid_from/valid_to`）。**表结构完全够用，只缺 Java 层**（见后端缺口 P1-4）。

#### 页签 2 · 模拟理论考核配置（`prep/theory`）

| 区块 | 内容 |
|---|---|
| ① 抽题规则 | 题源（**默认锁定本部门"综合模拟库"**，见 §7.1 改动②）/ 抽题数量（默认 **10**，与后端 `PracticeServiceImpl:63` 写死的 10 对齐）/ 题型配比（单选·多选·判断各几题，之和 = 抽题数量） |
| ② 难度配比 | 易 / 中 / 难 占比（对应 `question.difficulty`） |
| ③ 时间规则 | 是否限时 / 时长 / 可否重练 |
| ④ 预览 | 右侧「按当前规则试抽一套」按钮 → 展示实际抽到的题目列表，便于校验配比 |
| ⑤ 保存 | 保存即对本部门实习生生效 |

> ⚠️ **注意**：抽题数量、题型配比目前**在后端写死/无配置表**（`PracticeServiceImpl:63` 硬编码 10，且模拟考核不分题型）。这一页要落地需要后端把模拟考核配置化 —— 已列入 `后端实现现状盘点与考核补全方案.md`，**建议排在 P1**。
> 本期该页先做**只读展示现状 + 示例数据**，打「示例数据」标记。

> 🔀 **受「拆多库」决策影响的那一项**：拆库后模块库是多个题库，而 `selectPracticeBankByDept` 只会返回**唯一那条** `PRACTICE` 综合库。
> 所以本页的「题源」有两种形态：
> - **形态 A（不需要改后端）**：题源固定为综合模拟库，本节其余配置照旧。模拟考核**不按模块**。
> - **形态 B（需给 `/business/practice/start` 加可选 `bankId`）**：题源变成**模块多选**，实习生可在模拟考核页选模块练习。
>
> **这一项需你确认**（见 §八 下一步）。在确认前，本页按形态 A 出稿。

#### 页签 3 · 实操题库（`prep/subjects`）★ 已实现（`模拟实操管理` 升级版）

题库按**方向分组**组织，与实习生端「模拟考核 › 实操题库」的卡片网格一一对应（同一张表、同一套字段）。

| 区块 | 内容 |
|---|---|
| ① 概览 | 题目总数 / 方向分组数 / 已启用 / 最长用时（对齐实习生端 hero 统计） |
| ② 方向筛选 | 方向 chip 条（全部 + 各方向题数）；方向可自由填写，同名即同组 |
| ③ 列表 | 列：题名（含题干摘要）/ 方向 / 难度 / 建议用时 / 交付·参考图数 / 附件 / 状态 / 操作；支持部门（超管）、难度、状态、关键词筛选 |
| ④ 新建 / 编辑 | 题名\* / 方向\* / 方向说明 / 难度 / 建议用时 / 题干\* / 交付要求（每行一条）/ 开发约束（每行一条）/ 提交格式 / 命名规则 / 参考图（多图上传+缩略图）/ 附件（多文件）/ 排序号 / 状态 |
| ⑤ 页面提示 | 「本库同时供**模拟实操练习**与**正式考核实操环节**引用」——避免管理员重复录题 |

> ✅ **后端已闭环**：迁移脚本 `intern_assessment_practice_subject_ext.sql` 给 `practice_subject` 补齐
> `title / direction / direction_desc / difficulty / estimated_minutes / deliverables / dev_constraints /
> submit_format / naming_rule / reference_images / sort_no`，实体与 Mapper 同步扩展，
> **Controller / Service 的方法签名与权限注解一律未改**（既有的 list·{id}·POST·PUT·status·DELETE·published·published{id} 全部照旧）。
> 演示数据见 `intern_assessment_practice_subject_seed.sql`（开发部门 3 方向 × 5 题）。
> ✅ **2026-09-17 已端到端验证**：管理员写入（全字段）→ 列表回读（字段落库正确）→ 实习生端可见 → 停用后实习生侧消失 → 恢复启用 → 清理；另验证「只填题干」时题名/方向/难度自动兜底、跨部门取详情统一返回「不存在或未发布」。
> 实习生侧同一功能的页面规格见 `实习生端目录与页面布局设计方案.md` **§6.4.1**。

---

### 2.8 学习与考核管理 > 正式考核管理（`/…/study/exam`）★ 分环节发布 · 一页两卡

**一页两卡**（对齐已确认的页面样式 —— 理论考试卡 + 实操考试卡，各卡右上角显示发布状态，底部一条「保存并发布」）：

```
考核批次：[2026Q4 开发实习生（第一期） ▾]      [预览考生视角]  [历史批次]
┌ 理论考试 ─────────────────────── 实习生基础理论知识能力考核配置   〔已发布〕┐
│ 考试信息（左 4 栏）            │ 试题分布（右 8 栏）  [+新增][保存][保存发布] │
│ 考试形式 / 考试时长            │ 知识模块 | 题目数量 | 知识点占比 | 单选占比 | │
│ 通过分数-总分 / 考试题型        │ 单选题数量 | 多选占比 | 操作                │
│ 题目数量 / 试题分值            │                                            │
└──────────────────────────────────────────────────────────────────────────┘
┌ 实操考试 ─────────────────────── 与实习生考核认证页面同步的实操任务  〔已发布〕┐
│ 考试信息（左 4 栏）            │ 考试题目（右 8 栏）  [从实操题库选][保存][发布]│
│ 同理论六项                     │ 题目序号 | 题目名称 | 题目描述 | 考核要点 | 操作│
└──────────────────────────────────────────────────────────────────────────┘
 当前版本 v1.0 · 最近更新 2026-09-17 14:00 · 发布后在实习生端「正式考核」页面生效   [保存并发布]
```

**分环节发布 = 两个环节各自独立发布**：

- 理论卡「保存发布」→ 理论环节立即开放；实操卡可继续留草稿，此时实习生端显示「理论已开放 · 实操待发布」。
- **批次**（`exam.period_code`）只作页面顶部一个**下拉分组键** —— 本版**不做「期次列表」、不做折叠分组**，这是相对上一版的主要简化点。
- 落库只需 `exam` 幂等新增 **`stage VARCHAR(16)`**（`THEORY` / `PRACTICE`）；`period_code` 可选。`exam` 现有 `bank_id / question_count / single_count / multi_count / judge_count / single_score / multi_score / judge_score / duration / pass_line / start_time / end_time / subject_count / subject_content / subject_attachment` **全部够用，零新增**。

**两个环节各自的配置区**（沿用设计稿 §6.8，只保留实际要用的字段）：

| 环节 | 左栏「考试信息」 | 右栏 |
|---|---|---|
| **理论考试** | 考试形式 / 考试时长 / 通过分数-总分 / 考试题型 / 题目数量 / 试题分值 | **试题分布表**：知识模块 · 题目数量 · 知识点占比 · 单选题占比 · 单选题数量 · 多选题占比 · 操作；顶部 `+新增 / 保存 / 保存发布` |
| **实操考试** | 同上六项（题型 = 实操任务·文件包提交；分值 = 每题 50 分） | **考试题目表**：题目序号 · 题目名称 · 题目描述 · 考核要点 · 操作；顶部 `从实操题库选 / 保存 / 保存发布` |

- 理论「试题分布」**每一行 = 一个知识模块库**（这就是「拆多库」的落点，见 §2.6）；跨模块出卷写 `exam_paper_question`。
- 校验：各模块**题目数量之和 = 左侧「题目数量」**、**知识点占比之和 = 100%**，否则该卡「保存发布」置灰并提示原因。
- 实操提交的是**答案文件包**，不进入自动判分（详见下方与 §2.9）。

#### 成绩与判分 —— 本版的关键简化

| 环节 | 判分方式 | 管理员要做什么 |
|---|---|---|
| 理论考试 | ✅ **后端自动判分（已实现）** —— `AnswerSheetServiceImpl` 用 `normalizeAnswer()` 比对 `question.answer`，直接写 `is_correct / ai_score / final_score / pass_flag` 并置 `PUBLISHED` | **什么都不用做**，只看成绩 / 导出 |
| 实操考试 | ⚠️ **人工评分（已实现落库）** —— 实习生上传答案文件包（存 `subject_answer`），`gradePractically(sheetId, manualScore, manualComment)` 写 `manual_score / manual_comment` | 在 §2.9 下载文件包 → 查看 → **录入分数与评语** |

> **结论：不需要 AI 阅卷，也不需要逐题人工判理论题。** 理论自动、实操人工 —— 两条链路都在现有后端里，前端只做展示与录分。

#### 仍需补的后端（按优先级）

| 优先级 | 缺口 | 用途 |
|---|---|---|
| **P0** | `exam` 加 `stage` 列 | 区分理论 / 实操两个环节（不加则页面做不出分环节） |
| **P0** | 组卷写 `exam_paper_question` | 拆多库的完成前提（一份卷覆盖多个知识模块） |
| **P0** | `GET /business/answer-sheet/list`（待评分列表读接口） | §2.9 实操评分的队列来源 |
| P1 | `exam` 加「说明文字 / 说明附件 / 考生须知」三列 | 考核说明在实习生端的展示 |
| P1 | `exam_rule_snapshot` 接通（门槛 / 权重 / 补考） | 现零引用，勾了也是装饰 |

> ⚠️ 分环节发布的两点连带成本：① 实习生端「正式考核」页需补一个**「另一环节待发布」态**（原三态 + 1，见 §四第 5 项）；② 成绩统计口径从「按场」改为「按批次」（见 §2.9）。

---

### 2.9 学习与考核管理 > 成绩管理（`/…/study/scores`）★ 理论自动 · 实操人工

页签：**实操评分 / 成绩汇总**

#### 页签 1 · 实操评分（`scores/grading`）

理论已自动判分、**无需人工批阅**；这个页签只处理**实操**。

| 区块 | 内容 |
|---|---|
| ① 批次切换 | 顶部下拉（本部门各批次）；状态分段 `待评分 N / 已评分 N / 全部 N`；右侧 `批量通过` |
| ② 待评分队列（左 4 栏） | 按提交时间排序：`姓名 · 岗位` / `答案包已提交 · 时间 · 待评分`；点任意一条右侧加载面板 |
| ③ 评分面板（右 8 栏） | 顶部标「理论已自动判分 XX」；**考生提交的答案文件包**（📦 压缩包 · 大小 · 文件清单）→ `下载压缩包` / `在线预览`；`题目一得分` / `题目二得分`；`实操总分 x/100` 与通过线；评语（`manual_comment`）；`存草稿` / `提交并评下一份` |

> ✅ 落库已实现：`gradePractically(sheetId, manualScore, manualComment)` → `manual_score / manual_comment`；提交时 `subject_answer` 已存文件路径。
> ⚠️ 需补一个**读接口** `GET /business/answer-sheet/list`（按批次 + 评分状态查队列）；`score_correction`（改分留痕）表已建但零 Java 层。

#### 页签 2 · 成绩汇总（`scores/stats`）★ 新

| 区块 | 布局 | 内容 | 数据 |
|---|---|---|---|
| ① 批次切换 | 顶部 | 下拉：本部门各批次；分段：`合并视图 / 仅理论 / 仅实操` | ✅ |
| ② 汇总表 | 通栏 | 列：`# / 姓名 / 岗位 / 理论（自动） / 实操（人工） / 综合分 / 结论 / 操作（档案）` | 理论 ✅ 逐份；综合分待聚合 |
| ③ 薄弱知识点 | 右 4 栏 | 该批次错误率最高的知识点（hbar 条形） | ✅ `knowledge_point` **纯前端即可算** |
| ④ 导出 | 右上 | 导出该批次成绩表（Excel） | — |

**「结论」列口径**（分环节 + 部门直接终审 的交汇点）：

```
理论 pass_flag = true  且  实操 pass_flag = true   →  ✅ 通过   → 可发起转正（跳 §2.4）
任一为 false                                      →  ❌ 未通过 → 显示差哪一环节 / 差多少分
任一环节未出分（待发布 / 待评分）                  →  ⏳ 待定   → 不可转正，不给结论
```

> ⚠️ 因为**部门直接终审**没有二次复核，判定必须严格：**任一环节未完成一律「待定」而不是「通过」**。
> 综合分 = `理论 × 40% + 实操 × 60%`（默认权重，`exam_rule_snapshot` 未接通，暂硬算并标「示例」）。
> 统计口径：只统计正式考核（模拟不计入）；只算本部门，跨部门不可见。

---

### 2.10 任务与通知 > 任务管理（`/…/messages/tasks`）★ 新

**上方发布区 + 下方跟踪表**（管理端最高频的"发完要看结果"模式）

| 区块 | 内容 |
|---|---|
| ① 发布任务 | 任务标题 / 任务说明（富文本）/ 附件 / **指定实习生**（本部门多选，支持按岗位·培养状态批量选）/ 截止时间 / 提交方式（在线填写·上传文件·仅确认） |
| ② 已发布任务列表 | 列：任务标题 / 发布范围 / 应完成 / 已完成 / 完成率 / 截止时间 / 状态（进行中·已截止·已归档）/ 操作 |
| ③ 完成情况下钻 | 抽屉：逐个实习生 + 完成状态 + 提交内容/附件 + 提交时间；可「催办」 |
| ④ 批量动作 | 批量催办 / 批量归档 / 删除 |

> 后端缺口：`task_assignment` / `task_post` / `task_submission` 三表 0 行、**零 Java 层**。需要新增整套 CRUD（预计 1 控制器 + 1 service）。

### 2.11 任务与通知 > 通知管理（`/…/messages/notices`）★ 新

| 区块 | 内容 |
|---|---|
| ① 发布通知 | 通知标题 / 正文 / 类型（注册审核·考试提醒·评分完成·成绩发布·转正审批·证书生成·公告）/ **接收人**（指定实习生 / 按岗位 / 按培养状态 / 本部门全员）/ 附跳转（关联业务对象） |
| ② 已发通知列表 | 列：标题 / 类型 / 接收人数 / 已读 / 未读 / 发送时间 / 操作（撤回·删除） |
| ③ 阅读情况下钻 | 抽屉：谁已读、谁未读 |

> 后端缺口：`notification` 表 0 行、**零 Java 层**（11 列已含 `notify_type` / `biz_type` / `biz_id` / `read_status` / `expire_time`，结构够用）。
> **联动**：实习生端顶栏铃铛（本次已实现）直接读 `unread-count`，点开进 `/messages` 消息中心。

---

## 三、两角色联动映射表（管理端配置 → 实习生端呈现）

这张表是本方案的**验收基准**：管理端每一处配置，都能在实习生端找到唯一落点。

| # | 管理端动作 | 落库 | 实习生端呈现位置 | 现状 |
|---|---|---|---|---|
| 1 | 人员管理 > 注册审核 通过 + 分配导师 | `sys_user.user_status` / `mentor_name` | 登录后进入工作台；工作台 ① 身份条显示导师 | ✅ 已通 |
| 2 | 题库管理 > **模块库** 维护题目 | `question_bank`（**新增列 `knowledge_module`**）/ `question` | 「模拟考核」从**综合模拟库**随机抽 10 题（**不按模块** —— 受 `selectPracticeBankByDept ... LIMIT 1` 约束）；「备考资料」可挂模拟题入口 | ⚠️ 拆多库后**必须保留 1 条综合库**，否则模拟考核坏 |
| 3 | 模拟备考管理 > 备考资料 发布 | `material` | **「正式考核」页 ① 备考资料卡片**（预览 / 下载） | ⚠️ 待接口 |
| 4 | 模拟备考管理 > 模拟理论考核配置 | （待配置化） | 「模拟考核」抽题数量与配比 | ⚠️ 待接口；**若要"按模块练习"需给 `/business/practice/start` 加可选 `bankId`** |
| 5 | 模拟备考管理 > 实操题库 发布 | `practice_subject` | 「模拟考核」页签内的实操练习列表（`/published`） | ✅ 已通 |
| 6 | 正式考核管理 > 考试说明（文字/文档） | `exam` | **「正式考核」页顶部说明条 + 附件** | ⚠️ 部分 |
| 7 | 正式考核管理 > **理论考试** 配置 + **单独发布** | `exam`（`bank_id`/`single_count`/`multi_count`/`judge_count`/`single_score`/`pass_line`/`start_time`/`end_time`） | **「正式考核」页 ③ 理论考试卡**：点击进入答题（`/start/{examId}`） | ✅ 已通（题型数量与分值**现有后端已支持**，无需新增） |
| 8 | 正式考核管理 > **实操考试** 配置 + **单独发布** | `exam.subject_content` / `subject_attachment` | **「正式考核」页 ④ 实操考试卡**：上传成果；理论未发布时显示「待发布」 | ✅ 已通 |
| 9 | 正式考核管理 > **批次 + 分环节发布**（理论 / 实操各自独立发布）★ 分环节发布新增 | `exam.stage` ★ **需幂等加列**（`exam.period_code` 可选作批次键） | 理论 / 实操**两卡各自独立状态**；本环节未发布时显示「待发布」 | ⚠️ **需幂等加列**（不新建表） |
| 10 | 正式考核管理 > 规则开关（完成率门槛/协议/补考） | `exam_rule_snapshot` | 未达门槛时「正式考核」页顶部门槛提示条 + 入口置灰 | ⚠️ **后端未实现**（表零 Java 层） |
| 11 | 成绩管理 > 批阅 | `answer_sheet_item.*` | 「正式考核成绩」页逐题明细 | ✅ 已通 |
| 12 | 成绩管理 > 发布成绩 | `answer_sheet.status='PUBLISHED'` | 「正式考核成绩」页出现该**节**成绩 | ✅ 已通 |
| 13 | 成绩管理 > 成绩统计 | `assessment_result`(建议) | 「考核成绩与转正申请」页的总览 / 部门平均分 / 薄弱模块；**口径按批次**（两环节都及格才算通过） | ⚠️ 待接口 |
| 14 | 人员管理 > 实习转正审核 **直接通过** | `promotion_application`（跳过 `SUPER_PENDING`）/ `certificate` / `sys_user.user_status` | 工作台 ⑧ 培养进度走到"已发证"；**顶栏铃铛收到通知**；工作台底部证书标签**点亮** | ⚠️ **后端未实现**（零 Java 层，整套新增） |
| 15 | 任务与通知 > 发布任务 | `task_assignment` 等 | **顶栏铃铛红点** + 工作台「当前任务」列表 + 任务详情页 | ⚠️ **后端未实现** |
| 16 | 任务与通知 > 发布通知 | `notification` | **顶栏铃铛红点** + `/messages` 消息中心 | ⚠️ **后端未实现** |
| 17 | 课程管理 发布课程 | `course` / `course_chapter` / `study_item` | 「在线学习」课程目录 + 课程学习页 | ✅ 已通 |

**图例**：✅ 已通（零改动即可用）｜⚠️ 待接口/需加列（前端先示例数据打标）｜⚠️ 后端未实现（需补 Java 层）

**统计（按三个决策修订后）**：✅ 已通 **7 组**｜⚠️ 待接口/需加列 **6 组**（含新增的第 9 行 批次+分环节发布）｜⚠️ 后端未实现 **4 组**。共 17 组。

---

## 四、实习生端需同步的调整（联动反哺）

部门管理员端补齐后，实习生端有 **5 处**要跟着动。按"必须先做 / 可后做"排序（第 5 项因「分环节发布」决策而新增）：

| # | 实习生端位置 | 调整 | 为什么必须联动 |
|---|---|---|---|
| 1 | **顶栏铃铛 → `/messages` 消息中心** | 把 `/messages` 从"骨架假数据页（`assessment/index.vue`）"改造成**真实消息中心**：未读/已读列表、按类型筛选、点击跳转关联业务 | 管理端 #14/#15 发出去的通知和任务必须有人收。这是**主动线**，优先级最高 |
| 2 | **工作台「当前任务」区块** | 目前是假数据，改为读真实任务：待办任务卡（标题 / 截止时间 / 状态 / 去完成） | 管理端 #14 的落点 |
| 3 | **工作台底部：保密协议 / 电子证书标签** | 证书标签在 `promotion_application` 通过后由灰变亮、可点击预览（`certificate.template_url`） | 管理端 #13 的落点（**注意：现在是部门直接终审就发证，所以证书会比原设计更早出现**） |
| 4 | **「正式考核」页顶部** | 补三样：① 考试说明（文字/附件）② **资格门槛提示条**（"学习完成率需达 70%，当前还差 8%"）③ 各环节的独立状态 | 管理端 #6/#9 的落点 |
| 5 | **「正式考核」页状态机 ★ 需补「环节未发布」态** | 原设计只有「未开始 / 进行中 / 已结束」三态。分环节发布后两环节各自有状态，需补一个「另一环节待发布」：<br/>① 均未开放<br/>② **理论进行中**（实操入口显示「待发布」）<br/>③ **实操进行中**（理论已出分）<br/>④ 全部结束（成绩已公布） | **这是「分环节发布」决策的主要成本**。缺了这个态，实习生会看到"整场已结束但实操还没考"，无法理解 |

> ⚠️ 第 5 项是本次新增的最大工作量。好消息：`formalInternBlockedPaths` 已经是**路径级**拦截清单（`utils/internTabs.js` 是页签单一数据源），加状态不需要动侧栏或路由，只动「正式考核」页自身。

**保持不变**（已与后端联动，本次一律不动）：在线学习列表与课程学习页、模拟考核答题与回顾、正式考核答题、考核成绩与转正申请页的成绩区块、工作台的学习数据区块。

---

## 五、后端支撑现状与缺口

| 模块 | 后端现状 | 缺口 | 优先级 |
|---|---|---|---|
| 工作台 | 注册/批阅/考核/审计可查 | 完成率均值、正确率均值、**最薄弱知识点 Top5**、**待转正数** | P1 |
| 注册审核 | ✅ 完整 | 无 | — |
| 实习生管理 | 花名册可查、协议可查、学习/模拟/正式成绩**逐人可查** | 批量聚合（比率类） | P1 |
| 个人全景档案 | 各段数据都可查 | 组装成本高，建议 1 个聚合接口 | P1 |
| 实习转正审核 | ❌ `promotion_application` / `certificate` 零 Java 层 | 整套：申请提交、审核、发证。**部门直接终审** → 状态机跳过 `SUPER_PENDING`，比原设计**少一个环节**，工作量略减，但**必须补纠错路径**（`operate_log` + 超管撤回） | **P0** |
| 课程管理 | ✅ 完整 | 无 | — |
| 题库管理 | ✅ CRUD/导入/模板完整 | **拆多库新增**：`question_bank` 加 `knowledge_module` 列 + `bank_type` 增 `MODULE` 取值；**必须保留 1 条 PRACTICE 综合库**（见 §八.2） | **P0**（不加列则页面做不出来） |
| 模拟备考 · 备考资料 | ❌ `material` 零 Java 层 | 只读 + 上传接口 | P1 |
| 模拟备考 · 模拟理论配置 | ❌ 抽题数写死 10、题型不可配 | 配置化 + 落库；按模块练习需给 `practice/start` 加可选 `bankId` | P1 |
| 模拟备考 · 实操题库 | ✅ 完整 | 无 | — |
| 正式考核管理 | ✅ 说明/理论/实操配置与发布**基本完整**（`exam` 已有 `bank_id`/`single_count`/`multi_count`/`judge_count`/`single_score`/`pass_line`/`start_time`/`end_time`/`subject_content`） | **分环节发布新增**：`exam` 加 `period_code` + `stage` 两列；考试说明字段（文字+附件） | **P0**（加列）｜P1（说明字段） |
| **正式考核跨模块出卷** | ❌ 三条抽题 SQL 全是单库（`bank_id = ?`） | 新增**组卷**：写入 `exam_paper_question`（表已建、零 Java 层） | **P0**（拆多库的前置依赖） |
| **正式考核资格校验** | ❌ `exam_rule_snapshot` 零引用 | 读快照做门槛校验（**开关式，无快照则不拦 → 零回归**） | **P0** |
| 成绩管理 · 批阅 | ✅ 完整 | 无 | — |
| 成绩管理 · 统计 | ❌ `assessment_result` 从未写入 | 聚合接口（先只读聚合，不写表） | P1 |
| **薄弱知识点分析** | ❌ 明细未快照 `knowledge_point` | 明细表加 1 列快照 + 聚合接口 | **P0** |
| 任务与通知 | ❌ 4 张表零 Java 层 | 整套：任务 CRUD + 提交 + 通知 + 未读数 | **P0** |

> 详细解决办法（含 SQL / 代码草稿、风险评估、落地批次）见 `后端实现现状盘点与考核补全方案.md`。

---

## 六、实施顺序建议（按三个决策修订）

| 阶段 | 内容 | 后端依赖 | 前端工作量 |
|---|---|---|---|
| **A0. 建表/加列（先做，阻塞后续）** | ① `question_bank` 加 `knowledge_module`；② `exam` 加 `period_code` + `stage`。两个幂等迁移脚本 | 无（纯 DDL，见 §八.1/§八.2） | 无 |
| **A1. 拆库数据迁移** | 145 题按 `knowledge_point` 归入模块库；保留 1 条综合模拟库 | 无（纯 SQL，见 §八.3） | 无 |
| **B. 骨架** | 新 4 目录 + 10 个二级菜单（移动 + 新建），全部指向占位页；旧目录隐藏 | 无 | 小（1 个迁移脚本 + 1 个路由文件） |
| **C. 能真数据的先真** | 工作台（② 待办、④ 漏斗、⑥ 考核概览、⑧ 动态）、实习生管理表格、成绩统计的人员列表 | 无（全部现有接口） | 中 |
| **D. 沿用现有页面的 3 处** | 课程管理 / **题库管理（模块库列表）** / 实操题库 / 考核批阅 —— **只动 template+style** | 无 | 中 |
| **E. 新页面 + 示例数据打标** | 备考资料、模拟理论配置、正式考核管理（**一页两卡：理论 + 实操**）、转正审核（**直接终审版**）、任务与通知 | 后端未就绪，先示例数据 | 大 |
| **F. 后端补接口后替换** | P0 顺序：**组卷**（拆多库前置）→ 转正审核 → `exam_rule_snapshot` 接通 → 成绩聚合/批阅列表 → 备考资料/任务通知 | 后端 | 小/项 |
| **G. 实习生端 5 处联动** | 消息中心（主动线）、工作台任务区、证书标签、正式考核页顶部、**正式考核页「环节未发布」态**（分环节发布的主要成本） | 部分依赖 F | 中 |
| **H. 超管端同步** | 超管端「业务管理」扩成同构 10 项；**并删除转正终审环节**（因已归部门） | 无 | 中 |

> ⚠️ **顺序上的两个硬依赖**：
> ① **A0 → A1 → D/E 必须在前**：不加列、不拆库，题库管理页与正式考核页就没有可渲染的数据结构。
> ② **`exam_paper_question` 组卷（F 的第一项）是拆多库的完成前提**：只拆库不组卷，正式考核会退化成"只能考单个模块"。

---

## 七、三个决策的落地实现方案（拍板后必读）

三个决策里，**决策 1 最省事**（整套表零 Java 层，全是新增）、**决策 2 有中等成本**（加 2 列 + 实习生端多一个状态）、**决策 3 最需要动手**（加列 + 数据迁移 + 必须补组卷 + 题量前置）。按难度排序展开。

---

### 7.1 决策 3 · 拆多库：3 处改动 + 2 个前置条件

#### 改动 ① — `question_bank` 加模块列（幂等脚本，可重复执行）

```sql
-- sql/intern_assessment_bank_module.sql
SET @c := (SELECT COUNT(*) FROM information_schema.COLUMNS
           WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'question_bank'
             AND COLUMN_NAME = 'knowledge_module');
SET @s := IF(@c = 0,
  'ALTER TABLE `question_bank`
     ADD COLUMN `knowledge_module` VARCHAR(64) DEFAULT NULL
     COMMENT ''知识点模块名（拆多库后每库对应一个模块；PRACTICE综合库为NULL）'' AFTER `bank_name`',
  'SELECT 1');
PREPARE st FROM @s; EXECUTE st; DEALLOCATE PREPARE st;
```

`bank_type` **不新增枚举值也不需要 DDL**（是自由 `VARCHAR(24)`，现有取值 `FORMAL` / `PRACTICE`），新增取值 **`MODULE`** 直接可用。
但**必须检查有没有别处按 `bank_type IN ('FORMAL','PRACTICE')` 硬编码**——加新取值可能让模块库在那些查询里"凭空消失"。已核查：现有代码只按**单个**取值过滤（`= 'PRACTICE'` / `= 'FORMAL'`），**没有 `IN` 硬编码**，所以新增 `MODULE` 是安全的。

#### 改动 ② — 保住模拟考核：**保留且仅保留 1 条 `PRACTICE` 库**

这是拆多库**唯一会导致线上功能损坏**的点。原则：

| 库类型 | 数量 | 用途 | 拆库时怎么办 |
|---|---|---|---|
| `PRACTICE` | **每部门恰好 1 条** | 「模拟考核」抽题入口（`selectPracticeBankByDept` 的唯一来源） | **整条保留不动**，题目也不动 |
| `FORMAL` | 1 条 | 原「正式题库」 | 把它的题目**搬空**到模块库（见 7.2） |
| `MODULE` | 每模块 1 条 | 按知识点组织；正式考核**组卷**的题源 | **新建** |

> 这样拆库对模拟考核是**零影响**的：抽题仍从那条综合模拟库随机抽 10 题，行为与今天完全一致。

**上线前自检（期望返回 0 行，非 0 就是拆坏了）**：

```sql
SELECT dept_id, COUNT(*) AS c
FROM question_bank
WHERE deleted = 0 AND bank_type = 'PRACTICE'
GROUP BY dept_id
HAVING c <> 1;
```

#### 改动 ③ — 跨模块出卷：写 `exam_paper_question`（拆多库的**完成前提**）

三条抽题 SQL 全是单库，`exam.bank_id` 也是单个字段。**只拆库不组卷，正式考核会退化成"只能考一个模块"**。
所以必须新增组卷能力 —— 好在表已经建好了（`exam_paper_question`：`exam_id / rule_id / question_id / seq / qtype / stem / options_json / answer / score / is_subjective`），落库时**题干、选项、答案、分值全部快照**。

组卷流程：

```
管理员选 N 个模块库 + 每模块抽几题
   ↓ 后端：每个模块库按 RAND() 抽指定数量
   ↓ 汇总 → 按题型配比校验（single/multi/judge 数量对齐 exam 的 three count）
   ↓ 快照写入 exam_paper_question（seq 排序）
考生答题时：直接读 exam_paper_question，不再 RAND()
```

**副带收益**：现在 `PracticeServiceImpl` / `AnswerSheetServiceImpl` 每次 `ORDER BY RAND()` 抽题、**不存卷**，导致成绩复盘时题面无法还原。组卷快照一次性解决这个问题。

#### 前置条件 ① — 题量必须补到「每模块 ≥ 15 题」

不满足这个，拆库后**卷面抽不满**。自检脚本：

```sql
SELECT b.dept_id, b.knowledge_module, COUNT(q.id) AS n
FROM question_bank b
LEFT JOIN question q ON q.bank_id = b.id AND q.deleted = 0 AND q.status = 1
WHERE b.deleted = 0 AND b.bank_type = 'MODULE'
GROUP BY b.dept_id, b.knowledge_module
HAVING n < 15
ORDER BY n;
```

现状（实测）：开发 104 的 24 题散在 14 个模块 → 平均 **1.7 题/模块**；设计 105 的 48 题散在 17 个模块 → **2.8 题/模块**；交付/质检/建模 **0 题**。
**17 模块 × 15 题 ≈ 255 题/部门** —— 这是本次工程量最大的一项，且**属于业务出题工作，不能靠改代码解决**。请先确认谁负责补题、什么时候到位。

#### 前置条件 ② — 清理脏值

`question.knowledge_point = '7'` 这 1 条会在拆库时变成一个名为「7」的模块库。先归一：

```sql
UPDATE question SET knowledge_point = '未分类'
WHERE deleted = 0 AND (knowledge_point IS NULL OR knowledge_point = '' OR knowledge_point = '7');
```

---

### 7.2 拆库数据迁移脚本（不做这步 = 没拆）

```sql
-- sql/intern_assessment_bank_split.sql
-- 前置：已执行 7.1 改动①（加 knowledge_module 列）、前置条件②（清脏值）
-- 特性：幂等，可重复执行；只搬 FORMAL 库的题，PRACTICE 库整条不动

-- ① 推导「部门 × 模块」组合（源：正式库的题目知识点）
DROP TEMPORARY TABLE IF EXISTS tmp_bank_split;
CREATE TEMPORARY TABLE tmp_bank_split (
  dept_id BIGINT NOT NULL,
  knowledge_module VARCHAR(64) NOT NULL,
  PRIMARY KEY (dept_id, knowledge_module)
) ENGINE = InnoDB;

INSERT IGNORE INTO tmp_bank_split (dept_id, knowledge_module)
SELECT b.dept_id, q.knowledge_point
FROM question q
JOIN question_bank b ON b.id = q.bank_id
WHERE q.deleted = 0 AND b.deleted = 0 AND b.bank_type = 'FORMAL'
  AND q.knowledge_point IS NOT NULL AND q.knowledge_point <> '';

-- ② 建模块库（不存在才建 —— 用派生表包一层绕开 MySQL 自引用限制）
INSERT INTO question_bank
  (bank_name, bank_type, knowledge_module, dept_id, description, status, question_count)
SELECT CONCAT(d.dept_name, '·', t.knowledge_module, '模块库'),
       'MODULE', t.knowledge_module, t.dept_id,
       CONCAT('由原正式题库按知识点拆分；模块：', t.knowledge_module),
       'ENABLED', 0
FROM tmp_bank_split t
JOIN sys_dept d ON d.dept_id = t.dept_id
LEFT JOIN (
    SELECT dept_id, knowledge_module FROM question_bank
    WHERE deleted = 0 AND bank_type = 'MODULE'
) ex ON ex.dept_id = t.dept_id AND ex.knowledge_module = t.knowledge_module
WHERE ex.dept_id IS NULL;

-- ③ 把正式库的题目搬进对应模块库
UPDATE question q
JOIN question_bank src ON src.id = q.bank_id
                      AND src.bank_type = 'FORMAL' AND src.deleted = 0
JOIN question_bank dst ON dst.dept_id = src.dept_id
                      AND dst.knowledge_module = q.knowledge_point
                      AND dst.bank_type = 'MODULE' AND dst.deleted = 0
SET q.bank_id = dst.id
WHERE q.deleted = 0;

-- ④ 重算 question_count（顺带修好已知的冗余字段不同步问题）
UPDATE question_bank b
LEFT JOIN (
    SELECT bank_id, COUNT(*) AS c FROM question WHERE deleted = 0 GROUP BY bank_id
) t ON t.bank_id = b.id
SET b.question_count = IFNULL(t.c, 0)
WHERE b.deleted = 0;

-- ⑤ 校验：模块库列表 + 题量
SELECT b.dept_id, b.knowledge_module, b.question_count
FROM question_bank b
WHERE b.deleted = 0 AND b.bank_type = 'MODULE'
ORDER BY b.dept_id, b.question_count DESC;
```

**迁移后预期形态（以开发 104 为例）**：

| 库 | 类型 | 题量 |
|---|---|---|
| 开发部门模拟题库（bank 2） | `PRACTICE` | 24（**含原 24 题，不动**）|
| 开发部门正式题库（bank 3） | `FORMAL` | **0（已成空壳，保留或删除均可）** |
| 开发·DIX脚本模块库 | `MODULE` | 4 |
| 开发·nginx / Docker / 平台模块库 | `MODULE` | 各 3 |
| 开发·孪生体导入 / SpringBoot 模块库 | `MODULE` | 各 2 |
| 开发·森大屏 / MySQL / 承重规范 / ECharts / Java / Redis / 答题规则 模块库 | `MODULE` | 各 1 |

> ⚠️ 看到「各 1」就知道为什么必须补题了。**这个迁移脚本跑完，正式考核在题量补齐前是无法出卷的。**

> 📌 **关于空壳 `FORMAL` 库**：建议**保留为空壳**（不删），因为 ① `exam.bank_id` 可能已有历史值指向它；② 有些查询按 `bank_type='FORMAL'` 找题库，删了会改变行为。若要删，先 `SELECT COUNT(*) FROM exam WHERE bank_id = 3` 确认无引用。

---

### 7.3 决策 2 · 分环节发布：加 1 列（`stage`）+ 批次键（可选）

```sql
-- sql/intern_assessment_exam_period.sql
-- ① 批次分组键（可选：页面顶部下拉用，不做期次列表）
SET @c := (SELECT COUNT(*) FROM information_schema.COLUMNS
           WHERE TABLE_SCHEMA=DATABASE() AND TABLE_NAME='exam' AND COLUMN_NAME='period_code');
SET @s := IF(@c=0,
  'ALTER TABLE `exam` ADD COLUMN `period_code` VARCHAR(32) DEFAULT NULL
     COMMENT ''批次分组键（可选）：同批次的 理论/实操 两场考试共用同一值'' AFTER `exam_mode`',
  'SELECT 1');
PREPARE st FROM @s; EXECUTE st; DEALLOCATE PREPARE st;

-- ② 环节标识
SET @c := (SELECT COUNT(*) FROM information_schema.COLUMNS
           WHERE TABLE_SCHEMA=DATABASE() AND TABLE_NAME='exam' AND COLUMN_NAME='stage');
SET @s := IF(@c=0,
  'ALTER TABLE `exam` ADD COLUMN `stage` VARCHAR(16) DEFAULT NULL
     COMMENT ''环节：THEORY理论 / PRACTICE实操'' AFTER `period_code`',
  'SELECT 1');
PREPARE st FROM @s; EXECUTE st; DEALLOCATE PREPARE st;

-- ③ 便于按批次查
SET @c := (SELECT COUNT(*) FROM information_schema.STATISTICS
           WHERE TABLE_SCHEMA=DATABASE() AND TABLE_NAME='exam' AND INDEX_NAME='idx_exam_period');
SET @s := IF(@c=0, 'ALTER TABLE `exam` ADD KEY `idx_exam_period` (`period_code`,`stage`)', 'SELECT 1');
PREPARE st FROM @s; EXECUTE st; DEALLOCATE PREPARE st;
```

**不需要新增「环节未发布」字段**：各环节状态直接由该 `exam` 行的 `status` + `start_time/end_time` **派生**（见 §2.8）。派生规则集中在前端一个 `computed` 里，避免两处存储不一致。

**存量数据处理**：现有 `exam` 2 行测试数据（`exam_name`='m'/'l'）`period_code` 为 `NULL` → 视为"未编入批次"，前端归到「未分批」下拉项，不影响运行。

**`startExam` 改不改？** 不用改签名，但**准入判断要看 `stage`**：现有 4 项准入（PRE 角色 / 部门匹配 / `PUBLISHED` / 未参加过）已经能正确拦住"未发布的那一节"，所以**零改动可用**。这也是分环节发布成本可控的原因。

---

### 7.4 决策 1 · 部门直接终审：少一个环节，但要补三条

因为 `promotion_application` / `certificate` / `stage_evaluation` **零 Java 层**，整套都是新增，**没有"改老接口"的负担**。

| # | 改动 | 具体做法 |
|---|---|---|
| 1 | 状态机跳过 `SUPER_PENDING` | 审批动作直接 `DEPT_PENDING → PASSED`；`super_approve_by/time` 两列保留不写（留给超管代操作时留痕）。若担心误解，可在迁移脚本里 `ALTER` 更新 `status` 列注释 |
| 2 | `sys_user.user_status` 驱动方下沉 | 审批通过时由 Service 把 `PENDING_PROMOTE → FORMAL_TRAINEE`，**不再等超管**。同时 `certificate` 落证 + 回写 `promotion_application.cert_no` |
| 3 | 权限 | 为 `DEPT_ADMIN` 新增转正审批菜单 + 角色授权（走幂等菜单脚本，与 §一的菜单迁移合并做） |
| 4 | **纠错路径（强烈建议保留）** | ① 每次审批写 `operate_log`（表已建、零 Java 层）；② 超管端保留「撤回转正」入口（把 `FORMAL_TRAINEE` 退回 `PRE_TRAINEE` + 证书作废）。**理由：部门自己批自己部门的人 + 自动发证不可逆，没有任何复核环节，一旦误批只能靠超管兜底** |

> ⚠️ **这条决策改动了一条已写进需求的业务规则**：原规则是「预备→正式需通过正式考试**并经超管审批**」。
> 现在变成「部门审批即生效」。请在需求文档里同步修改，否则后续验收会与需求文档冲突。

---

### 7.5 三个决策的交叉影响（容易被漏掉的两处）

**① 超管端设计稿失效** —— 这是决策 1 的直接后果。

`超管端整体设计稿.html` 目前画的是：
- 「超管 = 部门管理员同名功能的全局版」+ 业务管理 5 项 → 现在部门端是 **4 目录 / 10 项**，超管的"超集"不成立；
- 转正审核里**画了超管终审环节** → 现在终审归部门，这个环节要删（只留"撤回/统计"）。

**建议超管端同步为**：4 个同构目录 + 1 个系统管理（超管独有），转正审核项降级为「查看 + 撤回转正」。

**② 业务规则文档要改两处**：
- 「预备→正式需经超管审批」→ 改为「部门审批即生效」
- 「一场考试 = 一次考核」→ 改为「一个批次（含理论/实操两环节）= 一次完整考核」

**③ 与"已实现接口一律不改"的边界确认** —— 三个决策**都没有触碰**现有接口的签名/返回结构/权限注解：

| 动作 | 是否改现有接口 |
|---|---|
| `question_bank` 加列、`exam` 加列 | ❌ 不改（纯 DDL，MyBatis-Plus 实体加字段即可） |
| 组卷写 `exam_paper_question` | ❌ 不改（全部新增） |
| 转正审核整套 | ❌ 不改（原表零 Java 层） |
| 保留 1 条 PRACTICE 库 | ❌ 不改（正是为了**保护**现有行为） |
| **唯一例外**：若要让「模拟考核也按模块练习」 | ✅ **需要**给 `/business/practice/start` 加**可选** `bankId`（不传走原逻辑，向后兼容）—— **这一步需你单独确认** |

---

## 八、下一步（等你一句话即可推进）

按依赖顺序，我可以立刻做的是**不依赖你补题的两件事**：

1. **出 3 个幂等迁移脚本**（可直接执行、可重复执行）：
   - `intern_assessment_bank_module.sql`（加 `knowledge_module` 列）
   - `intern_assessment_bank_split.sql`（拆库 + 搬题 + 重算题量）
   - `intern_assessment_exam_period.sql`（加 `period_code` + `stage` + 索引）
2. **出部门管理员端的菜单迁移脚本**：把现有 6 个业务菜单**移动**到新 4 目录下（复用 ID，保住 DEPT_ADMIN 已有授权），并补转正审批菜单。

**需要你先定的一件事**：拆库后「模拟考核」是否也要按模块练习？
- 若**要** → 我给 `/business/practice/start` 加可选 `bankId`（向后兼容），实习生端模拟考核页加"选模块"步骤；
- 若**不要** → 严格零后端改动，模拟考核继续从综合库抽 10 题。

**需要你安排的一件事**：17 个模块 × 约 15 题 ≈ **每部门 255 题**的补题工作（交付/质检/建模三部门从 0 开始）。这是拆多库能不能真正跑起来的前提。

---

## 九、前端落地状态（2026-09-17 已实现，与本文对照）

> 本章记录「设计稿 → 真实页面」的落地结果，供验收时逐条对账。
> 原则：**能接真实接口的区块一律接真实接口；没有聚合接口的区块用设计稿示例值并打「示例」标记**，不出现空壳。

### 9.1 侧栏与路由

| 项 | 做法 | 位置 |
|---|---|---|
| 四目录侧栏 | **前端硬编码**，不读 `sys_menu` | `store/modules/permission.js` → `buildDeptAdminSidebar()` |
| 真实路由 | 扁平挂在 `/department` 下，`hidden:true` | `router/index.js` dynamicRoutes |
| 超管 | **不套用本侧栏**，继续走后端菜单（保留系统管理） | `GenerateRoutes` 里的 `!roles.includes('SUPER_ADMIN')` |

**为什么不做 `sys_menu` 迁移**（对应 §一「与现有 sys_menu 的迁移对照」）：
`sys_menu` 里部门管理员仍是老的「部门运营 / 考核认证管理」两目录，且 14 个页面指向公共骨架页 `assessment/index`；
菜单表属「共同登记区」（同事分支也在动），本轮先用前端侧栏把设计稿落地，**不需要跑任何 SQL**。
后续若要转回菜单表，按 `router/index.js` 里 `/department` 那段逐条建菜单行即可（path / component / meta 完全对应）。

侧栏分组的拼法：组内子节点 path 只取**末段**（`people/students` → `students`），使「组路径 + 末段」正好等于真实路由。

### 9.2 页面清单（共 8 个新页面 + 4 个复用）

| 目录 | 页面 | 文件 | 数据来源 |
|---|---|---|---|
| 工作台 | 工作台 | `views/department/dashboard/index.vue` | 待办 / 漏斗 / 考核概览 / 动态=真实；三个比率均值、学习进度分布、薄弱知识点=示例。**2026-09-17**：考核概览表新增「环节」列（理论/实操）与批次说明，呼应「同一实习生分阶段多次参加」 |
| 人员管理 | 注册审核 | 复用 `views/business/register/index.vue` | 全真实 |
| 人员管理 | 实习生管理 | `views/department/people/students.vue` | 姓名/岗位/培养状态/导师/最近活跃=真实；保密协议、两个比率、正式考核=示例 |
| 人员管理 | 实习转正审核 | `views/department/people/promotion.vue` | 演示态（`promotion_application` 零 Java 层），但**交互闭环**：审批/驳回会真的移动列表状态 |
| 人员管理 | 个人全景档案 | `views/department/people/profile.vue`（hidden 下钻页） | 基础信息真实；能力画像/学习明细/考核记录=示例 |
| 学习与考核管理 | 课程管理 | 复用 `views/business/course/runtime.vue` | 全真实 |
| 学习与考核管理 | 题库管理 | 复用 `views/business/questionBank/index.vue` | 全真实 |
| 学习与考核管理 | 模拟备考管理 | `views/department/study/prep/index.vue`（3 页签） | 页签 1/2=示例；页签 3「实操题库」=**真实闭环**（复用 `practiceSubject/index.vue`） |
| 学习与考核管理 | 正式考核管理 | `views/business/exam/index.vue` | **全真实 + 按稿改版（2026-09-17）**：批次列表（同名考核=同一批次的理论/实操两环节）+ 一页两卡分环节发布（编辑/保存发布/停用/启用/删除）+ 试题分布（示例占比）与实操题干直看；新增表单字段「考核时长 / 时间窗」已由后端 `exam` 表与 Mapper 支持 |
| 学习与考核管理 | 成绩管理 | `views/department/study/scores/index.vue`（2 页签） | **全真实**：`exam/list` + `answer-sheet/grading` + `detail` + `grade` + `publish` |
| 任务与通知 | 任务管理 | `views/department/messages/tasks.vue` | 演示态（`learning_task` 等 6 表零 Java 层），发布/催办本地闭环 |
| 任务与通知 | 通知管理 | `views/department/messages/notices.vue` | 演示态（`notification`/`notice` 零 Java 层），发送/重发本地闭环 |

### 9.3 视觉基线：`department-module.scss`

新增 `assets/styles/department-module.scss`（命名前缀 `d`，与实习生端 `practice-module.scss` 对齐）。
**关键坑（已在文件头注明）**：本文件被各页 `@import` 到 `<style lang="scss" scoped>`，**因此不能出现 `:root` 选择器** ——
scoped 会把 `:root` 编译成 `:root[data-v-xxx]`（永远匹配不到），导致所有 `var(--x)` 失效、整页无色。
所以设计令牌一律用 **SCSS 变量**（`$blue:#1764f5` …）而不是 CSS 自定义属性。

### 9.4 已验证结果（2026-09-17）

| 验证项 | 手段 | 结果 |
|---|---|---|
| 8 个新页面三段语法（template / script / SCSS） | `.codex-tmp/check_sfc.js` | 8/8 通过 |
| 真实 webpack 编译 | `vue-cli-service build` | `DONE Build complete` |
| dev server(9530) 是否已出包 | `curl /static/js/app.js` grep 页面标识串 | 14 个标识串全部命中（包体 +385KB） |
| 令牌是否生效 | 构建产物 CSS 中查 `.dept-page[data-v-*]{background:#eef2f7}` | 命中（证明 SCSS 变量已解析） |

### 9.8 课程资料只保留「文档 / 视频」，去掉章节检测（2026-09-17 第七轮）

- **资料类型**：`business/course/index.vue` 去掉「章节测试（QUIZ）」单选项与「提交测试答案」完成方式，去掉种子数据里的「章节自测」项、QUIZ 类型标签与「题目配置」提示块；发布校验改为「每项必须绑定文件」。
- **后端**：`CourseContentServiceImpl.ITEM_TYPES` 收窄为 `DOC/VIDEO`（新增 QUIZ 直接报「学习资料类型不合法」）；`validateQuizJson` / `normalizeQuizJson` 移除；`LearningServiceImpl` 去掉 QUIZ 完成分支。
- **数据**：历史 QUIZ 单项已软删（脚本 `ruoyi-vue/sql/intern_assessment_course_drop_quiz.sql`，幂等）；`item_count` / `avg_completion_rate` / `countUnboundAssets` 均过滤 `deleted=0`，故课程单项数与完成率自动剔除测试项（开发类课程 4 项 → 3 项）。
- **学习页**：文档可在线阅读（PDF 内联 / Word: docx-preview / PPT: pptx-preview）**也可随时下载**；下载走 `/common/download/resource`，不再把「下载」当作完成。

### 9.7 部门管理员的登录落点改为部门工作台（2026-09-17 第五轮）

**问题**：部门管理员登录后仍落在通用工作台 `/index`（`views/index.vue` 的部门分支），而 d2 设计的部门工作台 `/department/dashboard` 需要再点侧栏才能进入。

**做法（前端路由守卫，零 SQL、零后端改动）**：

| 文件 | 改动 |
|---|---|
| `src/permission.js` | 新增 `isDeptAdminOnly()`（`DEPT_ADMIN` 且**不含** `SUPER_ADMIN`/`admin`）与 `DEPT_DASHBOARD_PATH='/department/dashboard'`；`protectedTarget()` 里把指向 `/index`（或 `/`）的访问改投部门工作台 |
| `src/components/Breadcrumb/index.vue` | `/department/**` 路由的面包屑首项改为「工作台 → /department/dashboard」；`DeptDashboard` 视为根（不再前置首页） |
| `src/layout/components/Navbar.vue` | 登出后跳 `/` 而非 `/index`，由守卫按角色决定落点 |

**行为矩阵**：部门管理员（`test_dev_admin` 等）→ `/department/dashboard`；超管（`admin` / `test_SUPER_ADMIN`）→ 继续 `/index`（含系统管理）；实习生 → 继续 `/index`（i2 工作台），协议/正式实习生拦截逻辑不变。
**遗留**：`views/index.vue` 里的部门管理员分支（`isDeptAdmin` 系列文案）已无入口，代码保留未删（超管仍走同页的全局分支），如需彻底清理可单独排一轮。

### 9.6 理论考试配置化：知识分布 + 发布时间 + 指定人员（2026-09-17 第四轮）

参考「模型生产协同系统」原型里超级管理员**理论考试配置**板块的结构（考试信息 6 项 + 知识分布 = 知识点 × 占比），落到本系统部门管理员的**模拟 + 正式**理论考试配置，并把闭环打通：

| 页面 | 文件 | 新增能力 |
|---|---|---|
| 学习与考核管理 › 模拟备考管理 › 模拟理论考核配置 | `views/department/study/prep/index.vue` | 题源题库自动识别（`bank_type=PRACTICE`）、题型配比与每题分值、**知识分布表**（「从题库导入知识点」按题库 `question.knowledge_point` 聚合铺满，「抽题数量 ≤ 题库可用」实时校验）、**试抽一套**（真实调后端抽题）、**保存并生效**（无配置则先建 `exam_mode=PRACTICE + exam_type=THEORY` 再保存知识分布并发布） |
| 学习与考核管理 › 正式考核管理 | `views/business/exam/index.vue` | 理论环节卡内**知识分布编辑**（导入/校验/试抽/保存配置）+ 新增**发布设置**卡：发布时间（立即 / 定时 + 起止时间窗）、指定人员（全部在培 / 指定名单多选，取自本部门花名册）；发布时把知识分布、时间窗、名单一并写入 |

**后端新增（均可继续沿用，零破坏性改动）**：

| 项 | 内容 |
|---|---|
| 迁移脚本 | `ruoyi-vue/sql/intern_assessment_exam_config_migration.sql`（幂等）：新增 `exam_knowledge_rule`（知识分布，驱动抽题）、`exam_participant`（指定人员，空 = 全部在培） |
| 新接口 | `GET /business/exam/bank/{bankId}/knowledge-points`（知识点+题量）、`POST /business/exam/bank/{bankId}/try-draw`（试抽，不落库）、`PUT/GET /business/exam/config/{id}`（保存 / 读取知识分布+名单+时间窗）；`PUT /business/exam/publish/{id}` 升级为可带 body |
| 抽题改造 | `PracticeServiceImpl.startPractice` 优先读本部门已发布的模拟理论配置：按知识点**按全局题型比例分摊**抽题（同知识点内随机取题），题型配额不足时同知识点优先换题型、再从题库补齐；`submitPractice` 按配置的分值判分并返回 `score / fullScore / passLine / passed`。无配置时回退原「随机 10 题、每题 1 分」 |
| 正式考核校验 | `startExam` 增加**时间窗校验**（未开放/已截止拦截）与**指定人员校验**；`myExamList` 按名单过滤并返回 `startTime / endTime / publishedAt / assigned` |
| 顺带修复 | `ExamMapper.updateExam` 原先**缺 `start_time / end_time` 的 SET 子句**（导致时间窗存不进库）→ 已补；`application.yml` 补 `spring.jackson.date-format: yyyy-MM-dd HH:mm:ss`（与前端 `el-date-picker` 的 `value-format` 对齐，否则发布时间无法反序列化） |

**实测结果（2026-09-17）**：

| 验证项 | 结果 |
|---|---|
| 知识点聚合 / 试抽 / 保存 / 发布 | 全部 200；试抽 10 题、知识分布逐项命中 |
| 实习生按配置抽题 | `configured=true`，题量 10，知识点 = Java 1 · MySQL 2 · 平台 2 · Docker 2 · Redis 2 · 答题规则 1 |
| 按配置判分 | 10 题 × 1 分 → `score=5.0 / fullScore=10.0 / passLine=6.0 / passed=false` |
| 指定人员 | 名单内（user 126）可见场次；名单外（设计部门账号）不可见 |
| 时间窗 | 定时发布后回读时间窗正确；未到开放时间开始考试 → 「该考核尚未开放，开放时间 2026-12-01 09:00」 |
| 前端 | 5 个文件 `check_sfc` 通过、`build:prod` `DONE Build complete`、dev-server 命中「知识分布 / 从题库导入知识点 / 试抽一套 / 指定人员」等标识 |

> **说明（如实标注）**：知识分布按配置**精确命中**；题型配比受「该知识点题目本身的题型构成」限制，可能小幅偏差（如配置 单选4/多选3/判断3，实抽可能为 6/2/2）——已在实现注释与配置页文案中说明「知识分布是考什么，题型配比为组卷倾向」。

### 9.5 考核模块按稿改版（2026-09-17 第二轮）

需求：**「模拟考核和正式考核页尽量还原设计稿；正式考核每个实习生可能会阶段性参加多次」**。做法与结论：

| 页面 | 文件 | 改版要点 |
|---|---|---|
| 正式考核管理（d8） | `views/business/exam/index.vue` | ① **批次列表**：按考核名称分组，同名考核 = 同一批次的理论/实操两环节，行内显示两个环节状态、已作答、待批阅，可点「配置环节」切换；② **一页两卡**：选中批次后左右两卡分别配置理论 / 实操环节，各自「保存发布 / 停用 / 启用 / 编辑 / 删除」（分环节发布）；③ 理论卡带「试题分布」表（模块占比为示例数据）；④ 实操卡直看题干与附件、跳转成绩管理；⑤ 表单补「考核时长 / 时间窗」两个真实字段 |
| 部门工作台（d2 微调） | `views/department/dashboard/index.vue` | 考核概览表新增「环节」列并加批次说明：同一实习生可在多个批次分阶段多次参加，每批次独立计分 |

**后端复用结论（重要）**：
- 「多次参加」**不需要改表** —— 一个批次 = 一条 `exam` 记录；理论 / 实操 = 同一批次下 `exam_type` 分别为 `THEORY` / `PRACTICAL` 的两条记录，各自 `status` 独立，天然支持「理论已发布 · 实操留草稿」。
- 已核对 `exam` 表真实字段：`exam_mode`、`bank_id`、`single/multi/judge_count + score`、`subject_count`、`subject_content`、`subject_attachment`、`duration`、`start_time`、`end_time`、`pass_line`、`published_at`、`publisher_id` 全部存在，`ExamMapper.xml` 的 insert/update 均已写入，**本页字段零新增**。
- 批次内补考所需字段也已存在：`exam_rule_snapshot.retake_count`（补考次数）+ `answer_sheet.retake_seq`（第几次作答）；但 `AnswerSheetServiceImpl.startExam()` 当前「已参加过该考核，不能重复参加」硬拦截，**批次内补考放行属 P1 后端待补**。
- P1 待补（页面已用示例值 + 标注，不阻塞）：① `GET /business/answer-sheet/my` 返回体补 `startTime` / `endTime` / `publishedAt`（实习生端场次时间窗真显）；② 批次标识显式化（现由考核名称解析）；③ 理论「模块占比」统计接口。

**本轮验证结果（2026-09-17 第二轮）**：

| 验证项 | 手段 | 结果 |
|---|---|---|
| 4 个改动页三段语法 | `check_sfc.js` | 4/4 通过 |
| 真实 webpack 编译 | `vue-cli-service build` | `DONE Build complete`；产物 CSS `chunk-251485a2`（`stat-strip`/`pm-qgrid`）、`chunk-a550620c`（`pub-strip`/`session-grid`）、`chunk-229f5058`（`dm-pair`/`dm-fields`/`dm-note-card`）均命中 |
| dev-server(9530) 已出包 | `curl /static/js/app.js` grep | 命中 `批次列表`、`考核场次`、`session-grid`、`stat-strip`、`pm-qgrid`、`阶段性多次参加`、`剩余考试次数` |
| 设计稿完整性 | 标签配平 + 章节 id 全集 | `<section>` 22/22、`<div>` 配平；22 个章节 id 无缺失 |

> **踩坑记录**：移走了上一轮被中断构建残留的 `dist/` 后构建才恢复正常（残留目录被遗留子进程占用，表现为「Building for production...」后长时间无输出、`dist/static/js` 被清空）。后续再遇同类卡顿：先 `mv dist dist.old` 再重跑。
| 接口权限（DEPT_ADMIN） | 取 token 直连 8080 | `register/summary`、`exam/list`、`answer-sheet/grading/2`、`practice-subject/list` 全部 200 |

实测到的真实数据形态（写页面时按此对齐）：

- `GET /business/register/summary` → **snake_case**：`{"total_count":3,"pending_count":0,"passed_count":3,"rejected_count":0}` → 页面用 `pick(obj,'totalCount','total_count')` 兼容两种。
- `GET /business/answer-sheet/grading/{id}` → 数组，每人一行；**未交卷的人没有 `sheetId`**：
  `[{"userName":"test_design_intern","sheetId":1,"aiScore":0.00,"finalScore":0.00,"passFlag":0,"sheetStatus":"PUBLISHED"},{"userName":"18888888888"}]`
  → 「待评分」= 有 `sheetId` 且 `manualScore==null` 且状态非 PUBLISHED。
- `ExamController` 复用 `business:bank:*` 权限，**DEPT_ADMIN 已具备** → 正式考核 / 成绩管理两页对部门管理员开箱可用。

### 9.5 本次相对设计稿的 3 处主动调整

1. **个人全景档案**的按钮由「提交转正审核」改为「**进入转正审核**」：本角色就是终审人，"提交"语义不成立；未达门槛时按钮禁用并显示"尚未具备转正资格"。
2. **成绩管理**只挑**真正的主观题**（`isSubjective=1`）做人工评分，不再回退到全部题目 —— 纯理论卷显示"已自动判分，无需人工录分"提示，避免管理员手工改掉自动分。
3. **综合分**不再按设计稿的「理论40%+实操60%」硬算（依赖未接通的 `exam_rule_snapshot`），改为直接展示答卷自身的 `final_score`，并在表格下方注明原因。

### 9.6 仍未做的事

- 侧栏目录项的**角标数字**（设计稿里「注册审核 2」「转正审核 3」）：融谷 侧栏无原生角标，需改 `Sidebar/Item.vue`，本轮未做。
- `d6 题库管理` / `d8 正式考核管理` 两个复用页面**未做视觉改版**（功能已闭环，改版属下一轮）。
- 实习生端页面改版（本次只做部门管理员端，按你的优先级排序）。

