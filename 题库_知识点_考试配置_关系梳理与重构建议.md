# 题库 / 知识模块 / 考试配置 —— 关系梳理与重构建议

> 2026-09-21　依据：**实测**库表结构、数据分布、后端抽题实现（非推断）
> 结论一句话：**「题库」= 一个部门的一门科目；「知识点」只是题目标签；「知识模块」这个词既非题库也非知识点，建议废弃。**

---

## 一、现状：两条正交链路 + 四组撞名

### 1.1 真实表结构

| 表 | 角色 | 关键字段 |
|---|---|---|
| `question_bank` | **知识维度·第 1 层**：题库（按部门归属） | `bank_name`、`bank_type`(**FORMAL/PRACTICE**)、`dept_id`、`question_count` |
| `question` | 知识维度·第 2 层：题目 | `bank_id`、`qtype`、`difficulty`、**`knowledge_point`**、`position_id`(可选) |
| `exam` | 考核维度：考核场次 | `exam_mode`(**FORMAL/PRACTICE**)、`exam_type`(**THEORY/PRACTICAL**)、`dept_id`、`module_id`、`duration`、`pass_line` |
| `exam_bank_rule` | **抽题规则（现行）** | `exam_id`、`bank_id`（注释：**业务语义 = 知识模块**）、`single/multi/judge_count` |
| `exam_knowledge_rule` | 抽题规则（**旧**，仍在兜底） | `exam_id`、`knowledge_point`、`question_count`、`ratio`、`single_ratio` |
| `practice_module` | 考核维度·分组层（**仅模拟考核**） | `dept_id`、`name` |
| `exam_subject_item` | 实操题目（`exam_type=PRACTICAL` 时） | `exam_id`、`seq`、`title`、`score` |
| `answer_sheet` / `answer_sheet_item` | 答卷 / 逐题（带 `stem_snapshot`、`full_score` 快照） | — |

### 1.2 数据佐证（2026-09-21 实测）

| 观察 | 数值 | 含义 |
|---|---|---|
| 题库 | **10 个 = 每部门「正式题库 + 模拟题库」各一**（104/103/105/106/107） | `bank_type` 实际表达的是**"服务哪种考核"**，不是"题型种类" |
| 题库内题目数 | 0 / 24 / 48 / 49 | 题库 = **一门科目的整库** |
| `question.knowledge_point` | **17 个不同值**（Docker 30、若依 30、DIX脚本 24、nginx 24、ECharts 18、MySQL 18、Redis 18、孪生体导入 18…） | ★ **一个题库里装着多个知识点** ⇒ **"题库 = 知识模块"这个等式不成立** |
| `exam_bank_rule` | **仅 1 行**（exam 28） | 新模型刚启用，绝大多数考核还没迁 |
| `exam_knowledge_rule` | **6 行**（历史配比） | 旧模型仍在生效 |

### 1.3 抽题到底看谁（代码实测，`PracticeServiceImpl` 第 88-115 行）

```java
List<ExamBankRule> bankRules = examRuleMapper.selectBankRules(cfgExamId);
if (bankRules != null && !bankRules.isEmpty()) {
    questions = pickByBankRules(bankRules);          // ① 新模型：按「题库 × 题型」配额
    result.put("bankRules", ...);
} else {
    List<ExamKnowledgeRule> rules = examRuleMapper.selectKnowledgeRules(cfgExamId);
    questions = pickByConfig(fallbackBankId, rules, config);   // ② 旧模型：单库 + 知识点配比
}
```

⇒ **这是「新优先 + 旧兜底」的双轨制**（不是 bug），但导致：**同一门考核的抽题依据取决于"有没有配过题库规则"** —— 这正是"关系说不清"的根源。

`ExamServiceImpl` 侧：新模型的保存/校验已有（`normalizeBankRules`/`saveBankRules`/`applyBankRuleSummary`），**但仍保留旧的 `validateKnowledgeRules`/`saveKnowledgeRules`**，并在详情里回传 `knowledgeRules`（第 265/314/369/838-895 行）。

### 1.4 四组撞名（混乱的直接来源）

| 界面上出现的词 | 实际实体 | 真实含义 | 建议 |
|---|---|---|---|
| **知识模块**（配置页标签「知识模块 (题库)」） | `question_bank` | 部门的一门科目 | ❌ **废弃这个词** |
| **知识点**（题目表单字段） | `question.knowledge_point` | 题目标签（Docker/Redis…） | ✅ 保留，但**必须写明"不参与抽题"** |
| **模块**（模拟备考管理） | `practice_module` | 模拟考核的**分组容器**（与知识无关） | ⚠️ 改叫「**备考模块**」 |
| `bank_type` = FORMAL/PRACTICE | `question_bank.bank_type` | 题库**服务**正式还是模拟 | ⚠️ 中文改「**适用范围**」 |

再加两处**字面**撞车（最坑）：

| A | B | 差别 |
|---|---|---|
| `exam_mode =` **PRACTICE** | `exam_type =` **PRACTICAL** | 前者=**模拟自测**，后者=**实操** —— 一字之差、含义完全不同 |
| `exam.exam_type`（理论/实操） | `question.qtype`（单选/多选/判断） | 都叫"题型"，建议前者叫「**考核形式**」、后者叫「**题目类型**」 |

---

## 二、建议的目标模型（方案 A，推荐）

```
组织：部门（sys_dept）
  │ 一个部门一套
  ├─ 知识维度 ─────────────────────────────  ← 与考核正交
  │    题库 question_bank（= 部门的一门科目，正式/模拟各一）
  │       └── 题目 question（题型 · 难度 · 分值）
  │             └── 知识点 knowledge_point 〔题目标签：只用于统计/画像/难度分析〕
  │
  └─ 考核维度 ─────────────────────────────
       备考模块 practice_module（仅模拟考核；分组的容器）
         └── 考核 exam（性质：正式/模拟 × 形式：理论/实操）
               ├── 组卷规则 exam_bank_rule（题库 × 题型 配额）★ 抽题唯一依据
               ├── 实操题目 exam_subject_item（仅 form=实操）
               └── 答卷 answer_sheet → answer_sheet_item（题干/满分快照）
```

**一句话记住**：
> **题库是"从哪儿出题"，知识点是"这题讲什么"，组卷规则是"每个题库各出几道什么题型"。三者里只有前两者在"知识"侧，后者在"考核"侧。**

### 命名收敛表（建议统一口径）

| 概念 | 统一叫法 | 英文/表 | 不用再说 |
|---|---|---|---|
| 部门的一门科目 | **题库** | `question_bank` | ~~知识模块~~ |
| 题目标签 | **知识点** | `question.knowledge_point` | ~~知识模块~~ |
| 考核的性质 | **考核性质**：正式考核 / 模拟自测 | `exam.exam_mode` | ~~考试类型~~ |
| 考核的形式 | **考核形式**：理论 / 实操 | `exam.exam_type` | ~~考试类型~~（也易混） |
| 题目的类型 | **题目类型**：单选/多选/判断 | `question.qtype` | ~~题型~~（与"考核形式"混用） |
| 模拟考核的分组 | **备考模块** | `practice_module` | ~~模拟模块~~（与"知识模块"撞名） |
| 抽题配比 | **组卷规则** | `exam_bank_rule` | ~~知识分布~~ |
| 题库的用途 | **适用范围**：正式 / 模拟 | `question_bank.bank_type` | ~~题库类型~~ |

### `exam_knowledge_rule` 的处置（三步退役，可回滚）

| 步 | 动作 | 影响 |
|---|---|---|
| 1（现状） | 保持"新优先 + 旧兜底" | 无 |
| 2 | 前端**停止传** `knowledgeRules`；后端 `validateKnowledgeRules`/`saveKnowledgeRules` 改为**只读兼容**（不再写）；补一个"仍在走旧链路的考核"清单（`exam` 无 `exam_bank_rule` 行） | 需前端 `ExamConfigCard`/`prep` 去掉旧字段；**建议先跑清单再改** |
| 3 | 旧考核逐个迁移到 `exam_bank_rule`（或明确标记为历史不再使用）→ 去掉 `PracticeServiceImpl` 的兜底分支 → `exam_knowledge_rule` 转为归档表（保留数据，不删除） | 需一次数据迁移脚本（幂等） |

---

## 三、三个方案对比

| | **A：两层知识结构**（推荐） | B：引入「知识模块」实体 | C：章节制（题库>章节>知识点） |
|---|---|---|---|
| 模型 | 题库 → 知识点（标签） | 新增 `knowledge_module`，题库归属模块 | 新增章节层，知识点挂章节 |
| 抽题依据 | 题库 × 题型（现状） | 模块 × 题型 | 章节 × 题型 |
| 改动量 | **小**：仅文案 + 退役旧表 | 中：新表 + 题库归属 + 组卷换轴 + 3 个页面 | 大：再加一层 + 数据录入成本 |
| 迁移 | 无 | 需为 10 个题库分配模块 | 需重建题库结构 |
| 适用 | 现状"部门=一个科目、按题型抽题" | 真需要"跨题库按知识领域抽题" | 教培/章节化课程体系 |
| 风险 | 低 | 中（组卷换轴会动抽题核心） | 高 |
| **结论** | ✅ **现在做** | ⏸ 等业务提出"跨库按知识领域抽题"再做 | ❌ 不需要 |

> ★ 方案 B 的真实价值场景只有一个：**同一知识点分散在多个题库里，且希望按知识点配额抽题**（现状：Docker 同时出现在正式库和模拟库）。如果业务上确实要这个，B 是正解；否则 A 足够。

---

## 四、页面与文案调整清单（改完即"捋顺"）

| 页面 | 现状 | 建议 |
|---|---|---|
| `business/questionBank/index.vue`（题库管理） | 标题「题库管理」 | 标题下方加一句定位：「一个部门一门科目；题目用**知识点**打标，供统计与画像」 |
| `business/questionBank/detail.vue`（题库详情） | 「知识点」列 + 表单字段 + 知识点统计 | ① 表单字段 label 改「知识点（题目标签）」+ 提示「不参与组卷抽题」；② 统计区块标题改「知识点分布（参考）」 |
| `exam/components/ExamConfigCard.vue`（考核配置） | 标签写「**知识模块 (题库)**」、标题「组卷题库与分值」 | ① 标签改「题库」；② 标题改「**组卷规则**（每个题库各出几道什么题型）」；③ 一行说明：「抽题只按题库×题型配额；知识点不参与」 |
| `department/study/prep/index.vue`（模拟备考管理） | 页签「模拟模块」+ 表格列「理论考核/实操题」 | ① 页签改「**备考模块**」；② 页面说明写明"模块只是分组，抽题仍看组卷规则" |
| 各处 `exam_mode`/`exam_type` 文案 | 「类型 / 形式 / 模式」混用 | 统一为「**考核性质**：正式/模拟」「**考核形式**：理论/实操」 |
| 题库列表 | 只有「题库名称/类型/状态」 | 类型列标题改「**适用范围**」；加一列「知识点数」（可由 `knowledge_point` 去重统计） |

---

## 五、风险与待确认

1. **双轨制期间的坑**：同一门考核，配了题库规则就走新链路、没配就走旧链路（按知识点）。**运营侧无法从界面看出该考核实际走哪条链路** → 建议在配置页显示「当前抽题依据：题库×题型 / 知识点配比（历史）」一行。
2. **跨库知识点**：若将来要"按知识点配额抽题"，需先解决"同一知识点跨多个题库"的归属问题（方案 B 的动机）。
3. **`question.position_id`（岗位）与 `question_bank.dept_id`（部门）并存**：题库按部门建、题目可标岗位 —— 需确认"同部门多岗位"是否要拆题库（现为不拆，靠 `position_id` 过滤）。
4. **快照机制已到位**：`answer_sheet_item.stem_snapshot/full_score` 已解决"题目被改删后旧答卷失真"，本次梳理**不需要**再动。
## 六、旧链路影响面清单（2026-09-21 实测，用于决定何时退役 `exam_knowledge_rule`）

### 6.1 全部有效考核的实际链路

| id | 考核名称 | 性质/形式 | 状态 | 部门 | 题库规则 | 知识点配比 | **实际抽题链路** |
|---|---|---|---|---|---|---|---|
| **28** | 001 | 正式/理论 | DRAFT | 交付 | **1 条** | 0 | ✅ ① **题库 × 题型**（新） |
| **2** | l | 正式/理论 | PUBLISHED | 设计 | 0 | 0 | ② **单库兜底**（`bank_id=12` + 题型数量）—— **不看知识点** |
| **25** | 开发部门模拟理论自测配置 | **模拟**/理论 | PUBLISHED | 开发 | 0 | **6 条** | ⚠️ ② **知识点配比（旧）** ← **唯一真正依赖旧表** |
| 1 | m | 正式/理论 | PUBLISHED | 设计 | — | — | 已删除（忽略） |

> 关键结论 1：**`exam_knowledge_rule` 只对"模拟考核 + 未配题库规则"生效**。
> 正式考核的回退走 `exam.bank_id` + 题型数量（`AnswerSheetServiceImpl.pickTheoryQuestions`），**与知识点无关**。
> 关键结论 2：**要退役旧表，只需处理 exam 25 一个考核** —— 影响面极小。

### 6.2 exam 25 的旧配比明细

| 知识点 | 抽题数 | 单选占比 |
|---|---|---|
| Java | 1 | 50% |
| MySQL | 2 | 50% |
| 若依 | 2 | 50% |
| Docker | 2 | 50% |
| Redis | 2 | 50% |
| 答题规则 | 1 | 50% |
| **合计** | **10 题** | — |

该考核当前题型数量为 单选 4 / 多选 3 / 判断 3（合计 10，与配比合计一致）。

### 6.3 迁移方案（二选一）

| | **a. 迁成「题库 × 题型」（推荐，可立即做）** | b. 保留旧链路 |
|---|---|---|
| 做法 | 为 exam 25 插入 1 条 `exam_bank_rule`：`bank_id=2`（开发部门模拟题库）、`single=4 / multi=3 / judge=3` | 不动，继续走兜底 |
| SQL | `INSERT INTO exam_bank_rule (exam_id, bank_id, single_count, multi_count, judge_count, sort_no, create_time) VALUES (25, 2, 4, 3, 3, 1, NOW())`（幂等化：先判存在再插） | 无 |
| 抽题效果 | 题量与题型**完全不变**，但**不再保证"每个知识点各覆盖 N 题"**（改为整库按题型随机） | 保持知识点覆盖 |
| 影响 | 模拟自测（不计正式成绩）—— 影响可接受 | `exam_knowledge_rule` 与兜底分支**无法退役** |
| 回滚 | `DELETE FROM exam_bank_rule WHERE exam_id=25`（旧配比仍在，自动回到旧链路） | — |

> 若业务上**必须**保证"每个知识点都被覆盖"，那只有两条路：① 保留旧链路；② 走**方案 B**（把知识点升级为可挂载的实体、组卷按"知识领域 × 题型"）。
> 否则建议选 **a**，随后即可停写旧配比（前端停传 + 后端只读兼容），最后归档 `exam_knowledge_rule`。

### 6.4 另附：代码注释里也残留了误称（建议一并订正）

- `AnswerSheetServiceImpl.pickTheoryQuestions` 注释：「优先多题库配置（exam_bank_rule）：「**题库即知识模块**」…」
  ⇒ 应改为「按**题库 × 题型**配额抽题；题库 = 部门的一门科目」。
- `exam_bank_rule.bank_id` 的列注释：「题库ID(question_bank.id)，**业务语义=知识模块**」⇒ 建议改为「题库ID（= 部门的一门科目）」。

---

## 七、本轮已落地 / 未落地（2026-09-21）

### ✅ 已落地：术语文案收敛（26 处替换，5 个前端文件 + 3 个后端文件注释）
| 文件 | 改动 |
|---|---|
| `business/questionBank/index.vue` | 页头加定位说明（题库=一门科目 / 知识点=标签、不抽题）；列标题「类型」→「**适用范围**」；筛选选项 **修正为「正式考核用(FORMAL) / 模拟考核用(PRACTICE)」**（原写成 `value="THEORY"` → 选它会筛出 0 行，**是个真 bug**）；`bankTypeLabel` 语义修正（FORMAL 原被显示成"理论题库"）；KPI 卡「模拟题库」→「模拟考核库」 |
| `business/questionBank/detail.vue` | 统计卡与表单字段标注「知识点（题目标签）」+「不参与组卷抽题」；分布区块**订正误导文案**（原文写"题量越多的知识点越适合作为组卷抽题范围"） |
| `exam/components/ExamConfigCard.vue` | 列头「知识模块（题库）」→「**题库**」；标题→「**组卷规则**（每个题库各出几道什么题型）」；★ **新增「当前抽题依据」提示**（题库×题型 / 单库兜底 / 知识点配比兜底），让双轨制在界面上可见 |
| `department/study/prep/index.vue` | 页签与区块「模拟模块」→「**备考模块**」；说明改为「模块只是分组容器；抽题仍按组卷规则」 |
| `business/exam/index.vue` | 列标题「类型」→「**考核形式**」 |
| 后端 3 个文件（`Exam`/`ExamBankRule`/`AnswerSheetServiceImpl`/`ExamServiceImpl`） | 订正 5 处「**题库即知识模块**」误称（**仅注释**，不影响行为） |

**验证**：5 个 SFC 静态校验全部通过；真机探针确认——题库页表头/筛选项/KPI/定位说明均正确，**选「正式考核用」正确筛出「开发部门正式题库/24 题」**（bug 已修）；配置卡在 exam 28 显示「当前生效：题库 × 题型」（绿标）、在 exam 2 显示「当前生效：单库兜底（未配组卷）」（黄标）；备考页页签已改「备考模块」。

> 注：`drawBasisText` 里「知识点配比（历史兜底）」分支实际只在**模拟考核**出现；而本配置卡所在列表硬编码 `examMode='FORMAL'`，故该分支当前为防御性代码（模拟考核的配置在「模拟备考管理」页内）。

### ⏸ 本轮**未**落地（按你的决定）
1. **exam 25（唯一依赖旧知识点配比的模拟考核）先不动库** —— 保持走 `exam_knowledge_rule` 兜底。
   ⇒ 因此 `exam_knowledge_rule` 与 `PracticeServiceImpl` 的兜底分支**暂不能退役**。
   ⇒ 待你确认「每个知识点各抽 N 题」这个覆盖要求是否重要后再定（重要 → 保留/走方案 B；不重要 → 按 §6.3-a 插一条 `exam_bank_rule` 即可迁移）。
2. `exam_bank_rule.bank_id` 的**列注释**（DDL）未改（你说本轮不动库）。
3. 未做：前端停止传 `knowledgeRules`、后端 `validateKnowledgeRules/saveKnowledgeRules` 只读化。


