# `feature/exam-module` 合并 + 数据库迁移 · 执行报告

> 执行时间：2026-09-21 11:07–11:45　执行方式：**GitHub 网页合并** + 本地 shell 写引用 + pymysql 跑迁移
> 依据文档：`合并操作手册_exam-module到main.md`、`考核与成绩_设计修订清单_基于exam-module.md`

---

## 一、结论摘要

| 项 | 结果 |
|---|---|
| 合并 | ✅ **已完成**（PR #6，merge commit `0803f67e`） |
| 冲突 | 2 个，**都已解干净**（全树无冲突标记残留） |
| 6 个 SQL 迁移 | ✅ **全部执行成功**，且**复跑零变化（完全幂等）** |
| 本地仓库 | ✅ 顺带修好了**对象库缺失 3 个对象的旧损坏** |
| 新分支 | ✅ `feature/exam-merge-cleanup` @ `0803f67e`（HEAD 已切过去） |
| 待办 | ⚠️ 后端需**重编+重启**才会带上新接口；2 处调用点改动**尚未提交** |

---

## 二、合并结果（GitHub 网页）

```
0803f67  Merge pull request #6 from Ranin-ll/feature/exam-module
  父提交：a229d04104（main，含 PR#5）+ 4ff2bd42（分支，含网页解冲突提交 4ff2bd4）
```
- 分支 tip 因「网页解冲突」从 `d4e31c0521` → `4ff2bd42`（GitHub 把冲突解决结果提交回 PR 头分支）
- 远端 main：`a229d04104` → **`0803f67ec8cfcd8b31a1246b8aed6881757d7f34`**

### 2 个冲突的最终解法（已验收）
| 文件 | 解法 | 验收 |
|---|---|---|
| `ruoyi-ui/src/utils/filePreview.js`（add/add） | 采用**并集版**（11 个导出，分支侧 `extOf` 改名 `extLabel`） | ✓ 11 个导出俱在，5 个引用方所需符号全部齐备 |
| `ruoyi-ui/src/views/assessment/learning/index.vue`（content） | 保留 **main 的 `isFormal` 文案**，同时接受分支侧删减（`learning-rules`/`comingSoon` 已清） | ✓ 两者兼得 |

> ★ 网页端标签是**反的**：`Current change` = 同事的 PR 分支、`Incoming change` = main（详见手册 §一.1）。

---

## 三、本地仓库状态

| 项 | 值 |
|---|---|
| `main` | `0803f67ec8cfcd8b31a1246b8aed6881757d7f34`（= 远端 main） |
| 当前分支 | **`feature/exam-merge-cleanup`** @ `0803f67e` |
| 工作区 | 已与合并后的 main 对齐（922 个文件）；分支删除的 `assessment/practice/subject.vue` 已删 |
| 未提交 | ① 2 处调用点改名（**新**）② 你的 2 个下钻设计稿改动（原有）③ 12 个未跟踪文档（原有） |
| 备份 | `.codex-tmp/premerge-backup/`（合并前快照 + 13 个文档 + MIDX/孤立 idx） |

### 顺手修掉的旧损坏（09-18 遗留，**不是本次造成的**）
- 症状：`git read-tree HEAD` 报 `unable to read tree`，但 `git status` 谎报"干净"（cache-tree 短路，**别信 status**）
- 缺失：tree `03f26661`（`ruoyi-business/src/main/java/com`）、tree `0f1b9f77`（`ruoyi-common/.../com/ruoyi/common`）、blob `0b60a344`（`api/business/personnel.js`）
- 修复：从 index/工作区用 `git mktree` / `git hash-object -w` **原样重建**（OID 逐字一致）；终检 `git fsck` 无 `missing *`
- 另：过期 `multi-pack-index` 已移走（备份可放回）；确认本仓库是**浅克隆**；有一个 09-18 遗留的孤立 `.idx`（`.pack` 已丢）

### 一处需要知道的假象
`checkout-index` 后 `git status` 一度报 309 个文件 M —— 实为 **index 统计缓存未填充 + `core.autocrlf=true`** 所致；用 blob 哈希逐个比对证实 18 个"改动"内容与 HEAD **完全一致**，刷新后归零。

---

## 四、SQL 迁移执行结果

备份：`.codex-tmp/backup_intern_assessment_before_exam_module.sql`（570 KB / 73 表，`--single-transaction`）

| # | 脚本 | 结果 |
|---|---|---|
| 1 | `intern_assessment_exam_bank_rule.sql` | 新建表 `exam_bank_rule` |
| 2 | `intern_assessment_practical_items.sql` | 新建表 `exam_subject_item`；加列 `answer_sheet_item.subject_item_id` |
| 3 | `intern_assessment_practice_module.sql` | 新建表 `practice_module`；加列 `practice_subject.module_id`；补 5 个 `business:pmodule:*` 权限 |
| 4 | `intern_assessment_answer_sheet_item_snapshot.sql` | 加列 `answer_sheet_item.stem_snapshot` / `full_score` |
| 5 | `intern_assessment_practice_record_exam.sql` | 加列 `practice_record.exam_id` |
| 6 | `intern_assessment_material_menu.sql` | 补 5 个 `business:material:*` 权限 |

**前后对照（实测）**
| 指标 | 前 → 后 |
|---|---|
| 新表 | 0 → **3**（`exam_bank_rule` / `exam_subject_item` / `practice_module`） |
| 新列 | 0 → **5** |
| `sys_menu` | 150 → **160**（10 个按钮权限） |
| `sys_role_menu` | 248 → **268**（两个角色各 +10：`SUPER_ADMIN` / `DEPT_ADMIN`） |

**存量回填（实测）**
| 项 | 结果 |
|---|---|
| `practice_module` | 2 条「默认模块」（dept 104 开发 / 105 设计） |
| `practice_subject.module_id` | 16 条实操题全部归集到默认模块 |
| `answer_sheet_item` 快照 | 20 条明细**全部**补上 `stem_snapshot` + `full_score`（残留为空 = **0**） |
| `exam_subject_item` | 0 条 —— **正常**：库内 3 场考核全为 `THEORY` 且 `subject_content` 为空，回填条件（PRACTICAL 且有题干）不匹配 |

**幂等验证**：把 6 个脚本**再执行一遍**，指纹 `(表3, 列5, menu160, role_menu268, subject_item0, module2)` **零变化** ✅

---

## 五、遗留与下一步

| # | 事项 | 说明 |
|---|---|---|
| 1 | ✅ **后端已重编 + 重启（11:45）** | 合并只动了 `ruoyi-business`(40) / `ruoyi-ui`(26) / `sql`(6)，**未动 ruoyi-admin** → 只需重建业务模块：真实编译 148 个源文件 → 内层 jar 覆盖 `.codex-tmp/app/BOOT-INF/lib/` → 重启 8.5s。**5 个新接口全部 200**，旧接口回归 200（详见 §七） |
| 2 | ⚠️ **前端重启 dev-server** | 合并带来多文件增删，watcher 会停滞 → 必须重启（不是 HMR 能解决的） |
| 3 | ⚠️ **修复提交待推送（main 目前仍是坏的）** | 本地已在 `feature/exam-merge-cleanup` 上提交 **`0ff82c3a`**（父 `0803f67e`）：清理 `filePreview.js` 的粘贴残留 + 2 处调用点 `extOf → extLabel`。**本机不能 push** → 需你在自己终端推送并合入 main（详见 §六） |
| 4 | 按修订清单调设计稿 | `考核与成绩_设计修订清单_基于exam-module.md` 的 6 处修订（3 处结构级：组卷模型=题库×题型、模拟考核加模块层级、L2 走题干/满分快照） |
| 5 | 3 个待确认 | ① 理论卷面满分是否回传 ② `bank-options` 返回结构 ③ `grading/{examId}` 字段是否变了 |

### 回滚方法（如需）
- **代码**：`main` 回 `a229d04104`、分支回 `d4e31c0521`（都在本地对象库里；本机建议用 shell 写 ref 而非 `git reset`）
- **数据库**：`/e/ia-dev/mysql/server/bin/mysql.exe -uroot -proot intern_assessment < .codex-tmp/backup_intern_assessment_before_exam_module.sql`
  （迁移都是**只加不删**，回滚实际上非必需）

---

## 六、合并后发现的「粘贴污染」（已修，待推送）

### 现象
前端整页报错（`localhost:9530/super/ops/course-admin`）：
```
Failed to compile.
./src/utils/filePreview.js
SyntaxError: ...filePreview.js: Missing semicolon. (152:2)
```

### 根因
网页解冲突时，把 `合并冲突解决_filePreview.js` 的内容粘进 GitHub 编辑器时，**连带粘进了 4 行编辑器状态栏文字**，成了文件末尾的垃圾行：
```
151 | }
152 | Ln 3, Col 1      ← 垃圾（编辑器光标位置）
153 | 行 151           ← 垃圾
154 | JavaScript       ← 垃圾（语言模式）
155 | 只读             ← 垃圾
```
⇒ babel 在 152 行解析失败 → 整包 `Failed to compile`。

### 修复
1. 裁掉末尾 4 行，文件内容与备好的并集文件**逐字节一致**；`node --check` exit 0
2. 全仓扫描同类污染（`^Ln \d+, Col \d+$` / `^行 \d+$` / 单独成行的 `JavaScript`/`只读`）→ **无其它中招文件**
3. dev-server 自动重编译：`DONE Compiled successfully in 1496ms`；首页 / `app.js` 均 HTTP 200；产物里已含 `extLabel`（3 处）
4. 提交为 **`0ff82c3a`**（plumbing：`git add` → `write-tree` → `commit-tree -p 0803f67e` → shell 写 ref）

### ★ 教训（下次必做）
**网页解冲突粘贴后，必须对落地文件做一次校验**，别只看"冲突标记没了"：
```bash
node --check <改动的 .js>                       # 语法
grep -rnE "^(Ln [0-9]+, Col [0-9]+|行 [0-9]+|JavaScript|只读)\s*$" <改动目录>   # 粘贴残留
```
同理适用于 `.vue`（`vue-cli-service` 编译即验）与 Java。

### 推送命令（请在你自己的终端执行）
```bash
cd "E:/实习生学习考核系统"
git push -u origin feature/exam-merge-cleanup
# 然后在 GitHub 开 PR：feature/exam-merge-cleanup → main，合并即可修复 main
```
> 备注：本次提交只改 3 个文件（`filePreview.js` 删 4 行、两个调用点各 1 行改名），可在 PR 里直接核对。

---

## 七、后端重编与端到端验证（11:45）

### 起因
部门管理员端「模拟备考管理」页界面正常，但右上角连弹 **「系统接口404异常」×3** —— 后端仍是**合并前的旧 jar**，新接口不存在。

### 重建范围（先查清再动手）
```
git diff --name-status 96891c1d0d 0803f67e → ruoyi-business 40 / ruoyi-ui 26 / sql 6 / 根文档 1
```
⇒ **ruoyi-admin 与 ruoyi-common 未变** ⇒ 只需重建 `ruoyi-business`，不必重打 87MB fat jar。

### 步骤与验证
| 步 | 操作 | 验证 |
|---|---|---|
| 1 | `taskkill /PID 45892 /F` 停后端 | 8080 已释放（jar 不被占用才能覆盖） |
| 2 | classworlds launcher `-o -pl ruoyi-business -am -DskipTests package` | `BUILD SUCCESS` + **`Compiling 148 source files`**（真编译，非增量假成功） |
| 3 | 检查内层 jar | 12 个新类条目俱在（`Material`/`PracticeModule`/`ExamBankRule`/`ExamSubjectItem` 的 controller/domain/mapper/service），class 体积 0.9–9KB 正常；`mapper/business/MaterialMapper.xml`、`PracticeModuleMapper.xml` 也在 |
| 4 | 覆盖 `.codex-tmp/app/BOOT-INF/lib/ruoyi-business-3.8.8.jar` | 哈希比对一致（`2b418c97`），349450 → 397503 字节 |
| 5 | 后台重启 `java -cp "BOOT-INF/classes;BOOT-INF/lib/*" com.ruoyi.RongguApplication`（cwd = `.codex-tmp/app`） | `Started RongguApplication in 8.483 seconds`，8080 监听（pid 4580），日志无 ERROR |

### 接口冒烟（部门管理员 `test_dev_admin` / 104 开发）
| 接口 | 之前 | 现在 |
|---|---|---|
| `GET /business/material/list` | **404** | ✅ 200 `{"total":0,"rows":[]}` |
| `GET /business/practice-module/list` | **404** | ✅ 200（含回填的「默认模块」id=2 / deptId=104） |
| `GET /business/exam/bank-options?deptId=104` | 被旧 `/{id}` 吞掉 | ✅ 200 `bankId=2 开发部门模拟题库 PRACTICE questionCount=24 singleCount=12` |
| `GET /business/exam/intern-options?deptId=104` | 404 | ✅ 200（在培实习生花名册） |
| `GET /business/practice-subject/published` | — | ✅ 200（`moduleId=2`，说明回填生效） |
| 回归：`course/list`、`question-bank/list`、`task/my` | 200 | ✅ 仍 200 |

### 前端
dev-server **无需重启**（本轮未停滞，已 `Compiled successfully`）。刷新页面即可，404 提示应消失。

> ⚠️ 注意：curl 输出里的中文在某些行显示为乱码（如 `榛樿ā鍧`）是**终端 GBK 显示问题**，不是接口编码问题——同一响应里另有中文正常显示，且库为 `utf8mb4`。


