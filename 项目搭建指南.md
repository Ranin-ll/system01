# 实习生学习考核系统 - 若依项目搭建指南

> 技术栈：RuoYi-Vue 3.8.8 + SpringBoot + JDK1.8 + Maven + MyBatis-Plus + Vue2 + Element-UI
> 数据库：MySQL 8.0（intern_assessment）
> 本地项目路径：`E:\实习生学习考核系统\ruoyi-vue`

---

## 一、项目结构

```
ruoyi-vue/
├── ruoyi-admin/          # 启动入口（主应用）
│   └── src/main/resources/
│       ├── application.yml           # 主配置
│       └── application-druid.yml     # 数据库配置（已指向 intern_assessment）
├── ruoyi-common/         # 通用工具类
├── ruoyi-framework/      # 框架核心（安全/权限/拦截器）
├── ruoyi-generator/      # 代码生成器（核心工具）
├── ruoyi-quartz/         # 定时任务
├── ruoyi-system/         # 系统模块（用户/角色/部门/菜单）
├── ruoyi-ui/             # 前端（Vue2 + Element-UI）
└── sql/
    ├── ry_20240629.sql               # 若依系统表（sys_*）
    ├── quartz.sql                    # 定时任务表
    └── intern_assessment_business.sql # 业务表（37张，已融合）
```

---

## 二、数据库初始化

当前本地数据库连接：MySQL `localhost:3306`，数据库 `intern_assessment`，用户名 `root`，密码 `root`。Navicat Premium 只是数据库客户端，不是数据库存储位置；打开 Navicat 后新建 MySQL 连接，填入上述信息即可查看同一套数据。

Navicat 中重点查看：`sys_dept`（部门）、`position`（岗位）、`dept_position`（部门岗位映射）、`sys_role`（角色）、`sys_user`（登录账号和导师档案）、`sys_user_role`（账号角色）、`register_application`（注册申请）、`audit_record`（审核记录）。前端登录、注册岗位、注册审核和个人信息均通过后端读取这些表；登录页的测试账号按钮只负责填充账号密码，不是 Mock 登录。

固定有效测试账号密码均为 `admin123`：

| 类型 | 账号 |
|---|---|
| 超级管理员 | `test_super_admin` |
| 交付 / 开发 / 设计 / 质检 / 建模部门管理员 | `test_dept_admin` / `test_dev_admin` / `test_design_admin` / `test_qa_admin` / `test_model_admin` |
| 实施 / 开发 / 设计 / 质检 / 建模实习生 | `test_impl_intern` / `test_dev_intern` / `test_design_intern` / `test_qa_intern` / `test_model_intern` |

其中实施实习生用于测试 `FORMAL_TRAINEE`，其余四人为 `PRE_TRAINEE`。若依基础脚本自带的 `admin`、`common` 是框架内置角色；本项目业务角色固定为 `SUPER_ADMIN`、`DEPT_ADMIN`、`PRE_TRAINEE`、`FORMAL_TRAINEE` 四个。旧版 `test_pre_trainee`、`test_formal_trainee` 已停用，仅保留历史记录。

### 1. 执行 SQL 脚本（按顺序）

```bash
# 进入 MySQL
mysql -uroot -proot

# 全新数据库按以下顺序执行
source E:/实习生学习考核系统/ruoyi-vue/sql/ry_20240629.sql;
source E:/实习生学习考核系统/ruoyi-vue/sql/quartz.sql;
source E:/实习生学习考核系统/ruoyi-vue/sql/intern_assessment_business.sql;
source E:/实习生学习考核系统/ruoyi-vue/sql/intern_assessment_seed.sql;
source E:/实习生学习考核系统/ruoyi-vue/sql/intern_assessment_mentor_fields.sql;
source E:/实习生学习考核系统/ruoyi-vue/sql/intern_assessment_agreement_seed.sql;
source E:/实习生学习考核系统/ruoyi-vue/sql/intern_assessment_menu.sql;
source E:/实习生学习考核系统/ruoyi-vue/sql/intern_assessment_skeleton_menu.sql;
source E:/实习生学习考核系统/ruoyi-vue/sql/intern_assessment_register_review_fix.sql;
source E:/实习生学习考核系统/ruoyi-vue/sql/intern_assessment_intern_navigation.sql;
source E:/实习生学习考核系统/ruoyi-vue/sql/intern_assessment_test_accounts.sql;
```

`intern_assessment_menu.sql` 和 `intern_assessment_skeleton_menu.sql` 用于首次初始化菜单；其余业务修复脚本按脚本内的幂等规则可重复执行。`intern_assessment_duplicate_test_account_fix.sql`、`intern_assessment_test_account_name_fix.sql` 只用于修复旧环境，不属于全新数据库的必执行脚本。

### 2. 验证表创建

```sql
USE intern_assessment;

-- 查看系统表（若依自带）
SHOW TABLES LIKE 'sys_%';

-- 查看业务表（37张）
SHOW TABLES WHERE Tables_in_intern_assessment NOT LIKE 'sys_%' 
              AND Tables_in_intern_assessment NOT LIKE 'qrtz_%';

-- 查看 sys_user 扩展字段
DESC sys_user;
-- 应该能看到新增的：position_id, mentor_name, mentor_phone, user_status, 
--                  protocol_status, expected_entry_date, id_card, is_dept_admin
```

### 3. 核心表说明

| 类型 | 表名 | 说明 |
|---|---|---|
| **系统表（若依）** | sys_dept | 部门表（层级结构，业务用第一层） |
| | sys_user | 用户表（已扩展业务字段） |
| | sys_role | 角色表（已添加4个业务角色） |
| | sys_user_role | 用户-角色关联 |
| | sys_menu / sys_role_menu | 菜单权限 |
| **业务主数据** | position | 岗位类型（5个：实施/开发/设计/质检/建模） |
| | dept_position | 部门-岗位绑定 |
| **业务表** | register_application | 注册申请单 |
| | course / study_item / study_record | 学习域 |
| | exam / answer_sheet / assessment_result | 考试域 |
| | promotion_application / certificate | 转正与证书 |
| | ... | 共37张业务表 |

---

## 三、后端启动

在 VS Code 中已配置 `.vscode/tasks.json`，可用“终端 → 运行任务”直接执行“后端：编译打包”“后端：启动 8080”“前端：启动 9530”。

### 1. 配置数据库连接

已配置好：`ruoyi-admin/src/main/resources/application-druid.yml`

```yaml
master:
    url: jdbc:mysql://localhost:3306/intern_assessment?...
    username: root
    password: root  # 根据实际修改
```

### 2. 编译项目

```bash
cd /d E:\实习生学习考核系统\ruoyi-vue

# 设置 JDK8 环境
set JAVA_HOME=D:\java\openjdk-8u312-b07

# 编译（跳过测试）
..\.local\tools\apache-maven-3.9.11\bin\mvn.cmd -s .\.local\maven-settings.xml clean package -DskipTests
```

### 3. 启动后端

```bash
# 方式1：直接运行 jar
java -jar ruoyi-admin/target/ruoyi-admin.jar

# 方式2：Maven 运行
cd ruoyi-admin
mvn spring-boot:run
```

启动成功后访问：http://localhost:8080

默认账号：`admin / admin123`

---

## 四、前端启动

### 1. 安装依赖

```bash
cd /d E:\实习生学习考核系统\ruoyi-vue\ruoyi-ui

# 使用项目内置 Node 16.20.2
npm install
# 或
pnpm install
```

### 2. 启动前端

```bash
npm run dev
```

访问：http://localhost:9530

前端会自动代理到后端 http://localhost:8080

---

## 五、核心适配点（业务与若依融合）

### 1. 用户表扩展（sys_user）

| 若依字段 | 业务扩展字段 | 说明 |
|---|---|---|
| user_id | - | 用户ID |
| dept_id | - | 部门ID（关联 sys_dept） |
| - | **position_id** | 岗位类型ID（关联 position） |
| - | **user_status** | **账号状态机**（核心） |
| - | protocol_status | 协议签署状态 |
| - | mentor_name / mentor_phone | 审核时人工登记的导师姓名和联系方式，仅展示给对应实习生 |
| status | - | 若依账号状态（启用/停用） |

**账号状态机（user_status）**：
```
WAIT_AUDIT(待审核) 
  → REJECTED(已驳回) → 重新提交 → WAIT_AUDIT
  → PRE_TRAINEE(预备实习生)
    → PENDING_PROMOTE(待转正)
      → FORMAL_TRAINEE(正式实习生)
      → PRE_TRAINEE(驳回)
    → DISABLED(禁用) / ARCHIVED(离职归档)
```

### 2. 角色映射（sys_role）

| 业务角色 | 若依 role_key | data_scope | 说明 |
|---|---|---|---|
| 超级管理员 | SUPER_ADMIN | 1（全局） | 系统管理 |
| 部门管理员 | DEPT_ADMIN | 2（本部门） | 部门业务管理 |
| 预备实习生 | PRE_TRAINEE | 5（个人） | 学员 |
| 正式实习生 | FORMAL_TRAINEE | 5（个人） | 转正后学员 |

### 3. 数据隔离实现

若依已实现 `@DataScope` 注解，可在业务接口上使用：

```java
@DataScope(deptAlias = "d", userAlias = "u")
public List<RegisterApplication> selectRegisterList(RegisterApplication app) {
    // 自动注入部门范围过滤
}
```

### 4. 岗位决定内容

业务表（course/exam/material/question）通过 `position_id` 关联岗位：

```java
// 查询当前用户岗位可见的课程
Long positionId = SecurityUtils.getLoginUser().getUser().getPositionId();
List<Course> courses = courseMapper.selectByPosition(positionId);
```

---

## 六、代码生成器使用（核心工具）

若依自带代码生成器，可基于业务表快速生成 CRUD 代码。

### 1. 导入业务表到代码生成器

1. 启动后端，访问 http://localhost:8080
2. 登录 admin / admin123
3. 进入 **系统工具 → 代码生成**
4. 点击 **导入**，选择业务表（如 `register_application`）
5. 配置生成信息：
   - 生成模块名：`business`
   - 生成业务名：`register`
   - 生成功能名：`注册申请`
6. 点击 **生成代码**，下载 zip 包

### 2. 生成的代码结构

```
ruoyi-business/
├── controller/
│   └── RegisterApplicationController.java  # REST接口
├── domain/
│   └── RegisterApplication.java            # 实体类
├── mapper/
│   └── RegisterApplicationMapper.java      # MyBatis接口
├── service/
│   ├── IRegisterApplicationService.java    # 服务接口
│   └── impl/
│       └── RegisterApplicationServiceImpl.java  # 服务实现
└── resources/mapper/business/
    └── RegisterApplicationMapper.xml       # MyBatis XML

ruoyi-ui/src/
├── api/business/
│   └── register.js                         # 前端API
└── views/business/register/
    └── index.vue                           # 列表页
```

### 3. 建议生成的业务模块（按优先级）

| 优先级 | 业务模块 | 表 | 说明 |
|---|---|---|---|
| P0 | 注册审核 | register_application, audit_record | 账号生命周期 |
| P0 | 保密协议 | agreement_template, agreement_signature | 首登强制签署 |
| P1 | 学习管理 | course, study_item, study_record | 课程学习 |
| P1 | 题库管理 | question | 题库维护 |
| P2 | 考试管理 | exam, answer_sheet, assessment_result | 理论+实操 |
| P2 | 成绩管理 | assessment_result, score_correction | 成绩发布 |
| P3 | 转正审批 | promotion_application, certificate | 转正流程 |
| P3 | 能力画像 | stage_evaluation, profile_snapshot | 评价与画像 |

---

## 七、下一步开发建议

### 阶段1：跑通基础流程（1-2周）

1. **用代码生成器生成基础 CRUD**（注册申请、岗位、课程）
2. **实现注册审核接口**：
   - 提交注册申请
   - 部门管理员审核（通过/驳回）
   - 审核历史查询
3. **实现登录与账号状态校验**：
   - 登录时检查 `user_status`
   - 待审核/已驳回/禁用状态拦截
4. **实现保密协议签署**：
   - 首登检查 `protocol_status`
   - 未签署强制跳转协议页
   - 签署成功后更新状态

### 阶段2：核心业务（2-3周）

5. **学习管理**：
   - 课程/章节/单项 CRUD
   - 学习记录（进度跟踪）
   - 学习完成率统计
6. **题库管理**：
   - 题目 CRUD
   - Excel 导入（用若依的 EasyExcel 工具）
7. **模拟考试**：
   - 随机组卷（从题库抽10题）
   - 答卷保存
   - 自动评分

### 阶段3：正式考核与转正（2-3周）

8. **正式考试**：
   - 考试发布（理论+实操）
   - 规则快照冻结
   - 答卷状态机
9. **成绩管理**：
   - AI 评分（可先模拟）
   - 人工复核
   - 成绩发布
10. **转正审批**：
    - 门槛校验
    - 两级审批
    - 证书生成

---

## 八、常见问题

### Q1: JDK 版本不对？

```powershell
# 检查当前 Java 版本
& 'D:\java\openjdk-8u312-b07\bin\java.exe' -version

# 如果不是 1.8，设置环境变量
$env:JAVA_HOME = 'D:\java\openjdk-8u312-b07'
$env:Path = "$env:JAVA_HOME\bin;$env:Path"
```

### Q2: 数据库连接失败？

检查 `application-druid.yml` 中的：
- 数据库名：`intern_assessment`
- 用户名/密码：`root/root`（根据实际修改）
- MySQL 服务是否启动：`systemctl status mysql`

### Q3: 前端启动报错？

```bash
# 清除缓存重新安装
cd ruoyi-ui
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Q4: 代码生成器生成的代码在哪？

默认生成在 `ruoyi-generator/src/main/resources/vm/` 目录下的模板，
生成的 zip 包解压后按目录结构复制到对应模块。

---

## 九、项目交付清单

| 文件/目录 | 说明 |
|---|---|
| `E:\实习生学习考核系统\ruoyi-vue\` | 完整的若依项目（v3.8.8） |
| `sql/intern_assessment_business.sql` | 业务表融合脚本（37张） |
| `ruoyi-admin/src/main/resources/application-druid.yml` | 数据库配置（已指向 intern_assessment） |
| `JDK8 环境` | `/usr/lib/jvm/java-8-openjdk-amd64` |

---

## 十、快速启动命令（一键执行）

```powershell
$project = 'E:\实习生学习考核系统\ruoyi-vue'
$env:JAVA_HOME = 'D:\java\openjdk-8u312-b07'

# 首次使用时，先按“二、数据库初始化”的顺序执行全部 SQL。

# 编译后端
Set-Location $project
& '..\.local\tools\apache-maven-3.9.11\bin\mvn.cmd' `
  -s '.\.local\maven-settings.xml' clean package -DskipTests

# 启动后端（新 PowerShell 窗口）
& 'D:\java\openjdk-8u312-b07\bin\java.exe' `
  -Duser.timezone=Asia/Shanghai -jar '.\ruoyi-admin\target\ruoyi-admin.jar'

# 启动前端（另一个 PowerShell 窗口）
Set-Location "$project\ruoyi-ui"
& '..\..\.local\tools\node-v16.20.2-win-x64\npm.cmd' run dev -- --port 9530
```

访问：http://localhost:9530
测试账号：`test_super_admin / admin123`

---

**项目已就绪，可以开始业务开发。**
