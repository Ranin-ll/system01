# 后端实现现状盘点 与 考核模块补全方案

> 盘点时间：2026-09-16　盘点方式：直接读 `ruoyi-business/src/main` 源码 + 直连 `intern_assessment` 库核对
> 前置约束（用户明确要求）：**已实现的后端接口一律不改**（签名 / 返回结构 / 权限注解都不动），考核域的后端缺口**先出方案，不擅自改**。

---

## 〇、结论速览

| 维度 | 结论 |
|---|---|
| 后端控制器 | **12 个**，对应 12 个接口前缀，全部真实可用 |
| 业务表 | 库内共 **66 张表**，其中业务表 **36 张** |
| 有 Java 层的表 | **18 张**（学、考、题库、注册、协议这几条主线） |
| **表在、但零 Java 层**（空转） | **18 张**——设计稿要的画像 / 证书 / 备考资料 / 通知 / 画像快照全在这里 |
| 本次改版是否动了后端 | **零改动**，只改前端 5 个文件 + 新增 1 个工具文件 |
| 考核域缺口 | 4 个 P0/P1 缺口，**都可以用「新增只读接口」的方式补，不动现有接口** |

一句话：**「能跑的都能跑，缺的是聚合读接口，不是业务写逻辑」**。设计稿要的数据在库里都建好了，只是没有对应的 Java 读取层，所以前端先用示例数据撑版面是对的。

---

## 一、后端实现状况盘点

### 1.1 控制器与接口清单（12 个，实测 200）

| # | 前缀 | 控制器 | 实习生可用？ | 说明 |
|---|---|---|---|---|
| 1 | `/business/auth` | InternAuthController | ✅ 匿名 | 注册、岗位下拉、协议查询/签署 |
| 2 | `/business/register` | RegisterApplicationController | ❌ | 注册审核 list/summary/status/history/submit/resubmit/audit |
| 3 | `/business/course` | CourseController | ❌ | 课程 CRUD / publish / disable / positions |
| 4 | `/business/course` | CourseContentController | ❌ | 章节 / 资料 / 附件上传 / 学习记录 |
| 5 | `/business/learning` | LearningController | ✅ 类级 `hasAnyRoles('PRE_TRAINEE,FORMAL_TRAINEE')` | 课程列表、课程详情、进度上报 |
| 6 | `/business/position` | PositionController | ❌ | 岗位管理 |
| 7 | `/business/practice` | PracticeController | ✅ `hasRole('PRE_TRAINEE')`（my/record 两者皆可） | 模拟考核：start / submit / my / record |
| 8 | `/business/practice-subject` | PracticeSubjectController | 一半 | 管理端 CRUD；`/published`、`/published/{id}` 走 `business:bank:list` |
| 9 | `/business/exam` | ExamController | ❌ | 考核管理（管理端） |
| 10 | `/business/answer-sheet` | AnswerSheetController | 一半 | start/submit 仅 PRE；my/detail 两者皆可；grading/grade/publish 管理端 |
| 11 | `/business/question` | QuestionController | ⚠️ 只有 `preview`/`submit` 走 `business:bank:list` | 题目的 CRUD 与导入是管理端 |
| 12 | `/business/question-bank` | QuestionBankController | ❌ | 题库 CRUD |

### 1.2 表 ↔ Java 层对应（关键结论）

**✅ 有 Java 层（18 张）**——本次改版**地面保护区**，一律只读不改：

```
study_item          study_record        course              course_chapter
question            question_bank       exam                answer_sheet
answer_sheet_item   practice_record     practice_record_item practice_subject
position            dept_position       register_application audit_record
agreement_template  agreement_signature
```

**⚠️ 表在、零 Java 层（18 张）**——设计稿要的版面大多落在这里，前端目前用示例数据：

| 空转表 | 设计稿用在哪 | 行数 |
|---|---|---|
| `material` | 备考资料页 | 0 |
| `certificate` | 工作台/成绩页的电子证书 | 0 |
| `assessment_result` | 成绩总览、部门对比、综合分 | 0 |
| `exam_rule_snapshot` | 正式考核资格校验 + 综合分权重 | 0 |
| `practical_submission` | 实操成果提交（正式考核实操环节） | 0 |
| `notification` | 顶栏消息铃铛 | 0 |
| `profile_snapshot` | 能力画像快照（总表） | 0 |
| `ability_dimension` | 能力画像四维度配置 | 0 |
| `stage_evaluation` | 阶段评价（画像的第 4 个来源） | 0 |
| `exam_paper_question` | 固定卷（冻结题序） | 0 |
| `promotion_application` | 转正申请 | 0 |
| `learning_task` / `task_assignment` / `task_post` / `task_submission` | 任务中心 | 0 |
| `score_correction` | 成绩更正留痕 | 0 |
| `operate_log` / `notice` | 审计 / 公告 | 0 |

> 表结构都是完备的，不是「待建表」，是「待接读接口」。这点很重要——补全成本很低。

### 1.3 考核域实测现状（读源码得到，非猜测）

| 环节 | 现状 | 证据 |
|---|---|---|
| 模拟考核抽题 | **固定抽 10 题**，`ORDER BY RAND()` | `PracticeServiceImpl:63` `selectQuestionsForExam(bankId, null, 10)` |
| 模拟考核返回 | 只返回 `id / qtype / stem / optionsJson`，**不含 knowledge_point** | `PracticeServiceImpl:67-75` |
| 模拟交卷落库 | 逐题快照 `stem/options/analysis/正确答案`，**未快照 knowledge_point** | `PracticeRecordItem` 字段表（无该列） |
| 正式考核准入 | 仅校验：① PRE 角色 ② 部门匹配 ③ 考试 PUBLISHED ④ 未参加过 | `AnswerSheetServiceImpl:46-63` |
| **正式考核资格校验** | **完全没有**——不校验学习完成率、不校验协议、不支持补考 | 同上，`exam_rule_snapshot` 一行都没读 |
| 正式考核组卷 | 按 `exam.single_count/multi_count/judge_count` 分题型随机抽 | `AnswerSheetServiceImpl:86-92` |
| 正式考核发布 | 只写 `answer_sheet`（`final_score` = ai + manual，对比 `exam.pass_line`） | `AnswerSheetServiceImpl:345-401` |
| **综合分 / 加权** | **未实现**——`exam_rule_snapshot.theory_weight/practice_weight` 从未被使用 | `grep exam_rule_snapshot` → 0 命中 |
| **assessment_result** | **从未被写入** | `grep assessment_result` → 0 命中 |
| 题量硬编码 | 模拟考核的 10 是**写死在 Service 里**的 | `PracticeServiceImpl:63` |

### 1.4 冻结边界（本次改版绝不触碰）

- `ruoyi-business/**` 全部 Java 代码与 Mapper XML：**0 处改动**
- `ruoyi-ui/src/api/business/*.js`：**0 处改动**
- 12 个控制器的路由、`@PreAuthorize`、返回结构：**0 处改动**
- 库表结构：**0 处 DDL**

---

## 二、本次改版实际动作（全部在前端）

### 2.1 真正的病根

用户反馈「没有丝毫变化」，根因**不在**菜单表，而在：

```js
// ruoyi-ui/src/store/modules/permission.js
const sidebar = isInternRole(rootGetters.roles)
  ? buildInternSidebar()        // ← 旧实现直接 return constantRoutes.slice()
  : constantRoutes.concat(sidebarRoutes)
```

旧 `buildInternSidebar()` 返回 `constantRoutes`，而 `constantRoutes` 里实习生可见的只有两项：
**工作台（`/index`）+ 消息中心（`/messages`）**。

→ 也就是说 **实习生侧栏根本不读 `sys_menu`**。所以之前无论怎么改菜单表、跑多少次迁移脚本，侧栏都纹丝不动——这就是「没有丝毫变化」的全部原因。

### 2.2 改动清单（6 个文件，全是前端）

| 文件 | 类型 | 改动 |
|---|---|---|
| `src/utils/internTabs.js` | **新增** | 页签单一数据源：5 个页签定义 + 角色裁剪 + 正式实习生拦路径 |
| `src/store/modules/permission.js` | 改 | `buildInternSidebar(roles)` 重写：只产「工作台 + 学习与考核（含 5 页签）」，消息中心移出一级目录 |
| `src/permission.js` | 改 | `formalInternBlockedPaths` 追加 `/learning/guide`、`/learning/mock`（页签化后旧前缀拦不到） |
| `src/views/assessment/learning/shell.vue` | 改 | 页签改为 `computed`，按角色裁剪 |
| `src/layout/components/Navbar.vue` | 改 | 顶栏新增消息铃铛（**仅实习生角色可见**） |
| （上一轮已建）`views/assessment/guide/index.vue`、`views/assessment/result/index.vue`、`views/assessment/learning/shell.vue`、`router/index.js` | 已建 | 备考资料页、正式考核成绩页、页签壳、5 条子路由 + 旧路径 redirect 兼容 |

### 2.3 改版后侧栏（实习生视角）

```
工作台                    → /index                        （未动，保留 affix）
学习与考核 ▾              → /assessment/intern/learning/courses
  ├ 在线学习              → /assessment/intern/learning/courses   ← 已接后端，仅换入口
  ├ 备考资料              → /assessment/intern/learning/guide     ← 示例数据
  ├ 模拟考核              → /assessment/intern/learning/mock      ← 已接后端
  ├ 正式考核              → /assessment/intern/learning/exam      ← 已接后端
  └ 正式考核成绩          → /assessment/intern/learning/result    ← 真实成绩 + 示例统计
（消息中心 → 顶栏铃铛）
```

正式实习生自动只看到：在线学习 / 正式考核 / 正式考核成绩（备考、模拟被裁剪 + 路由层拦截）。

### 2.4 安全性确认

- 所有 URL 保持 `/assessment/intern/**` 不变 → **9 处硬编码跳转点零改动**（`views/index.vue` 6 处、`learning/index.vue:186`、`detail.vue:483`、`assessment/index.vue:297`）
- 旧路径 `/assessment/intern/study-guide`、`/mock-exam` 保留 redirect 兼容（`mock-exam` 用函数式 redirect 保住 `?start=1`）
- 未新建任何 `views/**` 子目录（避免 webpack 目录缓存导致的 `Failed to compile`）
- 生产构建 `npm run build:prod` **通过**；dev server 已重编译，实测已下发新代码

---

## 三、考核模块后端待完善清单 + 解决办法

> 以下全部遵循「**只新增、不改动**」原则。风险评估按「是否会影响现有已跑通功能」判定。

### P0-1　薄弱模块分析：`knowledge_point` 断层

**问题**：`question.knowledge_point` 有值（17 类知识点，Docker 15 / 平台 15 / DIX脚本 12 / nginx 12 …），但
- `practice_record_item` 落库时**没快照 knowledge_point**；
- `/business/practice/record/{id}` 返回的明细里没有知识点；
- 实习生账号调 `/business/question/list` 是 **403**（`business:question:list` 未授予 PRE_TRAINEE）。

**方案 A（零后端改动，今天就能用）**
前端用已有的 `/business/question/preview/{bankId}?limit=N` 建立 `questionId → knowledge_point` 映射，再和 `practice/record` 的 `questionId + isCorrect` 做 join，前端算薄弱模块。
- ✅ 立即可用（PRE_TRAINEE 有 `business:bank:list`；`previewQuestions` 只清空 `answer`/`analysis`，**`knowledge_point` 原样返回**）
- ℹ️ 该 API 目前**没有任何页面在调用**（`previewQuestions` 只在 `api/business/question.js` 里导出过，是死代码），所以方案 A 会是它的第一个消费方，不存在改动别人的风险
- ⚠️ 隐患：`selectQuestionsForExam` 是 `ORDER BY RAND() LIMIT #{limit}`，**limit 由前端传且服务端无上限** → 传 `limit=9999` 就等于把整个题库（题干+选项）拉走。属于既有的越权风险，见 **S-1**。

**方案 B（推荐，增量接口，不动现有接口）**
新增一个只读聚合接口：

```java
/** 实习生的知识点掌握度（用于薄弱模块分析） */
@PreAuthorize("@ss.hasAnyRoles('PRE_TRAINEE,FORMAL_TRAINEE')")
@GetMapping("/business/practice/analysis")
public AjaxResult analysis() { ... }
```

一条 SQL 就够（`question` 里 `knowledge_point` 现成）：

```sql
SELECT q.knowledge_point AS knowledgePoint,
       COUNT(*)                                        AS total,
       SUM(CASE WHEN i.is_correct = 1 THEN 1 ELSE 0 END) AS correct
FROM practice_record_item i
JOIN practice_record r ON r.id = i.record_id AND r.user_id = #{userId}
JOIN question q        ON q.id = i.question_id
GROUP BY q.knowledge_point
```

**方案 B+（强烈建议，1 列 DDL + 1 行代码）**
给 `practice_record_item` 加 `knowledge_point varchar(64)`（可空），交卷时一起快照。
理由：该表的设计初衷就是「**快照**，保证题目后续被修改或删除后历史记录仍可完整回看」（见 `PracticeRecordItem` 类注释）。现在只快照了题干/选项/解析，漏了知识点，与自己的设计原则不一致。
- 影响面：响应结构不变 → **对现有前端零影响**
- 附带收益：`knowledge_point` 唯一脏数据 `'7'` 可以在快照时归一到「未分类」

> **建议**：今天先用方案 A 把版面点亮，方案 B/B+ 排进下一轮后端迭代。

---

### P0-2　正式考核资格校验缺失（`exam_rule_snapshot` 空转）

**问题**：`startExam` 只校验角色/部门/发布状态/是否考过。`exam_rule_snapshot` 里这些字段一个都没用：

| 字段 | 应有语义 | 现状 |
|---|---|---|
| `need_course_done` + `course_done_threshold` | 学习完成率 ≥ 70% 才解锁正式考核 | 未校验 |
| `protocol_required` | 协议必签 | 仅前端 `permission.js` 拦（未签就重定向 `/index`），后端无校验 |
| `retake_count` | 补考次数 | 未支持（`startExam` 直接「不能重复参加」） |
| `theory_weight` / `practice_weight` / `theory_pass_line` / `practice_pass_line` | 综合分与单项线 | 未使用，目前只看 `exam.pass_line` |

**方案（增量，兼容旧数据）**
在 `startExam` 里**增补**读 `exam_rule_snapshot`：**查不到快照行 → 维持现状不拦截**（老考试数据一行快照都没有，这样零回归）；查到才按规则拦。

```java
ExamRuleSnapshot rule = examRuleSnapshotMapper.selectByExamId(examId); // 新增 Mapper
if (rule != null) {
    if (isTrue(rule.getProtocolRequired()) && protocolStatus != 1) throw new ServiceException("请先签署保密协议");
    if (isTrue(rule.getNeedCourseDone())) {
        BigDecimal rate = learningService.completionRate(userId);        // 复用现有能力
        if (rate == null || rate.compareTo(rule.getCourseDoneThreshold()) < 0) {
            throw new ServiceException("学习完成率需达到 " + rule.getCourseDoneThreshold() + "% 才能参加正式考核");
        }
    }
    // 重复参加 → 改为「按 retake_count 判断是否还有补考机会」
}
```

同时管理端「考核配置与发布」需要一个写接口把规则落进快照表（`freeze_time` 就是发布时冻结的那一下）。

> **风险**：低。`exam_rule_snapshot` 现在 0 行，对现行流程是**完全无副作用**的开关。

---

### P1-3　成绩总览 / 部门对比：`assessment_result` 从未写入

**问题**：`publishResult` 只更新 `answer_sheet`。设计稿的「结果总览 + 个人/部门对比 + 明细表」要的是一张按 `exam_id + user_id` 聚合的表，现在没有。

**方案（二选一，推荐先 A 后 B）**

- **A. 只读聚合接口（推荐先做）**：不改任何写逻辑，直接查询聚合
  ```
  GET /business/answer-sheet/summary?examId=
  → { myScore, deptAvg, deptMax, deptMin, rank, participants, passRate }
  ```
  基于 `answer_sheet`（`final_score`、`dept_id` 从 `exam` 取）+ 部门范围（`currentScopeDeptId()`）。
  口径按设计稿要求：**只统计正式考核**（`sheet_type='FORMAL'`），部门平均分只在本部门范围内算。
  - ✅ 零写入风险，今天就能让「部门平均分」从示例数据换成真数据

- **B. 补 `assessment_result` 形成**：在 `publishResult` 末尾按 `theory_sheet_id` (+`practice_sub_id`) 合成 `total_score = theory×theory_weight + practice×practice_weight` 落库。
  - 收益：一次算好，后续画像/证书都从它取，避免每次聚合
  - ⚠️ 属于写逻辑变更 → 建议等 P0-2 的规则快照一起做，并做好 `publish_result` 的重入幂等

---

### P1-4　备考资料 `material`（0 行、无 Java 层）

**方案**：新增只读接口，管理端先能手填/上传，实习生端消费
```
GET /business/material/published            → 按当前用户的 dept_position 命中的岗位过滤
GET /business/material/{id}                 → 详情（含 file_url 供 PDF 预览）
```
表结构已完备（`material_type` 已是 DOCUMENT/VIDEO/PRACTICE、`status` 已是 DRAFT/PUBLISHED/DISABLED、带 `valid_from/valid_to` 有效期）。
**在接口就绪前**：前端 `views/assessment/guide/index.vue` 已用 `DEMO_MATERIALS` 撑版面并打「示例数据」标签 ✅

---

### P1-5　电子证书 `certificate`（0 行、无 Java 层）

**方案**：新增 `GET /business/certificate/my`（按 `user_id` 返回 `cert_no / template_url / generate_time`）。
证书生成动作挂在「转正审批通过」之后（`promotion_application` → `certificate`），属后续审批流的活。
**接口就绪前**：`result/index.vue` 的 `DEMO_CERT` 占位 ✅

---

### P2-6　消息通知 `notification`（0 行、无 Java 层）

**方案**：新增 3 个只读+轻写接口
```
GET  /business/notification/list        → 当前用户，按 read_status 筛
GET  /business/notification/unread-count → 顶栏铃铛角标
PUT  /business/notification/read/{id}
```
顶栏铃铛**已经做好了**（本次改版，仅实习生可见，指向 `/messages`），角标接上 unread-count 即可。

---

### P2-7　能力画像（`profile_snapshot` + `ability_dimension` + `stage_evaluation`，全 0 行、无 Java 层）

**方案**：`ability_dimension` 先按岗位灌 4 个维度（学习投入 / 理论掌握 / 实践能力 / 规范遵从），再只读接口：
```
GET /business/portrait/my?period=
GET /business/portrait/dimensions
```
**两条硬约束**（记忆里的业务规则，务必落到 SQL）：
1. **未测评维度一律返回 `null` 显示 `--`，不得用 0 分占位**
2. **`stage_evaluation.visible_scope='ADMIN_ONLY'` 的评价必须后端过滤**，不能出现在实习生端

---

### S-1　安全：`/business/question/preview/{bankId}` 的 limit 无上限

**问题**：`QuestionServiceImpl.previewQuestions` 里 `limit` 直通 SQL 的 `LIMIT #{limit}`，服务端不设上限。任何 PRE_TRAINEE 传 `limit=9999` 即可把整个题库的题干与选项拉走（答案/解析被清空，但题目本身泄漏，影响考核公平性）。

**方案**（2 行，改动极小）：
```java
int size = (limit == null || limit <= 0) ? 10 : Math.min(limit, 30);
```
- `previewQuestions` 这个前端 API 方法**目前没有任何页面调用**（只在 `api/business/question.js` 导出过，属死代码）→ 加 limit 上限对现有功能**零影响**，可与 P0-1 方案 B 同批上线
- ⚠️ 注意：这会同时关掉 P0-1「方案 A」那条捷径（想拉全题库建映射就拉不到了）——所以 **S-1 与 P0-1 方案 B 应当同批上线**，顺序是先上 B 再上 S-1。

---

## 四、建议落地顺序

| 批次 | 内容 | 影响现有功能？ | 前端改动 |
|---|---|---|---|
| 本批（已完成） | 侧栏两级化 + 5 页签 + 备考资料页 + 正式考核成绩页；未接后端的用示例数据 | 否 | 已完成 |
| 第 1 批 | P0-1 方案 A（前端算薄弱模块）+ P1-3 方案 A（成绩聚合只读接口） | 否 | 小 |
| 第 2 批 | P0-2 规则快照资格校验 + S-1 limit 上限 + P0-1 方案 B/B+ | 否（快照 0 行，开关式生效） | 无 |
| 第 3 批 | P1-4 备考资料、P1-5 证书、P2-6 通知 | 否（纯新增表 + 新接口） | 换掉示例数据 |
| 第 4 批 | P2-7 能力画像 + 转正审批流 + `assessment_result` 合成 | 否（新表新接口） | 工作台区块 |

---

## 五、顺带发现的数据质量问题

1. `question.knowledge_point` 有 1 条脏值 `'7'`（其余 16 类都是正常知识点名）→ 建议归一到「未分类」。
2. **3 个部门题库为空**：交付 / 质检 / 建模 0 题 → 这三个部门的实习生 `practice/start` 会直接抛「模拟题库暂无题目」。其中 `test_impl_intern`（交付）是 **FORMAL_TRAINEE**，走不到模拟考核，暂时不暴露；但 `test_qa_intern`、`test_model_intern` 是 PRE，一测就报错。**建议演示前先给这三个部门各补 10 道题。**
3. `question_bank.question_count` 冗余计数与实际题量不同步（bank 5 实际 49 题、bank 12 实际 48 题，字段都是 0）。
4. 测试残留：`exam` 2 行（`exam_name` = `'m'` / `'l'`，均为 PUBLISHED）、`practice_subject` 1 行（dept 105，`content='fty'`）——演示时会被看到，建议清理。
5. `exam.exam_type` 与 `exam_mode` 两个字段语义重叠（都是「模拟/正式」），目前代码用 `exam_type`。建议明确其中一个为唯一口径。
