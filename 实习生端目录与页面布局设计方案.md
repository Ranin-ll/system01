# 实习生端目录与页面布局设计方案

> 版本 **V1.1**　2026-09-17（V1.0 → V1.1：视觉稿与部门管理员端合并；修正 §四/§五 两处与实现不符的写法；补齐 §6.4 实操题库闭环）
> 实例基线：**开发部门 · 开发实习生**（`test_dev_intern`，PRE_TRAINEE，导师 李导师）
> 配套视觉稿：**`实习生与部门管理员端整体设计稿.html`**（双角色合一，顶部「实习生端 / 部门管理员端」切换）
> 姊妹文档：`部门管理员端目录与页面布局设计方案.md`（V1.1）、`后端实现现状盘点与考核补全方案.md`、`模拟考核与正式考核模块设计.md`
> 范围：**实习生端**（PRE_TRAINEE / FORMAL_TRAINEE）。管理端的对接面（发布 → 消费）见姊妹文档，两文互相引用。

---

## 修订摘要（V1.0 → V1.1，2026-09-17）

本版**不是加功能，而是把设计稿与已落地的代码对齐**。V1.0 里有 2 处写法与实现不符、1 处把「已实现」标成了「待实现」，本版逐条修正。

| # | 项 | V1.0 的写法 | V1.1 修正为 | 依据 |
|---|---|---|---|---|
| 1 | 配套视觉稿 | `实习生端整体设计稿.html`（单角色） | **`实习生与部门管理员端整体设计稿.html`**（两角色合一，顶部切换；CSS 按 `.ri` / `.rd` 作用域隔离，避免 66 处同名选择器互相覆盖） | 用户 2026-09-17 要求把两角色设计稿「整合统一」 |
| 2 | **侧栏改造机制** | §五：改 `sys_menu` 表 | **改前端 `store/modules/permission.js` 的 `buildInternSidebar()`** —— 实习生侧栏**不读 `sys_menu`**，改表零效果 | 实测：菜单迁移执行成功但侧栏无任何变化，根因即此 |
| 3 | 路由前缀 | `/intern/*` | **`/assessment/intern/*`**（工作台仍复用 `/index`） | `router/index.js` 的实际实现 |
| 4 | §6.4 实操题库 | 「骨架，待实现」 | **已接后端闭环**（表字段扩展 + 前端列表/详情 + 种子数据；Controller / Service 签名与权限注解未动） | 2026-09-17 完成并端到端验证通过 |
| 5 | 范围 | 「部门管理员端不在本次调整范围」 | 视觉稿已合并；**管理端对接面沿用姊妹文档**（§2.7 实操题库、§2.8 正式考核） | — |

> ⚠️ **第 2 条是最容易白干的坑**：V1.0 §五 给的菜单表改造方案**完全无效**（照做不会产生任何可见变化）。§五 已改写。
> ⚠️ **第 4 条是本版唯一「后端动了」的地方**，且只做了**扩展式**改动（加列 / 加 Mapper 方法 / 加内部归一化），**既有接口签名、返回结构、权限注解一律未改**。

## 零、系统组织与岗位（实例基线）

设计稿的示例数据取自**系统真实组织数据**（来源：`sys_dept` / `position` / `dept_position` / `sys_user`）。

系统共 **5 个业务部门**、**5 种实习生岗位**，部门与岗位**一一绑定**，同属「深圳总公司」(dept_id 101)：

| 部门 (dept_id) | 绑定岗位 (position_id) | 岗位编码 | 测试实习生账号 |
|---|---|---|---|
| 交付部门 (103) | 实施实习生 (1) | IMPLEMENTATION | `test_impl_intern`（正式） |
| **开发部门 (104)** | **开发实习生 (2)** | DEVELOPMENT | `test_dev_intern`（预备 · 导师 李导师） |
| 设计部门 (105) | 设计实习生 (3) | DESIGN | `test_design_intern`（预备） |
| 质检部门 (106) | 质检实习生 (4) | QA | `test_qa_intern`（预备） |
| 建模部门 (107) | 建模实习生 (5) | MODELING | `test_model_intern`（预备 · 导师 陈导师） |

业务角色 4 个：`SUPER_ADMIN`（全局只读统计）/ `DEPT_ADMIN`（本部门可写）/ `PRE_TRAINEE` / `FORMAL_TRAINEE`（仅本人）。

**本方案与设计稿统一以「开发部门 · 开发实习生」为示例实例**——研发培养链路最完整、最有代表性；换成其余四个岗位只需替换展示文案，页面结构与权限逻辑完全一致。

> 数据说明：部门 / 岗位 / 绑定关系 / 账号为**真实数据**；课程与题目中除真实课「开发流程与代码交付规范」（THEORY · 必修 · 已发布）、「开发工具与项目环境」（PRACTICE · 选修 · 已发布）外，其余课程名与题目为**示例数据**，仅用于撑起版面密度。

---

## 一、设计目标与三条原则

当前实习生端的问题是**入口太散**：侧栏虽有 10 个菜单，但只有「实习生工作台 / 消息中心」可见，其余 8 个（学习中心、考试中心、实践考核提交、任务中心、考核成绩、能力画像、协议与证书、个人信息）全部隐藏，靠工作台上一堆入口按钮下钻。结果是：工作台既要做决策又要做操作，页面越堆越长，实习生也说不清"下一步该点哪"。

改造遵循三条原则：

1. **一级目录按用户心智划分，不按系统模块划分。**
   实习生脑子里只有两件事：*我现在该干嘛*（工作台）和 *我要去完成的事*（学习与考核）。而不是"课程模块/考试模块/成绩模块"。
2. **工作台 = 数据分析与可视化中心，操作动作下沉。**
   工作台回答三个问题：我是谁、我的数据表现如何、下一步点哪。所有**统计分析与能力画像**都集中在工作台（KPI、趋势、分布、画像雷达）；而具体的**学习与答题动作**下沉到「学习与考核」。这样工作台不会随业务膨胀，但分析能力完整保留。
3. **列表页带页签，任务页全屏。**
   浏览型页面（课程列表、成绩列表）保留侧栏与页签，方便横向切换；专注型页面（看课程、答题、交卷）隐藏侧栏与页签，减少干扰。

---

## 二、一级目录设计

侧栏只有两项：

| 一级目录 | 路由 | 角色 | 说明 |
|---|---|---|---|
| 工作台 | `/index` | 预备 / 正式实习生 | 单页，不设页签 |
| 学习与考核 | `/assessment/intern/learning` | 预备 / 正式实习生 | 顶部页签容器，5 个子页 |

**为什么「学习与考核」内部用页签而不是二级侧栏菜单：**

- 实习生只有一个一级业务域，撑不起二级侧栏，展开后会显得空旷失衡；
- 学习 → 练习 → 考试 → 看成绩是一条线性心智路径，页签能横向切换、无跳页感；
- 页签栏占用横向空间，正好填补两项一级目录造成的侧栏留白。

**常驻元素（不再占用一级目录）：**

- 顶栏右上角：消息铃铛（未读角标）+ 头像下拉菜单（个人中心 / 退出登录）
- 这样「消息中心」「个人信息」从侧栏移除，仍可随时到达。

---

## 三、「学习与考核」五个页签

**页签的单一数据源**：`ruoyi-ui/src/utils/internTabs.js` 的 `INTERN_TABS` —— 左侧栏（`store/modules/permission.js`）与页签壳（`views/assessment/learning/shell.vue`）都从这里取，**避免两处各写一份走偏**。正式实习生按 `FORMAL_INTERN_TAB_NAMES` 关掉「备考资料 / 模拟考核」两页签。

| 页签 | 路由（实际实现） | 内容 | 状态 |
|---|---|---|---|
| 在线学习 | `/assessment/intern/learning/courses` | 岗位培养概览 + 课程目录（已接后端） | ✅ **已实现**，仅换入口 + 样式重排 |
| 备考资料 | `/assessment/intern/learning/guide` | 部门管理员上传的考试指南（PDF）下载与预览 | 骨架，待后端（`material` 零 Java 层） |
| 模拟考核 | `/assessment/intern/learning/mock` | **顶部模拟统计** + **理论（题库随机抽 10 题）** + **实操题库（按方向分组卡片网格）** | 理论骨架 / **实操题库已达闭环**（见 §6.4） |
| 正式考核 | `/assessment/intern/learning/exam` | 部门管理员发布的正式考核：备考资料 + 理论考试 + 实操考试 + 资格校验 | 骨架，待后端 |
| 考核成绩与转正申请 | `/assessment/intern/learning/result` | **考核成绩 + 转正申请**：结果总览 + 明细（按场次/环节）+ 对比 + 薄弱模块 + **转正申请（资格清单 + 状态时间线 + 申请表单）** + 电子证书 | 成绩全真实；转正申请为演示态 |

> **旧路径兼容**（勿删，已有跳转点依赖）：`/assessment/intern/study-guide` 与 `/assessment/intern/mock-exam` 均为 `redirect`；其中 `mock-exam` 用**函数式 redirect 保留 query**——`practice/record.vue` 的「再练一次」靠 `?start=1` 触发自动抽题。


> **内容来源（重要）**：正式考核的**备考资料、理论考试配置、实操考试题目，全部由对应部门管理员在管理端配置并发布**（参考管理端「考核配置与发布」页：备考资料 / 理论考试 / 实操考试三段 + 底部「保存并发布」）。
> 实习生端 `正式考核` 页是**只读消费端**：读取已发布版本的考试信息与题目列表，按完成率 / 协议 / 次数做资格校验后开放。发布后在实习生端生效（管理端底部提示"发布后在实习生考核认证页面生效"）。

**下钻页（`/assessment/intern` 的直接子路由，`hidden: true`）：**

全部**保留侧栏、不套页签壳**（页签壳 `shell.vue` 只包裹 `learning/*` 五个页签子路由，下钻页是它的**兄弟节点**），靠页面自身的面包屑往上返回；`meta.activeMenu` 统一指回 `/assessment/intern/learning`，保证侧栏高亮不丢。

| 页面 | 路由（实际实现） | 说明 | 状态 |
|---|---|---|---|
| 课程学习页 | `/assessment/intern/learning/course/:courseId` | 面包屑 + Hero + 页签 + 左目录 + 右正文 + 底部操作条 | ✅ 已实现（`learning/detail.vue`） |
| 模拟考核回顾 | `/assessment/intern/mock-exam/record/:recordId` | 当次题目与作答回顾 | ✅ 已实现（`practice/record.vue`） |
| **实操练习列表** | `/assessment/intern/practice-subject` | 实操题库：hero 统计 + 方向分组卡片网格 | ✅ **已实现（本次闭环）** |
| **实操题详情** | `/assessment/intern/practice-subject/:id` | 左参考图 + 缩略图组，右交付要求 / 开发约束 / 提交格式 / 命名规则 / 下载附件 | ✅ **已实现（本次闭环）** |
| 答题页 | `/assessment/intern/learning/exam/taking/:examId` | 顶部计时与题号卡 + 题干 + 底部导航 | 待建 |
| 实操提交页 | `/assessment/intern/learning/exam/submit/:taskId` | 拖拽上传 + 提交要求 + 评分项预览 | 待建 |
| 证书详情页 | `/assessment/intern/learning/result/certificate/:id` | 证书图 + 下载 | 待建 |
| 能力画像详情页 | `/assessment/intern/dashboard/portrait/:period` | 工作台画像区块下钻：多周期对比 + 维度明细 + 评价原文 | 待建 |


---

## 四、路由结构（以**实际实现**为准，可直接复制）

实习生端路由全部落在 `router/index.js` 的 `dynamicRoutes` 里（**前端硬编码，不走后端菜单**）。工作台复用 `constantRoutes` 的 `/index`，因此这里只声明 `/assessment/intern` 这一段。

```js
// router/index.js → dynamicRoutes
{
  path: '/assessment/intern',
  component: Layout,
  hidden: true,                                  // 不进侧栏，侧栏由 buildInternSidebar() 硬编码
  roles: ['PRE_TRAINEE', 'FORMAL_TRAINEE'],      // 只有实习生角色能加载
  children: [
    {
      // 「学习与考核」页签壳：承载 5 个页签子路由
      path: 'learning',
      component: () => import('@/views/assessment/learning/shell'),
      name: 'InternLearningShell',
      redirect: '/assessment/intern/learning/courses',
      meta: { title: '学习与考核', activeMenu: '/assessment/intern/learning' },
      children: [
        { path: 'courses', name: 'InternLearning',     component: () => import('@/views/assessment/learning/index'), meta: { title: '在线学习',     activeMenu: '/assessment/intern/learning', tab: 'InternLearning' } },
        { path: 'guide',   name: 'InternGuide',        component: () => import('@/views/assessment/guide/index'),    meta: { title: '备考资料',     activeMenu: '/assessment/intern/learning', tab: 'InternGuide' } },
        { path: 'mock',    name: 'InternMockExam',     component: () => import('@/views/assessment/practice/index'), meta: { title: '模拟考核',     activeMenu: '/assessment/intern/learning', tab: 'InternMockExam' } },
        { path: 'exam',    name: 'InternLearningExam', component: () => import('@/views/assessment/exam/index'),     meta: { title: '正式考核',     activeMenu: '/assessment/intern/learning', tab: 'InternLearningExam' } },
        { path: 'result',  name: 'InternResult',       component: () => import('@/views/assessment/result/index'),   meta: { title: '考核成绩与转正申请', activeMenu: '/assessment/intern/learning', tab: 'InternResult' } }
      ]
    },

    /* ---- 以下为下钻页：与 shell 平级，保留侧栏、不套页签壳 ---- */
    { path: 'learning/course/:courseId',  name: 'InternLearningCourse',     component: () => import('@/views/assessment/learning/detail'),      meta: { title: '课程学习',   activeMenu: '/assessment/intern/learning', tab: 'InternLearning' } },
    { path: 'mock-exam/record/:recordId', name: 'InternMockExamRecord',     component: () => import('@/views/assessment/practice/record'),      meta: { title: '模拟考核回顾', activeMenu: '/assessment/intern/learning', tab: 'InternMockExam' } },
    { path: 'practice-subject',           name: 'InternPracticeSubject',    component: () => import('@/views/assessment/practice/subject'),     meta: { title: '实操练习',   activeMenu: '/assessment/intern/learning', tab: 'InternMockExam' } },
    { path: 'practice-subject/:id(\\d+)', name: 'InternPracticeSubjectDetail', component: () => import('@/views/assessment/practice/subjectDetail'), meta: { title: '实操题详情', activeMenu: '/assessment/intern/learning', tab: 'InternMockExam' } },

    /* ---- 旧路径兼容（勿删，已有跳转点依赖）---- */
    { path: 'study-guide', redirect: '/assessment/intern/learning/guide' },
    { path: 'mock-exam',   redirect: to => ({ path: '/assessment/intern/learning/mock', query: to.query }) }  // 函数式：保留 ?start=1
  ]
}
```

**四条实现约束（踩过坑，务必保持）：**

1. **`meta.tab` 是页签高亮的唯一依据**。`shell.vue` 的 `activeTab` = `this.$route.meta.tab || this.$route.name`，因为 `route.name` 在下钻页上是 `InternPracticeSubjectDetail` 这种名字，直接比对不上页签名。**新加页签页/下钻页都要带 `meta.tab`。**
2. **`meta.activeMenu` 必须指回 `/assessment/intern/learning`**，否则侧栏「学习与考核」不亮。
3. **`mock-exam` 的 redirect 必须是函数式**（保留 query）——`record.vue` 的「再练一次」靠 `?start=1` 触发自动抽题，写成静态 `redirect: '...'` 会丢掉 query，功能静默失效。
4. **`practice-subject/:id` 的正则用 `(\\d+)`**，与既有 `mock-exam/record/:recordId(\\d+)` 风格一致，避免非数字路径被吞。

`shell` 组件 = 页头 + `el-tabs` + `<router-view>`，页签切换走 `$router.push`，保证前进后退与刷新都能定位到正确页签。


---

## 五、侧栏与菜单改造（★ 与 V1.0 相反：实习生侧栏**不读 `sys_menu`**）

### 5.1 真正生效的地方：`buildInternSidebar()`

```js
// store/modules/permission.js:45
const sidebar = isInternRole(rootGetters.roles)
  ? buildInternSidebar(roles)                              // ← 实习生走这条，硬编码
  : constantRoutes.concat(sidebarRoutes)                   // ← 管理端才读后端 /getRouters（即 sys_menu）
```

**结论：改 `sys_menu` 对实习生侧栏零效果。** 当前实习生侧栏由 `buildInternSidebar()` 生成：

```
[工作台]                              → /index
[学习与考核] (alwaysShow)             → /assessment/intern/learning
   └ 5 个页签（在线学习 / 备考资料 / 模拟考核 / 正式考核 / 考核成绩与转正申请）
     页签源 = @/utils/internTabs.js（与 learning/shell.vue 同源）
```

要调整实习生侧栏，**只改 `buildInternSidebar()`**（必要时同步 `internTabs.js`），改完刷新即可，**不需要跑任何 SQL**。

### 5.2 `sys_menu` 里那 10 个实习生菜单还有用吗？

**只剩一个用途：生成后端路由（`rewriteRoutes`）与权限标识。** 但实际渲染被 `dynamicRoutes` 覆盖——`dynamicRoutes` 声明在前，**同 path 时 `dynamicRoutes` 赢**，所以 DB 里那些空壳菜单遮不住前端硬编码页。

| 处置 | 菜单 | 做法 | 是否影响实习生侧栏 |
|---|---|---|---|
| 保留 | 工作台 / 学习与考核 | 维持现状（`visible='1'` 隐藏即可） | ❌ 无影响 |
| 保留隐藏 | 学习中心 / 考试中心 / 实践考核提交 / 任务中心 / 考核成绩 / 能力画像 / 协议与证书 / 个人信息 | `visible='1'`，**保留权限标识**（`business:*` 仍在用） | ❌ 无影响 |
| 顶栏入口 | 消息中心 | 已改为顶栏铃铛（`layout/components/Navbar.vue`） | ❌ 无影响 |

> ⚠️ **权限标识不能删**：`sys_menu.perms` 是 `@PreAuthorize("@ss.hasPermi('...')")` 的依据（如实操题库的 `business:bank:list`）。删菜单行会连带**掉权限**。
> 📌 历史脚本 `intern_assessment_intern_navigation.sql`（"只留工作台+消息中心"）**保留不动**即可；它控制的是 DB 侧菜单可见性，不影响实习生侧栏。


---

## 六、页面布局规格

### 6.1 工作台（`/index`）— 数据分析与可视化中心

工作台是全系统的**数据看板**，八个区块承载统计、分析与画像。自上而下排布：

| 区块 | 布局 | 内容 |
|---|---|---|
| 页头 | 左右两端对齐 | 标题 + 说明；右侧「切换周期」「刷新」「消息 N」 |
| ① 身份条 | 单行卡片（紧凑） | 头像 + 姓名/角色/岗位/部门/导师/入职日；右侧四项：保密协议、培养阶段、距考核门槛、数据更新时间 |
| ② KPI 指标 | 6 列等宽 | 必修完成率、累计学习时长、已完成单项、待完成课程、模拟考核正确率、能力综合值 |
| ③ 学习完成率趋势 | 左 8 栏 | 近 6 周折线图 + **考核门槛 70% 虚线**，直观显示"还差多少" |
| ④ 能力画像 | 右 4 栏 | 雷达图（多维度）+ 数据完整度进度条 + 来源明细 |
| ⑤ 课程完成情况 | 左 6 栏 | 每门课完成率横向条形，一眼看出短板 |
| ⑥ 学习时长分布 | 右 6 栏 | 按周/按课程柱状图，反映投入节奏 |
| ⑦ 考核成绩 | 左 6 栏 | 历次成绩柱状条；未考核项显示 `--` 与去向 |
| ⑧ 培养进度 | 右 6 栏 | 协议签署 / 在线学习 / 正式考核 / 转正发证 四步进度 |
| ⑨ 下一步做什么 | 左 8 栏 | 待办清单，每条 = 标题 + 差距说明 + 一个主按钮 |
| ⑩ 通知 | 右 4 栏 | 最近两条通知 + 消息中心入口 |
| ⑪ 协议与证书 | 全宽单行卡片 | 药丸按钮：**保密协议**（已签署 / 待签署）、**电子证书**（已获得 / 未获得）、**签署凭证**（已签署时才出现） |

⑪ 是最底部一行「已签署的协议 / 已获得的证书」入口，视觉规格：

- 容器：白底卡片、`border-radius: 6px`、`padding: 14px 18px`、细边框。
- 左侧标签「协议与证书：」深灰 14px；右侧药丸按钮浅蓝底 `#e8f1fd` + 蓝字 `#1764f5`、`border-radius: 8px`、`padding: 0 16px`、`min-height: 34px`。
- **状态内联在药丸里**：用一个浅色小圆角标签显示「已签署 / 未获得」，不额外占行。
- 三档色调：已获得（浅蓝）/ 待处理（浅橙 `#fff4e5`）/ 未生成（浅灰 `#f2f4f7`）。
- **未生成的记录点击不跳空页**，而是用提示说明原因（如"考核通过并完成转正审批后生成电子证书"）。

设计要点：

- **③ 的趋势图必须带门槛虚线**。这是实习生最关心的问题："我还差多少"，一条虚线比任何文字都直接。
- **④ 能力画像不可省略**（见 6.7）。它是学习、理论、实践、评价四来源合成的结果，是工作台唯一的"综合评价"视觉。
- **所有图表指标都要标注口径**。例如完成率趋势下注明"本人岗位全部已发布必修课程的完成率简单平均，与学习列表页同源"，避免同一指标两处口径不一致。
- **未生成的数据一律显示 `--` 并给出去向**，绝不以 0 占位（如"理论掌握 --"配"考试后生成"）。
- ④ 的能力画像与 ⑨ 的待办应形成联动：待办里可以直接写"实操题提交后实践能力维度将点亮，完整度可由 60% 提升至 80%"，让画像成为行动驱动力。


### 6.2 在线学习（`/assessment/intern/learning/courses`）

> **本页是本次改版的核心：只换入口，页面本体不动。** 原实现是 `hidden: true` 的独立路由，只能从工作台按钮进入；改版后并入一级目录「学习与考核」作为**默认页签**。`views/assessment/learning/index.vue` 的布局与 `/business/learning` 联动逻辑**零改动**，仅调整菜单归属与 `activeMenu`。

| 区块 | 内容（与现有实现一致） |
|---|---|
| 页头 | 小标 LEARNING CENTER + 标题「在线学习」+ 部门/岗位/培养周期描述；右侧「刷新进度」 |
| 页签栏 | 在线学习（当前）/ 备考资料 / 模拟考核 / 正式考核 / 考核成绩与转正申请 |
| 概览条 | 左：岗位培养块（图标 + 当前岗位培养 + 岗位名 + 部门/身份说明）；中：环形必修完成率；右 4 个事实：课程总数、已完成课程、学习中、已完成单项 x/y |
| 门槛提示 | 「完成率达 70% 才解锁正式考核，当前还差 8%」 |
| 课程目录头 | 序号 01 + 「课程目录」+ 说明；右上角「N 门课程」 |
| 筛选条 | 左：全部课程 / 学习中 / 已完成 分段；右：课程类型下拉（全部 / 文档理论 / 视频实操） |
| 课程列表 | 每行：类型图标（理论绿 / 实操蓝）+ 类型小标 + 课程名 + 必修/选修标 + 简介 + 元信息（章节数·时长·单项完成）+ 进度条与百分比 + 最近学习 + 右侧状态标 + 主操作按钮 |
| 主操作文案 | 按状态变化：开始学习 / 继续学习 / 回看课程 |
| 底部提示条 | 「学习记录自动保存」说明 + 「查看学习规则」 |
| 空状态 | 暂无符合条件的课程（课程发布后按岗位自动出现） |

### 6.3 课程学习页（`/assessment/intern/learning/course/:courseId`）

> 保持**嵌入 Layout 的普通页**（不改全屏）：面包屑 + Hero + 页签 + 左右两栏，与现有 `detail.vue` 一致。

```
面包屑：‹ 返回在线学习 / 课程名
─────────────────────────────────────────────
Hero 条：类型图标 | 类型小标 + 课程名 + 必修标 + 简介 + 元信息（岗位·章节·时长） | 完成率 55% + 进度条 | 继续学习
─────────────────────────────────────────────
页签条：全部(6) / 文档(3) / 视频(3)                       右：展开全部
─────────────────────────────────────────────
左侧 250px：课程目录（章节折叠，显示 x/y）    右侧：章节/类型 + 当前项标题 + 类型标 + 阈值 + 保存状态
            当前项左侧蓝色竖条高亮                   内容区（PDF / DOCX / PPTX 阅读器 或 视频播放器）+ 本节简介
─────────────────────────────────────────────
底部操作条：上一项 | 本项进度 · 共 N 单项已完成 M 项 | 下一项 | 确认完成阅读
```

要点：
- 文档项：「滚动到末页」才点亮「确认完成阅读」；首次打开需满 10 秒才允许提交（与后端 `MIN_DOCUMENT_OPEN_SECONDS` 一致）。
- 视频项：达到阈值自动完成，底部只保留「上一项 / 下一项」，不显示确认按钮。
- 离开页面前保存最后位置；保存状态常驻显示（"已自动保存 · 42%"）。

### 6.4 模拟考核（`/assessment/intern/learning/mock`）

分三块，自上而下：

**① 模拟统计条（顶部全宽）** —— 让实习生一眼看清自己的自测状态：

| 统计项 | 说明 |
|---|---|
| 累计自测次数 | 理论自测总次数 |
| 理论平均正确率 | 全部自测正确答案数 / 总题数 |
| 理论最高正确率 | 单选一次最高值 |
| 实操题量 | 实操题库题目总数（如 15 题 / 3 个开发方向） |
| 题库覆盖 | 知识模块数 / 题库总题数（如 4 模块 · 120 题） |
| 最近自测 | 时间戳 + 得分率角标 |

修正项：右上角「提分趋势」小折线，反映最近 5 次自测正确率走向。

**② 理论模拟** —— **题库随机抽 10 题**：

- 说明卡：随机抽 10 题、不计成绩、可重复练习；
- 主操作：「开始新自测」；
- 历史记录列表：时间 / 题数 / 正确率 / 用时 + 「查看错题」；
- 交卷后展示对错解析，不写入正式成绩。

**③ 实操题库** —— 就是一份**实操题库**（不随机抽，按开发方向分组的卡片网格）：

- 题头横幅：徽标「模拟试题库」+ 标题「开发模拟试题」+ 说明；右侧三项统计：**可练习题目 / 开发方向 / 最长用时**；
- 按**开发方向分组**，每组一个分组头（组名 + 适用说明 + 题数）：
  - 后端接口开发（接口设计、参数校验、异常处理）5 题
  - 数据访问开发（SQL 编写、索引优化、ORM 映射）5 题
  - 前端页面开发（组件拆分、表单校验、前后端联调）5 题
- 每题一张**试题卡片**：缩略图 + 题目名称 + 页脚「建议用时 xx 分钟」；点击进入详情页。

**实操题库详情页（`/assessment/intern/practice-subject/:id`）** —— 左图右文（**保留侧栏**，靠面包屑「返回实操题库」向上）：

| 区域 | 内容 |
|---|---|
| 左栏 | 参考图大图 + 底部缩略图组（点击切换，当前项加粗蓝框） |
| 右栏 · 头部 | 题目名称（如「用户登录接口开发」） |
| 右栏 · 信息格 | 试题类型（后端接口 / 数据访问 / 前端页面）/ 建议用时（两格并排） |
| 右栏 · 交付要求 | 要点列表（如：完成登录接口与参数校验 / 梳理异常处理与统一返回结构 / 提交可运行的代码与说明文档） |
| 右栏 · 开发约束 | 要点列表（如：接口符合 RESTful 命名规范 / 参数校验完整、返回结构统一） |
| 右栏 · 提交格式 | 单独信息框（如 `.java 源码 + SQL 脚本 + .zip`） |
| 右栏 · 命名规则 | 单独信息框（如 `MOCK_API-1_用户登录接口开发_v01.zip`） |
| 右栏 · 底部 | 主按钮「下载附件」（接口文档与数据表结构）；注明练习成果仅供自检、不计入正式成绩 |

> 设计要点：**理论随机抽、实操题库直看**——理论考的是知识覆盖，随机抽才有练习价值；实操考的是任务完成质量，题目本身固定，就是一份可浏览的题库：**按方向分组 + 卡片网格 + 左图右文详情页**，不做练习次数/提交状态这类统计。
> 主题约束：卡片沿用系统既有的浅色扁平风格（白底 + 1px 细边框 + 圆角 10px + 蓝主色），缩略图用系统同色系的扁平占位图，不引入照片、阴影或渐变。

#### 6.4.1 实操题库 · 已落地实现（闭环说明，2026-09-17）

③ 这一块**已经从「骨架」变成真后端**：实习生看到的题、方向、交付要求、参考图全部来自 `practice_subject` 表，由部门管理员在管理端维护（见姊妹文档 §2.7 页签 3），**同一张表、同一套字段**。

**① 数据模型 —— `practice_subject` 扩展后的 20 列**

V1.0 时该表只有 `id / dept_id / content / attachments_json / status / create_by / create_time / update_time / deleted`，**装不下一张试题卡片**。本次做了**幂等迁移**补齐 11 列：

| 列 | 类型 | 界面位置 | 说明 |
|---|---|---|---|
| `title` | varchar(200) | 卡片标题 / 详情页 H1 | 题名；为空时按题干前 40 字兜底 |
| `direction` | varchar(64) | 分组头 + 卡片角标 | **分组依据**（同名即同组）；自由填写，空则归「通用」 |
| `direction_desc` | varchar(255) | 分组头副标题 | 如「接口设计、参数校验、异常处理」 |
| `difficulty` | varchar(16) | 卡片标记 | `EASY` / `MEDIUM` / `HARD`，空默认 `MEDIUM` |
| `estimated_minutes` | int | 卡片页脚「建议用时 xx 分钟」/ hero「最长用时」 | 空则前端不显示 |
| `deliverables` | text | 详情页「交付要求」列表 | **按 `\n` 分行**渲染成要点列表 |
| `dev_constraints` | text | 详情页「开发约束」列表 | 同上，按 `\n` 分行 |
| `submit_format` | varchar(255) | 详情页「提交格式」信息框 | 如 `.java 源码 + SQL 脚本 + .zip` |
| `naming_rule` | varchar(255) | 详情页「命名规则」信息框 | 如 `MOCK_API-1_用户登录接口开发_v01.zip` |
| `reference_images` | text | 卡片缩略图 + 详情页大图/缩略图组 | **JSON 数组**（`[{name,url}]`），空则用占位图 |
| `sort_no` | int | 组内排序 | 越小越前，空默认 0 |

另加索引 `idx_psubject_dir(dept_id, direction, status)`，支撑「按部门 + 方向」取题。

> ⚠️ **列名避坑**：`constraints` 是 MySQL 保留字，列名取 **`dev_constraints`**，不是 `constraints`。

**② 接口 —— 全部沿用既有签名，一个没改**

| 方法 | 路径 | 权限注解 | 谁用 |
|---|---|---|---|
| GET | `/business/practice-subject/published` | `business:bank:list` | **实习生列表页**（只返回本部门 + 已启用） |
| GET | `/business/practice-subject/published/{id}` | `business:bank:list` | **实习生详情页** |
| GET | `/business/practice-subject/list` | `business:psubject:list` | 管理端列表 |
| GET / POST / PUT / DELETE | `/business/practice-subject`(`/{id}`) | `business:psubject:query` / `add` / `edit` / `remove` | 管理端 CRUD |
| PUT | `/business/practice-subject/status/{id}?status=` | `business:psubject:edit` | 启用 / 停用 |

**本次后端改动严格限定在「扩展」层面**：`PracticeSubject` 实体加字段、Mapper XML 的 `resultMap` / `subjectColumns` / `insert` / `update` 覆盖新列、`selectSubjectList` 增加 direction·difficulty·keyword 条件与 `status DESC, direction, sort_no, id` 排序、新增 `selectDirectionsByDept`（向前兼容，暂无人调用）；Service 只加**内部**的 `normalizeForWrite()` / `trimAll()` 归一化。**Controller 与 Service 的方法签名、返回结构、权限注解一律未动。**

**③ 可见性（后端强制，前端隐藏不作数）**

- 实习生侧：`selectPublishedForIntern()` 取当前登录人 `dept_id`，SQL 条件 `deleted=0 AND status=1 AND dept_id=?`，**跨部门拿不到**；
- 详情页对「不存在 / 已删除 / 非本部门 / 未启用」**统一返回同一句**「实操题不存在或未发布」，不通过报错文案泄露他部门题目的存在（已实测）；
- 管理端：部门管理员只能看/改本部门（超管全局只读统计）。

**④ 排序与分组**

- 实习生列表：`ORDER BY direction ASC, sort_no ASC, id ASC` → 前端按 `direction` **顺序分组**，组内按 `sort_no` 排列；
- 封面图：取 `reference_images[0]`，无则渲染内置扁平占位 SVG（不引入外链图片、不影响离线演示）。

**⑤ 归一化兜底（写入时，避免"卡片没标题"这类脏数据）**

| 字段 | 为空时的处理 |
|---|---|
| `title` | 取 `content` 前 40 字 |
| `direction` | `通用` |
| `direction_desc` | `本方向实操练习题` |
| `difficulty` | `MEDIUM` |
| `sort_no` | `0` |
| 编辑时 `title` 传**空串** | 视为「未修改」，置 `null` 跳过更新，**不覆盖已有题名** |

**⑥ 涉及文件**

| 层 | 文件 |
|---|---|
| 迁移（幂等） | `ruoyi-vue/sql/intern_assessment_practice_subject_ext.sql` |
| 演示数据 | `ruoyi-vue/sql/intern_assessment_practice_subject_seed.sql`（开发部门 3 方向 × 5 题） |
| 迁移前备份 | `ruoyi-vue/sql/_backup_before_practice_subject_ext.sql` |
| 实体 / Mapper / Service | `ruoyi-business/**/domain/PracticeSubject.java`、`resources/mapper/business/PracticeSubjectMapper.xml`、`service/impl/PracticeSubjectServiceImpl.java` |
| 前端接口 | `ruoyi-ui/src/api/business/practice.js`（实习生读）、`api/business/practiceSubject.js`（管理端写） |
| 实习生页面 | `views/assessment/practice/subject.vue`（列表）、`views/assessment/practice/subjectDetail.vue`（详情） |
| 管理端页面 | `views/business/practiceSubject/index.vue`（不在 router，走菜单 `component` 动态加载） |

**⑦ 与「正式考核 · 实操环节」的关系**

两处**共用同一张表**：管理端页面上标注「本库同时供模拟实操练习与正式考核实操环节引用」，避免管理员重复录题。区别只在消费方式——模拟是**只读浏览**（不提交、不计分），正式考核才走 `subject_answer` 上传答案文件包 → 管理员下载后人工评分（见姊妹文档 §2.9）。


### 6.5 正式考核（`/assessment/intern/learning/exam`）—— 只读消费部门管理员发布的内容

正式考核**不由实习生创建、也不由超管创建**，而是**由对应部门管理员配置并发布**。实习生端页面自上而下：

| 区块 | 内容 |
|---|---|
| 发布信息条 | 发布部门 / 发布人（部门管理员）/ 版本号（v1.0）/ 发布时间 / 状态（已发布） |
| ① 备考资料 | 管理部门上传的考试指南（PDF）：文件卡片 + 「文件预览」/「下载」 |
| ② 资格校验卡 | 三条件清单：完成率 ≥ 70%（当前 x%）、保密协议已签署、剩余考试次数；未通过则置灰并写出差距 |
| ③ 理论考试卡 | 考试形式 / 考试时长 / 通过分数-总分 / 考试题型 / 题目数量 / 试题分值 + 知识模块分布（示例：Java 语言与面向对象 / Spring Boot 与接口开发 / MySQL 与数据访问 / 代码规范与分支协作）；「开始考试」 |
| ④ 实操考试卡 | 考试形式 / 考试时长 / 通过分数-总分 / 考试题型 / 题目数量 / 试题分值 + 题目列表；「查看题目」「提交」 |

考前确认：点「开始考试」弹二次确认（含时限、次数消耗、不可暂停提示）。

**题目呈现规则**：

- 理论考试：按部门管理员配置的「试题分布」从题库抽题（知识模块占比 / 单选多选占比），题目数量与分值由配置决定；
- 实操考试：**题目列表直看**（题目序号 / 题目名称 / 题目描述 / 考核要点），与模拟的实操列表同结构；
- 实习生在正式考核页**只能看到题目与要求，看不到标准答案与批阅规则**。

### 6.6 考核成绩与转正申请（`/assessment/intern/learning/result`，原「正式考核成绩」）

原名「成绩与证书」，改为**正式考核成绩查询与统计页**，自上而下：

| 区块 | 内容 |
|---|---|
| ① 结果总览 | 是否通过（大状态）+ 理论分 / 实操分 / 综合分（综合分 = 理论×权重 + 实操×权重，权重由配置决定） |
| ② 对比统计 | 三柱对比：我的分 vs 部门平均分 vs 通过线（60）；另附得分率与排名 |
| ③ 成绩明细表 | 考核名称 / 类型（理论·实操）/ 时间 / 分数 / 用时 / 状态（用表格，不用卡片堆） |
| ④ 薄弱模块分析 | 理论按知识模块统计得分率横条，定位最弱模块；实操按考核要点给评价 |
| ⑤ 证书区 | 通过显示证书卡片 + 下载；未通过显示"完成培养并审批通过后生成" |

统计口径：**只统计正式考核成绩**（模拟自测不计入）；部门平均分仅对本部门范围计算，跨部门不可见。

### 6.7 能力画像（工作台 ④ 区块 + 独立下钻页）

能力画像是本系统区别于普通培训系统的核心，**必须留在工作台**，并配一个下钻详情页。

**数据来源与"逐步点亮"机制**

画像不是一次成型的，按数据来源逐步点亮。四个维度各有来源与权重：

| 维度 | 数据来源 | 表 | 未生成时的表现 |
|---|---|---|---|
| 学习投入 | 课程完成率 + 学习时长 | `study_record` | 有课程即生成 |
| 理论掌握 | 模拟考核 / 正式理论正确率 | `answer_sheet` / `assessment_result` | `--` 待考核 |
| 实践能力 | 实操提交物批阅结果 | `practical_submission` | `--` 待批阅 |
| 规范遵从 | 协议签署 + 按规范完成率 | `agreement_signature` / `study_record` | 有协议即生成 |

维度由 `ability_dimension` 按岗位配置（不同岗位维度可以不同），`stage_evaluation.dim_score_json` 承载管理员阶段评价的分值，`profile_snapshot` 存综合快照（含 `dim_scores_json`、`data_completeness`、`source_summary_json`、`algorithm_version`）。

**展示要点**

1. **雷达图 + 数据完整度**并列。完整度 = 已生成来源数 / 全部来源数，让实习生明白"画像还没全，是因为我还没考/还没批阅"，而不是"系统算错了"。
2. **未测评维度画虚线轴**并标注"待测评"，不要用 0 分画点——0 分和未测评是两件事。
3. **每个维度标注来源与样本量**。例如"理论掌握 58（来源：模拟自测 10 题）"，避免画像变成无法追溯的黑箱。
4. **画像与待办联动**，把"补哪个数据能点亮哪个维度"直接写成待办。
5. 阶段评价有 `visible_scope` 字段（`ADMIN_ONLY` / `OPEN`）：**只有 `OPEN` 的评价才允许出现在实习生端画像**，后端必须过滤，前端不能靠隐藏。
6. 画像数据不足时不给综合值，显示 `--` + "完成课程与考核后生成"，不得虚构。

### 6.8 管理端配套：部门管理员「考核配置与发布」（正式考核的内容来源）

正式考核由**对应部门管理员**配置发布，管理端页面结构（与实习生端 `正式考核` 页一一对应）：

| 区块 | 内容 |
|---|---|
| ① 备考资料 | 上传考试指南（PDF）；已上传显示文件名 + 「上传考试指南」「文件预览」；右上角状态标签（已发布 / 草稿） |
| ② 理论考试 | **考试信息**：考试形式 / 考试时长 / 通过分数-总分 / 考试题型 / 题目数量 / 试题分值；**试题分布表**：知识模块 / 题目数量 / 知识点占比 / 单选题占比 / 单选题数量 / 多选题占比 / 操作（编辑·删除）；顶部「新增 / 保存 / 保存发布」 |
| ③ 实操考试 | **考试信息**：同理论六项；**考试题目表**：题目序号 / 题目名称 / 题目描述 / 考核要点 / 操作（编辑·删除）；顶部「新增 / 保存 / 保存发布」 |
| 底部条 | 「当前版本 v1.0 · 最近更新 时间 · 发布后在实习生考核认证页面生效」+「保存并发布」主按钮 |

**权限与数据边界**：

- 部门管理员只能配置**本部门**的正式考核；`dept_id` 强制取当前登录人部门，不可跨部门。
- 理论考试的「试题分布」校验：各知识模块知识点占比之和 = 100%、题目数量之和 = 考试信息里的题目数量，否则不允许发布。
- 发布为**版本化**动作：每次发布生成新版本号，实习生端读的是"当前已发布版本"，草稿不生效（对应底部提示语）。
- 超管**只读统计**，不修改业务实例（不参与考核配置的编辑）。

**发布 → 消费对应关系**：

| 管理端配置 | 实习生端呈现 |
|---|---|
| 备考资料（考试指南 PDF） | 正式考核页 ① 备考资料卡片（预览 / 下载） |
| 理论考试 考试信息 + 试题分布 | 正式考核页 ③ 理论考试卡（信息 + 模块分布） |
| 实操考试 考试信息 + 考试题目 | 正式考核页 ④ 实操考试卡（信息 + 题目列表） |
| 保存并发布 | 正式考核页顶部发布信息条（部门 / 版本 / 时间） |

---

## 七、与现有代码的对应关系（含落地状态，2026-09-17 核对）

图例：✅ 已实现并接真后端 ｜ 🟡 页面已建但内容仍是示例数据 ｜ ⬜ 未建

| 页面 | 实际文件 | 路由 | 状态 / 说明 |
|---|---|---|---|
| 工作台 | `views/index.vue` | `/index` | ✅ 已接真实学习数据（`listLearningCourses()`）；KPI 6 项 / 完成率趋势 / 成绩图仍为占位。**本页只动 template + style** |
| 页签壳 | `views/assessment/learning/shell.vue` | 包裹 `learning/*` | ✅ 已实现 |
| 页签定义 | `utils/internTabs.js` | — | ✅ 已实现（侧栏与页签壳的**单一数据源**） |
| 在线学习 | `views/assessment/learning/index.vue` | `/assessment/intern/learning/courses` | ✅ 已实现（真后端；本次仅换入口 + 样式重排） |
| 课程学习页 | `views/assessment/learning/detail.vue` | `…/learning/course/:courseId` | ✅ 已实现（保持嵌入 Layout，不改全屏） |
| 备考资料 | `views/assessment/guide/index.vue` | `…/learning/guide` | 🟡 页面已建，内容示例数据（等 `material` 读接口） |
| 模拟考核 · 理论 | `views/assessment/practice/index.vue` | `…/learning/mock` | 🟡 抽题与判分走真后端，顶部统计条为示例数据 |
| **模拟考核 · 实操题库** | `views/assessment/practice/subject.vue` | `/assessment/intern/practice-subject` | ✅ **已闭环（本次，见 §6.4.1）** |
| **实操题详情** | `views/assessment/practice/subjectDetail.vue` | `/assessment/intern/practice-subject/:id` | ✅ **已闭环（本次）** |
| 模拟考核回顾 | `views/assessment/practice/record.vue` | `/assessment/intern/mock-exam/record/:recordId` | ✅ 已实现 |
| 正式考核 | `views/assessment/exam/index.vue` | `…/learning/exam` | 🟡 页面已建，示例数据 |
| 考核成绩与转正申请 | `views/assessment/result/index.vue` | `…/learning/result` | 成绩全真实（`answer-sheet/my` + `detail`）；部门平均/薄弱模块/证书/转正申请=示例或演示态 |
| 能力画像详情 | 待新建 `views/assessment/portrait/index.vue` | `…/dashboard/portrait/:period` | ⬜ 未建 |
| 路由 | `router/index.js` → `dynamicRoutes` | — | ✅ 已按 §四 落地 |
| 侧栏 | `store/modules/permission.js` → `buildInternSidebar()` | — | ✅ 已落地（**改这里，不是改 `sys_menu`**，见 §五） |
| 公共骨架页 | `views/assessment/index.vue` | 被 4 条路由复用 | ⚠️ **不要改**：「路由驱动 + `seedRows()` 假数据」的公共件，其他页面还在用 |

---

## 八、实施状态与下一步

**已完成（截至 2026-09-17）：**

1. ✅ 页签壳 `shell.vue` + `utils/internTabs.js` + 路由改造（5 页签跑通，旧路径 `study-guide` / `mock-exam` 保留 redirect 兼容）
2. ✅ 侧栏切到「工作台 + 学习与考核」两项（**改 `buildInternSidebar()`，不是 `sys_menu`**）
3. ✅ 在线学习列表 + 课程学习页（换入口 + 样式重排，script 数据层与 API 调用保留）
4. ✅ **实操题库闭环**：幂等迁移 + 实体 / Mapper / Service 扩展 + 实习生列表 / 详情页 + 管理端题库页 + 种子数据（详见 §6.4.1）
5. ✅ 工作台接真实学习数据；**2026-09-17 按视觉稿 i2 整体改版**：11 区块（身份条 → 6 KPI → 完成率趋势含 70% 门槛虚线 → 能力雷达 → 课程横条 → 学习时长 → 考核成绩 → 培养 4 步 → 待办 → 通知 → 协议证书条）。`views/index.vue` 实习生分支 template/style 重写、script 数据层与 `listLearningCourses` 绑定保留；真实数据块直接用 `learningOverview`/`previewCourses`/`protocolStatus`，无接口块（学习时长/模拟正确率/雷达/通知/入职天数/历史趋势）用示例数据 + `.dsample` 橙色标注，不以 0 占位
6. ✅ **能力画像详情页按视觉稿 i9 落地（2026-09-17）**：新建 `assessment/portrait/index.vue`（多维雷达 + 维度明细与来源表 + OPEN 阶段评价 + 多周期趋势），路由追加 `intern/portrait`（遮蔽 sys_menu 同路径骨架菜单）与 `intern/scores → learning/result` 重定向；学习投入样本量接真实 `listLearningCourses`，分值/评价/趋势为示例数据并标注
7. ✅ **模拟考核 / 正式考核按视觉稿 i4、i6 改版（2026-09-17 第二轮）**：
   - `assessment/practice/index.vue`（i4）：顶部 6 格模拟统计条（自测次数 / 平均正确率含迷你折线 / 最高正确率 / 实操题量 / 题库覆盖 / 最近自测，均由真实记录计算）+ 理论模拟卡（说明 + 历史记录表 + 开始新自测）+ 实操题库（hero 统计 + 按 `direction` 分组的卡片网格，真实 `practice-subject/published`）；作答与逐题回顾两阶段沿用原实现
   - `assessment/exam/index.vue`（i6）：发布信息条 + **考核场次列表**（同一实习生可阶段性多次参加：已通过 / 待批阅 / 进行中 / 未开始，场次=考核名称批次标识分组，两环节各自状态）+ 备考资料 + 资格校验（完成率与协议真实，剩余次数示例）+ 理论/实操环节卡；作答与完成阶段沿用原实现
   - **多次参加的后端复用结论**：一个批次 = 一条 `exam` 记录，理论/实操 = 同批次下 `exam_type` 不同的两条记录（各自 `status`，天然支持分环节发布），**无需改表**；补考字段 `exam_rule_snapshot.retake_count` + `answer_sheet.retake_seq` 已存在，但 `startExam` 目前硬拦截重复参加，批次内补考放行属 P1 待补
9. ✅ **理论考试配置化：实习生端按部门配置抽题（2026-09-17 第四轮）**：部门管理员在「模拟备考管理 → 模拟理论考核配置」配置**知识分布（题库知识点 × 抽题数量）+ 题型配比 + 分值 + 通过线**并保存生效后，实习生「模拟考核 → 开始新自测」调用 `/business/practice/start` 即**按配置抽题**（实测知识点逐项命中：Java 1 / MySQL 2 / 若依 2 / Docker 2 / Redis 2 / 答题规则 1），交卷按配置分值判分（score/fullScore/passLine/passed 真实返回），模拟页展示「本部门配置：N 题 · 单选x多选y判断z · 通过线 p 分 + 知识分布」。
   - 正式考核侧：部门端「正式考核管理」可配置**知识分布 + 发布时间（立即/定时 + 起止时间）+ 指定人员（全部在培 / 指定名单）**；实习生端场次卡显示**真实时间窗**与「指定人员」标记，未到开放时间无法开始（实测返回「该考核尚未开放，开放时间 …」），未在名单内的实习生看不到该场次。
10. ✅ **部门管理员登录落点改为部门工作台（2026-09-17 第五轮）**：`src/permission.js` 的 `protectedTarget()` 把部门管理员（`DEPT_ADMIN` 且非 `SUPER_ADMIN`/`admin`）对 `/index`（或 `/`）的访问改投 `/department/dashboard`；面包屑与登出跳转同步适配。**实习生端不受影响**（仍使用 `/index` 的 i2 工作台）。
11. ✅ **考核成绩与转正申请页不再展示电子证书（2026-09-17 第六轮）**：该页去掉「电子证书」区块（原 ⑥），改为在「转正申请」卡底部保留一句**证书去向指引**（按状态动态文案），并给出「去工作台查看」入口；**电子证书统一在工作台底部「协议与证书」栏**查看与下载（工作台那条 `record-band` 文案同步对齐「部门管理员审批转正后生成」）。设计稿 i7 同步改为五块。
12. ✅ **「正式考核成绩」重设计为「考核成绩与转正申请」（2026-09-17 第三轮，视觉稿 i7）**：`views/assessment/result/index.vue` 全量重写 = ① 结果总览（取最好通过场次，综合分 = 理论×40% + 实操×60%）② 成绩明细（按**场次 + 环节**，未参加显示 `--` + 去向）③ 得分对比（我的综合 / 部门平均示例 / 通过线）④ 薄弱模块分析（橙色 <60%）⑤ **转正申请**（资格核对清单四项、四态状态时间线、申请表单 + 提交/撤回/重新提交）⑥ 电子证书（仅审批通过后生成）。
   - 联动改名：`utils/internTabs.js` 页签 label、`router/index.js` 的 `meta.title`、`shell.vue` 页头文案；工作台「下一步做什么」新增「提交转正申请」待办指向本页。
   - 真实数据：成绩与资格清单四项（完成率 / 考核通过 / 必修全完成 / 协议）全部由既有接口推导；部门平均、薄弱模块、证书、**转正申请单**为示例/演示态（`promotion_application` 零 Java 层）。
   - 两端契约：实习生提交 → 部门管理员**终审即生效并发证**（状态 `DEPT_PENDING → PASSED / REJECTED`，跳过 `SUPER_PENDING`），与部门端「实习转正审核」页共用同一状态机。

**下一步（多数依赖后端就绪）：**

| # | 事项 | 依赖 | 对应章节 |
|---|---|---|---|
| 7 | 「备考资料」改为真实读接口 | `material` 表（15 列齐全，**零 Java 层**） | §6.5 ① |
| 8 | 「正式考核」发布信息 / 资格校验 / 题目列表 | `exam` + `exam_paper_question` | §6.5 ③④ |
| 9 | 「考核成绩与转正申请」按批次统计 + 薄弱模块分析 + **转正申请单接口** | `question.knowledge_point`（前端可算）+ `promotion_application`（零 Java 层） | §6.6 |
| 10 | 能力画像分值 / 评价 / 周期趋势改真实接口（**页面已按 i9 落地**，当前示例数据） | `profile_snapshot` / `ability_dimension` / `evaluation`（零 Java 层，须后端过滤 ADMIN_ONLY） | §6.7 |
| 11 | 答题页 / 实操提交页（考核进行中） | `answer_sheet` / `subject_answer` | §三 下钻页 |

---

## 九、与统一视觉稿的对应关系（`实习生与部门管理员端整体设计稿.html`）

### 9.1 视觉稿结构

两角色**合并为同一份 HTML**（265 KB / 22 个 `<section>`），顶部有「**实习生端 / 部门管理员端**」切换按钮，切换的是 `body[data-role]`：

| 前缀 | 角色 | 节数 | 锚点 |
|---|---|---|---|
| `i*` | 实习生端 | 10 | `i0` ~ `i9` |
| `d*` | 部门管理员端 | 12 | `d1` ~ `d12` |

**CSS 隔离机制（改稿前必读）**：两份原稿共用同一套 `:root` 变量，但存在 **66 个同名选择器**（典型如 `.badge` 在实习生稿是红色、在管理端稿是灰色）。合并时**没有直接拼接**，而是给各稿的非共享规则分别加作用域前缀 —— 实习生侧 `.ri`、管理端侧 `.rd`；只有 `.doc / .doc-nav / .sec* / .tag / .kbd / .frame*` 等约 41 条**共享规则**保持无前缀（同名则合并声明，长的一方胜出，如 `.sec-desc b` 保留 `font-weight:600`）。`@media` 块内规则按同样规则各自加前缀。

> ⚠️ 因此**手改视觉稿时必须带作用域前缀**（如 `.ri .badge{...}`），否则会污染另一个角色的同页样式。

### 9.2 本文档 § 与视觉稿锚点对照

| 本文档章节 | 视觉稿锚点 | 备注 |
|---|---|---|
| §零 系统组织与岗位（实例基线） | `i0` | 两稿同一份组织数据 |
| §一 设计目标与三条原则 | `i1` 引言部分 | — |
| §二 一级目录设计 | `i1` | 侧栏两项 + 5 页签 |
| §6.1 工作台 | `i2` | 数据分析与可视化中心 |
| §6.2 在线学习 | `i3` | — |
| §6.3 课程学习页 | `i8` | 下钻页 |
| **§6.4 模拟考核（含 §6.4.1 实操题库）** | **`i4`** | 理论抽题 + 实操题库（本次闭环的那一块） |
| §6.5 正式考核 | `i6` | 只读消费管理端发布内容 |
| §6.6 考核成绩与转正申请 | `i7` | — |
| §6.7 能力画像 | `i9` | 工作台 ④ 下钻 |
| §6.8 管理端配套：考核配置与发布 | `i5`（实习生视角）/ `d8`（管理端主体） | 管理端细节见姊妹文档 §2.8 |
| — 管理端全量 12 节 | `d1` ~ `d12` | 归属姊妹文档 `部门管理员端目录与页面布局设计方案.md` |
| — 两角色联动映射（验收基准） | `d11` | 与本文档 §6.8 的「发布 → 消费」表互为镜像 |
| — 三个决策落地影响 | `d12` | 转正终审 / 分环节发布 / 拆多库 |

> 打开方式：浏览器直接打开本地 HTML 文件即可（无需服务）；顶部切换角色，左侧导航项点击跳转到对应节。


